# คู่มือ: smartspec_dependency_updater

## 📚 ภาพรวม

`/smartspec_dependency_updater` เป็น workflow เชิงรุกที่ออกแบบมาเพื่อทำให้กระบวนการสแกนหา dependency ที่ล้าสมัย, ประเมินผลกระทบ และสร้าง tasks สำหรับการอัปเดตที่ปลอดภัยเป็นไปโดยอัตโนมัติ

**ความสามารถหลัก:**
- ✅ สแกนหา dependency ที่ล้าสมัยใน package managers หลายตัว (npm, pip, Maven, etc.)
- ✅ ประเมินช่องโหว่ด้านความปลอดภัย (CVEs) และ breaking changes
- ✅ สร้างรายงานการวิเคราะห์ผลกระทบ
- ✅ สร้าง tasks สำหรับการอัปเดต dependency พร้อมคำแนะนำที่ชัดเจน
- ✅ ผสานการทำงานกับ `smartspec_implement_tasks` สำหรับการอัปเดตอัตโนมัติ

---

## 🎯 การใช้งานพื้นฐาน

โดยทั่วไป workflow นี้จะรันตามกำหนดเวลา (เช่น รายสัปดาห์) เป็นกระบวนการเบื้องหลัง

### การรัน Scanner

```bash
/smartspec_dependency_updater --run-scan
```

**พฤติกรรม:**
1.  สแกนไฟล์ dependency ทั้งหมดใน repository
2.  สร้างรายงานใน `.spec/reports/dependency-updates/`
3.  ไม่สร้าง tasks เว้นแต่จะใช้ `--apply`

### การสร้าง Tasks สำหรับอัปเดต

```bash
/smartspec_dependency_updater --run-scan --apply --auto-update-safe
```

**พฤติกรรม:**
1.  รันการสแกน
2.  สร้างรายงาน
3.  สร้างไฟล์ `tasks.md` พร้อม tasks สำหรับการอัปเดตที่ปลอดภัยทั้งหมด (minor และ patch versions)

---

## ⚙️ วงจร Workflow

1.  **สแกน:** workflow สแกนหา dependency ที่ล้าสมัย
2.  **ประเมิน:** ตรวจสอบช่องโหว่ด้านความปลอดภัยและ breaking changes
3.  **รายงาน:** สร้างรายงานโดยละเอียด
4.  **วางแผน:** ด้วย `--apply` จะสร้างไฟล์ `tasks.md`
5.  **Implement:** `smartspec_implement_tasks` สามารถใช้เพื่อทำการอัปเดตโดยอัตโนมัติ
6.  **ตรวจสอบ:** workflow การ implement จะรัน tests เพื่อตรวจสอบการอัปเดต

---

## 🚩 Flags

- `--run-scan`: **(จำเป็น)** รันการสแกน dependency
- `--package-manager <name>`: สแกนเฉพาะ package manager ที่ระบุ (เช่น `npm`, `pip`)
- `--security-level <level>`: รายงานเฉพาะช่องโหว่ที่สูงกว่าระดับที่กำหนด (เช่น `high`, `critical`)
- `--auto-update-safe`: สร้าง tasks สำหรับการอัปเดตที่ปลอดภัยโดยอัตโนมัติ (minor และ patch versions)
- `--apply`: สร้างไฟล์ `tasks.md` พร้อม tasks สำหรับอัปเดต
