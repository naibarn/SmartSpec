# คู่มือ: smartspec_feedback_aggregator

## 📚 ภาพรวม

`/smartspec_feedback_aggregator` เป็นเครื่องมือของ **การปรับปรุงอย่างต่อเนื่อง** ในระบบนิเวศของ SmartSpec ทำหน้าที่ปิดวงจรระหว่าง production และ development โดยการรวบรวม feedback จากหลายแหล่งและแปลงเป็นข้อมูลเชิงลึกที่สามารถนำไปปฏิบัติได้

**ความสามารถหลัก:**
- ✅ รวบรวมข้อมูลจากการตรวจสอบ production, post-mortems และช่องทาง feedback ของผู้ใช้
- ✅ ใช้ NLP เพื่อวิเคราะห์และจัดหมวดหมู่ feedback
- ✅ ระบุแนวโน้มและปัญหาที่เกิดขึ้นซ้ำๆ
- ✅ สร้างคำแนะนำสำหรับ specs, tasks หรือ refactors ใหม่
- ✅ สร้างแดชบอร์ด feedback สำหรับการแสดงผล

---

## 🎯 การใช้งานพื้นฐาน

โดยทั่วไป workflow นี้จะรันตามกำหนดเวลา (เช่น รายวัน) เป็นกระบวนการเบื้องหลัง

### การรัน Aggregator

```bash
/smartspec_feedback_aggregator --run-once
```

**พฤติกรรม:**
1.  สำรวจแหล่ง feedback ทั้งหมดที่กำหนดค่าไว้
2.  ประมวลผลรายการ feedback ใหม่
3.  อัปเดตแดชบอร์ด feedback
4.  สร้างคำแนะนำใหม่
5.  ส่งรายงานสรุปไปยังผู้มีส่วนได้ส่วนเสีย

---

## ⚙️ วงจร Feedback

Workflow นี้สร้างวงจร feedback ที่มีประสิทธิภาพ:

1.  **การตรวจสอบ Production:** `smartspec_production_monitor` ตรวจพบการลดลงของประสิทธิภาพและสร้างรายงาน
2.  **Feedback Aggregator:** aggregator ประมวลผลรายงานและระบุแนวโน้ม: "p99 latency สำหรับ `spec-core-001` เพิ่มขึ้น 20% ในช่วง 7 วันที่ผ่านมา"
3.  **การสร้างคำแนะนำ:** aggregator สร้างคำแนะนำ:
    -   **ประเภท:** การสืบสวนประสิทธิภาพ
    -   **หัวข้อ:** สืบสวนการถดถอยของ p99 latency ใน `spec-core-001`
    -   **การดำเนินการ:** สร้าง spec ใหม่สำหรับการตรวจสอบประสิทธิภาพ
4.  **Spec ใหม่:** นักพัฒนาใช้คำแนะนำเพื่อสร้าง spec ใหม่ด้วย `smartspec_generate_spec_from_prompt`
5.  **การพัฒนา:** spec ใหม่ถูกวางแผน, สร้าง tasks และ implement
6.  **การ Deploy:** การแก้ไขถูก deploy ไปยัง production
7.  **การตรวจสอบ:** `smartspec_production_monitor` ยืนยันว่า latency กลับสู่ปกติ

**วงจรเสร็จสมบูรณ์!**

---

## 💡 แหล่ง Feedback

Workflow นี้สามารถกำหนดค่าให้ดึงข้อมูลจาก:

-   **รายงาน SmartSpec:**
    -   `.spec/reports/production-monitoring/`
    -   `.spec/reports/post-mortems/`
-   **เครื่องมือ Feedback ของผู้ใช้:**
    -   Zendesk
    -   Intercom
    -   Jira
-   **Version Control:**
    -   GitHub Issues
    -   GitLab Issues

---

## 🚩 Flags

- `--run-once`: **(จำเป็น)** รันกระบวนการรวบรวมครั้งเดียวแล้วออก
- `--source <name>`: รันการรวบรวมสำหรับแหล่งข้อมูลที่ระบุเท่านั้น
- `--since <date>`: ประมวลผลรายการ feedback ที่สร้างขึ้นตั้งแต่วันที่ระบุ
