# คู่มือ: smartspec_performance_profiler

## 📚 ภาพรวม

`/smartspec_performance_profiler` เป็น workflow พิเศษสำหรับระบุและวางแผนการแก้ไขปัญหาคอขวดด้านประสิทธิภาพใน codebase

**ความสามารถหลัก:**
- ✅ ผสานการทำงานกับเครื่องมือ profiling (เช่น `pprof`, `JProfiler`) เพื่อรวบรวมข้อมูลประสิทธิภาพ
- ✅ วิเคราะห์ข้อมูล profiling เพื่อระบุคอขวด (CPU, memory, I/O)
- ✅ แนะนำกลยุทธ์การปรับปรุงประสิทธิภาพพร้อมประเมินผลที่จะได้รับ
- ✅ สร้างแผนการปรับปรุงประสิทธิภาพ
- ✅ สร้าง tasks สำหรับงานปรับปรุงใน `tasks.md`

---

## 🎯 การใช้งานพื้นฐาน

workflow นี้โดยทั่วไปจะรันโดยวิศวกรประสิทธิภาพหรือนักพัฒนาเมื่อสงสัยว่ามีปัญหาด้านประสิทธิภาพ

### การรัน Profile

```bash
/smartspec_performance_profiler --run-profile --scenario api-stress-test
```

**พฤติกรรม:**
1.  รันสถานการณ์ทดสอบประสิทธิภาพ `api-stress-test`
2.  รวบรวมข้อมูล profiling
3.  สร้างรายงานใน `.spec/reports/performance/` พร้อมคอขวดที่ระบุ

### การสร้าง Tasks สำหรับปรับปรุง

```bash
/smartspec_performance_profiler --run-profile --apply --min-gain 10
```

**พฤติกรรม:**
1.  รันโปรไฟล์ประสิทธิภาพปริยาย
2.  สร้างรายงาน
3.  สร้างไฟล์ `tasks.md` พร้อม tasks สำหรับโอกาสในการปรับปรุงทั้งหมดที่คาดว่าจะให้ผลตอบแทนด้านประสิทธิภาพอย่างน้อย 10%

---

## ⚙️ วงจร Workflow

1.  **โปรไฟล์:** workflow รันแอปพลิเคชันพร้อมกับ profiler เพื่อรวบรวมข้อมูล
2.  **วิเคราะห์:** วิเคราะห์ข้อมูลเพื่อหา hotspots และคอขวด
3.  **วางแผน:** สร้างแผนการปรับปรุงโดยละเอียด
4.  **สร้าง Task:** ด้วย `--apply` จะสร้างไฟล์ `tasks.md`
5.  **Implement:** นักพัฒนาใช้แผนและ tasks เพื่อ implement การปรับปรุง

---

## 🚩 Flags

- `--run-profile`: **(จำเป็น)** รันการทำโปรไฟล์ประสิทธิภาพ
- `--scenario <name>`: รันสถานการณ์ทดสอบประสิทธิภาพที่เฉพาะเจาะจง (เช่น `high-load`, `api-stress-test`)
- `--profiler <name>`: ใช้ profiler ที่เฉพาะเจาะจง (เช่น `pprof`, `jprofiler`)
- `--min-gain <percentage>`: รายงานเฉพาะการปรับปรุงที่มีเปอร์เซ็นต์การปรับปรุงประสิทธิภาพที่คาดหวังขั้นต่ำ
- `--apply`: สร้างไฟล์ `tasks.md` พร้อม tasks สำหรับปรับปรุง
