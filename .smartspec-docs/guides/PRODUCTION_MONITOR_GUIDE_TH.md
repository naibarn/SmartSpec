# คู่มือ: smartspec_production_monitor

## 📚 ภาพรวม

`/smartspec_production_monitor` เป็น workflow หลักสำหรับ **การดำเนินงานบน production** ทำหน้าที่เป็นสะพานเชื่อมระหว่างบริการที่ทำงานอยู่จริงกับระบบนิเวศของ SmartSpec โดยการตรวจสอบสถานะและประสิทธิภาพอย่างต่อเนื่อง

**ความสามารถหลัก:**
- ✅ เชื่อมต่อกับแพลตฟอร์ม observability (Prometheus, Datadog, etc.)
- ✅ ติดตาม KPIs เทียบกับ NFRs ที่กำหนดใน `spec.md`
- ✅ สร้างการแจ้งเตือนเมื่อเกิด SLO breaches
- ✅ สร้างรายงานประสิทธิภาพสำหรับ feedback loop
- ✅ สามารถทำงานแบบครั้งเดียวหรือทำงานต่อเนื่องเป็น daemon

---

## 🎯 การใช้งานพื้นฐาน

### 1. ทำงานต่อเนื่องเป็น Daemon

```bash
/smartspec_production_monitor --spec-id spec-core-001-authentication --daemon
```

**พฤติกรรม (Default):**
- ทำงานใน background
- สอบถามเมตริกอย่างต่อเนื่องทุก 60 วินาที (สามารถกำหนดค่าได้)
- เรียกใช้ `smartspec_incident_response` หาก SLO ถูกละเมิด
- สร้างรายงานประสิทธิภาพรายวัน

### 2. ทำงานเป็นการตรวจสอบสถานะครั้งเดียว

```bash
/smartspec_production_monitor --spec-id spec-core-001-authentication --run-once
```

**พฤติกรรม:**
- สอบถามเมตริกครั้งเดียว
- พิมพ์สถานะปัจจุบันไปยังคอนโซล
- ออกจากการทำงาน

**กรณีการใช้งาน:**
- ตรวจสอบสถานะอย่างรวดเร็วระหว่างการ deploy
- ตรวจสอบสถานะของระบบหลังจากการ rollback
- ตรวจสอบด้วยตนเองระหว่างการสืบสวนเหตุการณ์

---

## ⚙️ การกำหนดค่า

Workflow นี้ใช้แหล่งข้อมูลการกำหนดค่าหลักสองแหล่ง:

### 1. `spec.md` (Non-Functional Requirements)

Workflow จะอ่านส่วน NFRs ของ `spec.md` ของคุณเพื่อกำหนด SLOs เป้าหมาย

```markdown
### Non-Functional Requirements

| Category | Requirement | Metric | Target |
|---|---|---|---|
| Performance | p99 Latency | `api_latency_p99` | < 200ms |
| Reliability | Error Rate | `api_error_rate` | < 0.1% |
| Availability | Uptime | `api_uptime` | > 99.9% |
```

### 2. `observability.yaml`

ไฟล์นี้ (สร้างโดย `smartspec_observability_configurator`) จะบอก workflow ว่าต้องสอบถามเมตริก *อย่างไร*

```yaml
provider: datadog
api_key: "{{env.DATADOG_API_KEY}}"
metrics:
  - name: api_latency_p99
    query: "avg:trace.rack.request.duration{service:my-api,env:prod}.p99"
  - name: api_error_rate
    query: "sum:trace.rack.request.errors{service:my-api,env:prod}.as_rate()"
```

---

## 💡 การทำงาน

1.  **โหลดการตั้งค่า:** อ่าน `spec.md` และ `observability.yaml`
2.  **สอบถามเมตริก:** ใช้ API ของผู้ให้บริการเพื่อดึงค่าเมตริกล่าสุด
3.  **เปรียบเทียบกับ SLOs:** เปรียบเทียบค่าที่ดึงมากับเป้าหมายจาก NFRs
4.  **การแจ้งเตือน:** หากเมตริกอยู่นอก SLO จะเรียกใช้ `smartspec_incident_response` พร้อม payload โดยละเอียด
5.  **การรายงาน:** สร้างรายงานรายวัน/รายสัปดาห์และเรียกใช้ `smartspec_feedback_aggregator`

---

## 🚩 Flags

- `--spec-id <id>`: **(จำเป็น)** ID ของ spec ที่จะตรวจสอบ
- `--daemon`: ทำงานเป็นกระบวนการเบื้องหลังอย่างต่อเนื่อง
- `--run-once`: ทำงานครั้งเดียวแล้วออก
- `--interval <seconds>`: (โหมด daemon เท่านั้น) ช่วงเวลาระหว่างการสอบถามเมตริก ค่าเริ่มต้นคือ 60
