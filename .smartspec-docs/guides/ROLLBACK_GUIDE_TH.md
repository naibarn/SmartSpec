# คู่มือ: smartspec_rollback

## 📚 ภาพรวม

`/smartspec_rollback` เป็น workflow ความปลอดภัยที่สำคัญสำหรับ **การดำเนินงานบน production** ให้วิธีการย้อนกลับการ deploy ที่ล้มเหลวไปยังเวอร์ชันที่เสถียรก่อนหน้าอย่างปลอดภัยและอัตโนมัติ

**ความสามารถหลัก:**
- ✅ เชื่อมต่อกับระบบ deploy
- ✅ สามารถเรียกใช้งานได้โดยอัตโนมัติหรือด้วยตนเอง
- ✅ สร้างแผนการ rollback ที่ชัดเจนและตรวจสอบได้
- ✅ ทำการตรวจสอบความปลอดภัยก่อนดำเนินการ
- ✅ ต้องการการอนุมัติด้วยตนเองสำหรับการ rollback ที่มีความเสี่ยงสูง

---

## 🎯 การใช้งานพื้นฐาน

### 1. การเรียกใช้ Rollback ระหว่างเหตุการณ์

นี่เป็นกรณีการใช้งานที่พบบ่อยที่สุด โดยทั่วไปจะรันโดยวิศวกรในช่องทางการตอบสนองต่อเหตุการณ์

```bash
/smartspec_rollback --failed-deployment-id deploy-12345
```

**พฤติกรรม:**
1.  ระบุเวอร์ชันที่เสถียรล่าสุด (เช่น `v1.2.0`)
2.  สร้างแผนการ rollback
3.  **ขอการอนุมัติด้วยตนเอง** ในช่องทางเหตุการณ์
4.  ดำเนินการ rollback หลังจากการอนุมัติ
5.  ตรวจสอบว่าระบบเสถียร
6.  โพสต์รายงานไปยังช่องทางเหตุการณ์

### 2. Rollback อัตโนมัติจาก Quality Gate

Workflow นี้สามารถรวมเข้ากับ CI/CD pipeline ได้

```bash
# ในสคริปต์ CI/CD ของคุณ
/smartspec_quality_gate --deployment-id deploy-12345
if [ $? -ne 0 ]; then
  /smartspec_rollback --failed-deployment-id deploy-12345 --auto-approve
fi
```

**พฤติกรรม:**
- หาก quality gate ล้มเหลว การ rollback จะถูกเรียกใช้งานโดยอัตโนมัติ
- `--auto-approve` จะข้ามขั้นตอนการอนุมัติด้วยตนเอง ทำให้สามารถ rollback ได้เร็วขึ้นและเป็นอัตโนมัติเต็มรูปแบบใน CI/CD

---

## ⚙️ แผนการ Rollback

ก่อนดำเนินการ workflow จะสร้างแผนที่ลักษณะดังนี้:

```markdown
# แผนการ Rollback: deploy-12345

**เหตุผล:** Quality gate ล้มเหลว (p99 latency > 200ms)
**จากเวอร์ชัน:** `v1.2.1`
**ไปยังเวอร์ชัน:** `v1.2.0`

## ⚠️ การตรวจสอบความปลอดภัย

- [✅] ไม่พบการเปลี่ยนแปลง schema ของฐานข้อมูลที่เข้ากันไม่ได้
- [✅] ไม่พบความเสี่ยงในการสูญเสียข้อมูล

## ขั้นตอน

1.  สลับ traffic การผลิตจาก `v1.2.1` ไปยัง `v1.2.0`
2.  ลดขนาด instances ของ `v1.2.1` เป็น 0
3.  รันการทดสอบการตรวจสอบบน `v1.2.0`

**อนุมัติแผนนี้หรือไม่? (yes/no)**
```

---

## 🚩 Flags

- `--failed-deployment-id <id>`: **(จำเป็น)** ID ของการ deploy ที่ล้มเหลว
- `--target-version <version>`: ระบุเวอร์ชันที่จะ rollback ไปด้วยตนเอง
- `--auto-approve`: ข้ามการอนุมัติด้วยตนเอง **ใช้ด้วยความระมัดระวัง**
- `--dry-run`: สร้างแผนการ rollback แต่ไม่ดำเนินการ
