| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_api_contract_validator คู่มือ (TH) | 6.0 | /smartspec_api_contract_validator | 6.0.x |

# /smartspec_api_contract_validator คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

workflow `/smartspec_api_contract_validator` ทำหน้าที่ตรวจสอบความถูกต้องของ API contracts เทียบกับ specifications และยืนยันความสอดคล้องกับ interfaces ที่กำหนดไว้

**วัตถุประสงค์:** ตรวจสอบความถูกต้องของ API contracts เทียบกับ specifications เพื่อให้แน่ใจว่า endpoints, พารามิเตอร์ และ responses ตรงตาม interfaces ที่กำหนด

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** quality-testing

---

## 2. การใช้งาน

### 🔗 การใช้งาน CLI

```bash
/smartspec_api_contract_validator \
  <spec_md> \
  [--contract-file <path>] \
  [--strict]
```

### การใช้งาน Kilo Code

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องระบุ flag `--platform kilo` เสมอ

```bash
/smartspec_api_contract_validator.md \
  <spec_md> \
  [--contract-file <path>] \
  [--strict] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: ตรวจสอบ API Contract (CLI)

**สถานการณ์:** นักพัฒนาต้องการตรวจสอบว่า API implementation ตรงกับ contract หรือไม่

**คำสั่ง:**

```bash
/smartspec_api_contract_validator specs/api/rest_endpoints/spec.md \
  --contract-file api/openapi.yaml
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดไฟล์ spec และ contract
2. ตรวจสอบ endpoints, พารามิเตอร์ และ responses
3. สร้างรายงานการตรวจสอบ
4. รหัสออก `0` หากถูกต้อง, `1` หากพบข้อผิดพลาด

### กรณีการใช้งาน 2: โหมดตรวจสอบเข้มงวด (Kilo Code)

**สถานการณ์:** pipeline CI ต้องการการตรวจสอบ contract อย่างเข้มงวด

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_api_contract_validator.md \
  specs/api/graphql/spec.md \
  --strict \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. ดำเนินการตรวจสอบแบบเข้มงวด
2. ความไม่ตรงกันใด ๆ จะทำให้ล้มเหลว
3. รหัสออก `0` หากตรงกันสมบูรณ์, `1` หากพบปัญหา

### กรณีการใช้งาน 3: ผลลัพธ์ JSON สำหรับการรวมระบบ (CLI)

**สถานการณ์:** การรวมระบบกับเครื่องมือภายนอกต้องการผลลัพธ์ในรูปแบบ JSON

**คำสั่ง:**

```bash
/smartspec_api_contract_validator specs/api/webhooks/spec.md \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. ดำเนินการตรวจสอบ
2. ผลลัพธ์จะรวมไฟล์ `validation.json` ที่มีผลลัพธ์ในรูปแบบโครงสร้าง
3. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `<spec_md>` | `<path>` | เส้นทางไปยังไฟล์ spec.md | ต้องอยู่ภายใต้ `specs/**` |

### flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--config` | เส้นทางไปยังไฟล์ configuration | `.spec/smartspec.config.yaml` | `cli` \| `kilo` \| `ci` \| `other` |
| `--platform` | แพลตฟอร์มที่ใช้รัน **จำเป็นสำหรับ Kilo Code** | (อนุมานค่า) | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### flags เฉพาะ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--contract-file` | เส้นทางไปยังไฟล์ API contract (OpenAPI, GraphQL schema ฯลฯ) | (ตรวจจับอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--strict` | เปิดใช้งานโหมดตรวจสอบเข้มงวด | `false` | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/api-contract-validator/<run-id>/validation.md` | รายงานการตรวจสอบ |

---

## 6. หมายเหตุ

- **Platform Flag:** เมื่อใช้ Kilo Code ต้องระบุ `--platform kilo` เสมอ  
- **โหมดเข้มงวด:** ใช้ `--strict` เพื่อการตรวจสอบแบบไม่มีข้อยกเว้น

---

**จบคู่มือ**