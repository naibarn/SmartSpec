| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_code_assistant คู่มือ (TH) | 6.0 | /smartspec_code_assistant | 6.0.x |

# /smartspec_code_assistant คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

คู่มือนี้อธิบายวิธีการใช้งาน workflow:

`/smartspec_code_assistant` workflow ให้ความช่วยเหลือด้านโค้ดด้วย AI สำหรับการดำเนินงาน การแก้ไขข้อผิดพลาด และการปรับปรุงโค้ดภายในกรอบการกำกับดูแลของ SmartSpec

**วัตถุประสงค์:** ช่วยนักพัฒนาในการดำเนินงานโค้ด แก้ไขข้อผิดพลาด และปรับปรุงโค้ด พร้อมทั้งรักษาหลักการกำกับดูแลของ SmartSpec และมาตรฐานคุณภาพโค้ด

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** implementation

---

## 2. การใช้งาน

workflow นี้รองรับการเรียกใช้งานผ่าน Command Line Interface (CLI) และ Kilo Code (สภาพแวดล้อมสคริปต์ภายใน)

### 🔗 การใช้งาน CLI

การเรียกใช้งาน CLI จำเป็นต้องระบุประเภทความช่วยเหลือและไฟล์หรือภารกิจเป้าหมาย

```bash
/smartspec_code_assistant \
  --mode <implement|fix|refactor> \
  --target <file_or_task> \
  [--context <spec_or_plan>] \
  [--apply]
```

### การใช้งาน Kilo Code

การเรียกใช้งาน Kilo Code มีโครงสร้างเหมือนกับ CLI โดยมักใช้ใน pipeline อัตโนมัติหรือสคริปต์ภายใน

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องระบุ flag `--platform kilo` ด้วยเสมอ

```bash
/smartspec_code_assistant.md \
  --mode <implement|fix|refactor> \
  --target <file_or_task> \
  [--context <spec_or_plan>] \
  [--apply] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: ดำเนินงานภารกิจ (CLI)

**สถานการณ์:** นักพัฒนาต้องการความช่วยเหลือในการดำเนินงานภารกิจเฉพาะจากไฟล์ tasks.md

**คำสั่ง:**

```bash
/smartspec_code_assistant \
  --mode implement \
  --target "TASK-001" \
  --context specs/auth/login_service/spec.md
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดภารกิจและบริบทของ spec
2. สร้างข้อเสนอแนะโค้ดสำหรับการดำเนินงาน
3. เขียนชุดตัวอย่างพรีวิวไปยัง `.spec/reports/code-assistant/<run-id>/`
4. ไม่มีการเปลี่ยนแปลงโค้ดจริง (ไม่มี flag `--apply`)
5. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: แก้ไขข้อผิดพลาดในไฟล์ (Kilo Code)

**สถานการณ์:** pipeline CI ตรวจพบข้อผิดพลาดในไฟล์ซอร์สและต้องการข้อเสนอแนะการแก้ไขอัตโนมัติ

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_code_assistant.md \
  --mode fix \
  --target src/services/payment.ts \
  --apply \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow วิเคราะห์ไฟล์เป้าหมายเพื่อหาข้อผิดพลาด
2. สร้างข้อเสนอแนะการแก้ไขตามรูปแบบข้อผิดพลาด
3. ด้วย flag `--apply` การแก้ไขจะถูกนำไปใช้กับไฟล์
4. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: ปรับปรุงโค้ด (CLI)

**สถานการณ์:** นักพัฒนาต้องการปรับปรุงโมดูลเพื่อเพิ่มคุณภาพและความสามารถในการดูแลรักษาโค้ด

**คำสั่ง:**

```bash
/smartspec_code_assistant \
  --mode refactor \
  --target src/utils/validation.ts \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow วิเคราะห์ไฟล์เป้าหมาย
2. สร้างข้อเสนอแนะการปรับปรุงโค้ด
3. ผลลัพธ์รวมไฟล์ `summary.json` ที่มีคำแนะนำการปรับปรุง
4. ไม่มีการเปลี่ยนแปลงโค้ดจริง (ไม่มี flag `--apply`)
5. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

พารามิเตอร์และ flags ต่อไปนี้ใช้ควบคุมการทำงานและพฤติกรรมของ `/smartspec_code_assistant` workflow

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `--mode` | `<string>` | โหมดความช่วยเหลือ: `implement`, `fix`, หรือ `refactor` | ต้องเป็นหนึ่งในค่าที่อนุญาต |
| `--target` | `<string>` | เส้นทางไฟล์เป้าหมายหรือรหัสภารกิจ | ต้องมีอยู่จริงและเข้าถึงได้ |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--config` | เส้นทางไปยังไฟล์กำหนดค่า SmartSpec | `.spec/smartspec.config.yaml` | `cli` \| `kilo` \| `ci` \| `other` |
| `--lang` | ภาษาสำหรับการสร้างรายงาน (เช่น `th`, `en`) | (ค่าเริ่มต้นของระบบ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--platform` | บริบทแพลตฟอร์มการทำงาน **จำเป็นสำหรับ Kilo Code** | (อนุมานอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--apply` | เปิดใช้งานการเปลี่ยนแปลงโค้ด | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--out` | เส้นทางฐานสำหรับผลลัพธ์ที่ปลอดภัย | `.spec/reports/code-assistant/` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--quiet` | ปิดการแสดงผลลัพธ์ล็อกปกติ | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Platform Support |
| :--- | :--- | :--- |
| `--context` | เส้นทางไปยังไฟล์ spec หรือ plan เพื่อเพิ่มบริบท | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

workflow สร้างผลลัพธ์ตามการตั้งค่าของมัน

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/code-assistant/<run-id>/...` | ผลลัพธ์ที่ปลอดภัย (ข้อเสนอแนะโค้ด, รายงานวิเคราะห์) |

---

## 6. หมายเหตุ

- **Platform Flag:** เมื่อใช้ Kilo Code ต้องระบุ `--platform kilo` เสมอเพื่อให้บริบทและการบันทึกถูกต้อง  
- **โหมดพรีวิว:** หากไม่มี `--apply` workflow จะสร้างพรีวิวที่ปลอดภัยโดยไม่แก้ไขไฟล์โค้ด  
- **การเขียนภายใต้การกำกับ:** ใช้ flag `--apply` เพื่อเปิดใช้งานการเปลี่ยนแปลงโค้ด  
- **ความตระหนักบริบท:** ระบุ `--context` กับไฟล์ spec หรือ plan เพื่อให้ข้อเสนอแนะโค้ดดียิ่งขึ้น  
- **การตั้งค่า:** workflow ปฏิบัติตามการตั้งค่าใน `.spec/smartspec.config.yaml`

---

**จบคู่มือ**