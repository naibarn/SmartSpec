| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_data_migration_generator คู่มือ (TH) | 6.0 | /smartspec_data_migration_generator | 6.0.x |

# /smartspec_data_migration_generator คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

workflow `/smartspec_data_migration_generator` สร้างสคริปต์การย้ายข้อมูลจากการเปลี่ยนแปลง schema

**วัตถุประสงค์:** สร้างสคริปต์การย้ายข้อมูลจากการเปลี่ยนแปลง schema เพื่อให้มั่นใจว่าการแปลงข้อมูลเป็นไปอย่างปลอดภัยและสามารถย้อนกลับได้

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** operations-deployment

---

## 2. การใช้งาน

### 🔗 การใช้งาน CLI

```bash
/smartspec_data_migration_generator \
  <spec_md> \
  [--from-schema <path>] \
  [--to-schema <path>] \
  [--apply]
```

### การใช้งาน Kilo Code

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code จำเป็นต้องระบุ flag `--platform kilo` ด้วยเสมอ

```bash
/smartspec_data_migration_generator.md \
  <spec_md> \
  [--from-schema <path>] \
  [--to-schema <path>] \
  [--apply] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: สร้างสคริปต์การย้ายข้อมูล (CLI)

**สถานการณ์:** สร้างการย้ายข้อมูลสำหรับการเปลี่ยนแปลง schema

**คำสั่ง:**

```bash
/smartspec_data_migration_generator specs/data/user_profile/spec.md \
  --from-schema db/schema_v1.sql \
  --to-schema db/schema_v2.sql
```

**ผลลัพธ์ที่คาดหวัง:**

1. สคริปต์การย้ายข้อมูลถูกสร้างขึ้น
2. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: การย้ายข้อมูลอัตโนมัติ (Kilo Code)

**สถานการณ์:** ระบบ CI สร้างสคริปต์การย้ายข้อมูล

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_data_migration_generator.md \
  specs/data/orders/spec.md \
  --apply \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. สคริปต์การย้ายข้อมูลถูกสร้างและบันทึก
2. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: ดูตัวอย่างการย้ายข้อมูล (CLI)

**สถานการณ์:** ดูตัวอย่างการย้ายข้อมูลโดยไม่ต้องนำไปใช้จริง

**คำสั่ง:**

```bash
/smartspec_data_migration_generator specs/data/products/spec.md \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. ตัวอย่างการย้ายข้อมูลแสดงผลในรูปแบบ JSON
2. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `<spec_md>` | `<path>` | เส้นทางไปยังไฟล์ spec.md | ต้องอยู่ภายใต้โฟลเดอร์ `specs/**` |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--platform` | แพลตฟอร์มที่ใช้รันคำสั่ง **จำเป็นสำหรับ Kilo Code** | (ตรวจจับอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--apply` | บันทึกสคริปต์การย้ายข้อมูล | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | แสดงผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--from-schema` | เส้นทางไปยังไฟล์ schema ต้นทาง | (ตรวจจับอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--to-schema` | เส้นทางไปยังไฟล์ schema ปลายทาง | (ตรวจจับอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/data-migration-generator/<run-id>/migration.sql` | สคริปต์การย้ายข้อมูลที่ถูกสร้างขึ้น |

---

## 6. หมายเหตุ

- **Flag แพลตฟอร์ม:** เมื่อใช้ Kilo Code ต้องระบุ `--platform kilo` เสมอ

---

**จบคู่มือ**