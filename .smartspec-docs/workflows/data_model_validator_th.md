| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_data_model_validator คู่มือ (TH) | 6.0 | /smartspec_data_model_validator | 6.0.x |

# /smartspec_data_model_validator คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

workflow `/smartspec_data_model_validator` ใช้สำหรับตรวจสอบความถูกต้องของ data models เทียบกับ specifications และ database schemas

**วัตถุประสงค์:** ตรวจสอบความถูกต้องของ data models เทียบกับ specifications เพื่อให้แน่ใจว่า database schemas, entities และความสัมพันธ์ตรงตามโครงสร้างที่กำหนดไว้

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** quality-testing

---

## 2. การใช้งาน

### 🔗 การใช้งาน CLI

```bash
/smartspec_data_model_validator \
  <spec_md> \
  [--schema-file <path>] \
  [--strict]
```

### การใช้งาน Kilo Code

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องใส่ flag `--platform kilo` เสมอ

```bash
/smartspec_data_model_validator.md \
  <spec_md> \
  [--schema-file <path>] \
  [--strict] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: การตรวจสอบ Data Model (CLI)

**สถานการณ์:** นักพัฒนาต้องการตรวจสอบ database schema เทียบกับ spec

**คำสั่ง:**

```bash
/smartspec_data_model_validator specs/data/user_profile/spec.md \
  --schema-file db/schema.sql
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow จะตรวจสอบ data model เทียบกับ spec  
2. รายงานการตรวจสอบจะถูกสร้างขึ้น  
3. exit code `0` หากถูกต้อง, `1` หากพบการละเมิด

### กรณีการใช้งาน 2: การตรวจสอบแบบเข้มงวด (Kilo Code)

**สถานการณ์:** pipeline CI ต้องการตรวจสอบ schema อย่างเข้มงวด

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_data_model_validator.md \
  specs/data/orders/spec.md \
  --strict \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. ดำเนินการตรวจสอบแบบเข้มงวด  
2. exit code `0` หากสมบูรณ์แบบ, `1` หากพบปัญหา

### กรณีการใช้งาน 3: ผลลัพธ์แบบ JSON (CLI)

**สถานการณ์:** การผนวกรวมต้องการผลลัพธ์การตรวจสอบในรูปแบบ JSON

**คำสั่ง:**

```bash
/smartspec_data_model_validator specs/data/products/spec.md \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. ดำเนินการตรวจสอบพร้อมผลลัพธ์ในรูปแบบ JSON  
2. exit code `0` (สำเร็จ)

---

## 4. พารามิเตอร์

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `<spec_md>` | `<path>` | เส้นทางไปยังไฟล์ spec.md | ต้องอยู่ภายใต้ `specs/**` |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--config` | เส้นทางไปยังไฟล์ configuration | `.spec/smartspec.config.yaml` | `cli` \| `kilo` \| `ci` \| `other` |
| `--platform` | แพลตฟอร์มที่ใช้รัน **จำเป็นสำหรับ Kilo Code** | (อนุมานค่า) | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | แสดงผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--schema-file` | เส้นทางไปยังไฟล์ database schema | (ตรวจจับอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--strict` | เปิดใช้งานโหมดตรวจสอบแบบเข้มงวด | `false` | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/data-model-validator/<run-id>/validation.md` | รายงานการตรวจสอบ |

---

## 6. หมายเหตุ

- **Platform Flag:** เมื่อใช้ Kilo Code ต้องใส่ `--platform kilo` เสมอ

---

**จบคู่มือ**