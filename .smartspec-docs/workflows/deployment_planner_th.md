| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_deployment_planner คู่มือ (TH) | 6.0 | /smartspec_deployment_planner | 6.0.x |

# /smartspec_deployment_planner คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

workflow `/smartspec_deployment_planner` สร้างแผนการปรับใช้จากสเปคและงานการดำเนินการ

**วัตถุประสงค์:** สร้างแผนการปรับใช้จากสเปค กำหนดกลยุทธ์การปรับใช้ ระยะเวลาการปล่อย และขั้นตอนการย้อนกลับ

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** operations-deployment

---

## 2. การใช้งาน

### 🔗 การใช้งาน CLI

```bash
/smartspec_deployment_planner \
  <spec_md> \
  [--strategy <blue-green|canary|rolling>] \
  [--apply]
```

### การใช้งาน Kilo Code

**หมายเหตุ:** เมื่อใช้ Kilo Code จำเป็นต้องใส่ flag `--platform kilo`

```bash
/smartspec_deployment_planner.md \
  <spec_md> \
  [--strategy <blue-green|canary|rolling>] \
  [--apply] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: สร้างแผนการปรับใช้ (CLI)

**สถานการณ์:** สร้างแผนการปรับใช้สำหรับ API ใหม่

**คำสั่ง:**

```bash
/smartspec_deployment_planner specs/api/v2_endpoints/spec.md \
  --strategy blue-green
```

**ผลลัพธ์ที่คาดหวัง:**

1. แผนการปรับใช้ถูกสร้างขึ้น
2. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: การปรับใช้แบบ Canary (Kilo Code)

**สถานการณ์:** วางแผนกลยุทธ์การปรับใช้แบบ canary

**คำสั่ง (Kilo Code Snippet):**

```bash
/smartspec_deployment_planner.md \
  specs/services/recommendation/spec.md \
  --strategy canary \
  --apply \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. แผนการปรับใช้แบบ canary ถูกสร้างและบันทึก
2. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: การปรับใช้แบบ Rolling (CLI)

**สถานการณ์:** วางแผนการปรับใช้แบบ rolling

**คำสั่ง:**

```bash
/smartspec_deployment_planner specs/frontend/dashboard/spec.md \
  --strategy rolling \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. แผนการปรับใช้แบบ rolling พร้อมผลลัพธ์ในรูปแบบ JSON
2. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `<spec_md>` | `<path>` | เส้นทางไปยังไฟล์ spec.md | ต้องสามารถระบุได้ภายใต้ `specs/**` |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--platform` | แพลตฟอร์มที่ใช้รัน **จำเป็นสำหรับ Kilo Code** | (อนุมาน) | `cli` \| `kilo` \| `ci` \| `other` |
| `--apply` | บันทึกแผนการปรับใช้ | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | แสดงผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--strategy` | กลยุทธ์การปรับใช้: `blue-green`, `canary`, `rolling` | `rolling` | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/deployment-planner/<run-id>/deployment_plan.md` | แผนการปรับใช้ |
| `specs/**/deployment/plan.md` | แผนการปรับใช้ที่บันทึกไว้ (เมื่อใช้ `--apply`) |

---

## 6. หมายเหตุ

- **Platform Flag:** เมื่อใช้ Kilo Code ต้องใส่ `--platform kilo` เสมอ

---

**จบคู่มือ**