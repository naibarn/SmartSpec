| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_design_system_migration_assistant คู่มือ (TH) | 6.0 | /smartspec_design_system_migration_assistant | 6.0.x |

# /smartspec_design_system_migration_assistant คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

workflow `/smartspec_design_system_migration_assistant` ช่วยในการย้ายส่วนประกอบ UI ไปยังระบบออกแบบใหม่

**วัตถุประสงค์:** ช่วยในการย้ายส่วนประกอบ UI ไปยังระบบออกแบบใหม่ โดยให้คำแนะนำการย้ายและการจับคู่ส่วนประกอบ

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** ui-design

---

## 2. การใช้งาน

### 🔗 การใช้งาน CLI

```bash
/smartspec_design_system_migration_assistant \
  <spec_md> \
  [--from-system <name>] \
  [--to-system <name>] \
  [--apply]
```

### การใช้งาน Kilo Code

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code จำเป็นต้องใส่ flag `--platform kilo` เสมอ

```bash
/smartspec_design_system_migration_assistant.md \
  <spec_md> \
  [--from-system <name>] \
  [--to-system <name>] \
  [--apply] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: ย้ายระบบออกแบบ (CLI)

**สถานการณ์:** ย้ายจาก Material UI ไปยัง Tailwind

**คำสั่ง:**

```bash
/smartspec_design_system_migration_assistant specs/ui/dashboard/spec.md \
  --from-system material-ui \
  --to-system tailwind
```

**ผลลัพธ์ที่คาดหวัง:**

1. สร้างคำแนะนำการย้ายระบบ
2. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: การย้ายระบบอัตโนมัติ (Kilo Code)

**สถานการณ์:** ระบบ CI สร้างแผนการย้ายระบบ

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_design_system_migration_assistant.md \
  specs/ui/forms/spec.md \
  --from-system bootstrap \
  --to-system chakra-ui \
  --apply \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. สร้างและบันทึกแผนการย้ายระบบ
2. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: ดูตัวอย่างการย้ายระบบ (CLI)

**สถานการณ์:** ดูตัวอย่างการย้ายระบบโดยไม่บันทึก

**คำสั่ง:**

```bash
/smartspec_design_system_migration_assistant specs/ui/navigation/spec.md \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. แสดงตัวอย่างการย้ายระบบในรูปแบบ JSON
2. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `<spec_md>` | `<path>` | เส้นทางไปยังไฟล์ spec.md | ต้องสามารถระบุเส้นทางภายใต้ `specs/**` ได้ |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--platform` | แพลตฟอร์มที่ใช้รัน **จำเป็นสำหรับ Kilo Code** | (อนุมานค่า) | `cli` \| `kilo` \| `ci` \| `other` |
| `--apply` | บันทึกคำแนะนำการย้ายระบบ | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | แสดงผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--from-system` | ชื่อระบบออกแบบต้นทาง | (ตรวจจับอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--to-system` | ชื่อระบบออกแบบปลายทาง | (จำเป็นต้องระบุ) | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/design-system-migration/<run-id>/migration_guide.md` | คำแนะนำการย้ายระบบ |

---

## 6. หมายเหตุ

- **Flag แพลตฟอร์ม:** เมื่อใช้ Kilo Code ต้องใส่ flag `--platform kilo` เสมอ

---

**จบคู่มือ**