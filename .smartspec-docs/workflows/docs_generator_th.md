| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_docs_generator คู่มือ (TH) | 6.0 | /smartspec_docs_generator | 6.0.x |

# /smartspec_docs_generator คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

คู่มือนี้อธิบายวิธีการใช้งาน workflow:

`/smartspec_docs_generator` workflow ใช้สำหรับสร้างเอกสารครบถ้วนจากสเปค แผนงาน และงานภายในกรอบงาน SmartSpec

**วัตถุประสงค์:** สร้างเอกสารครบถ้วนจากสเปค แผนงาน และงาน โดยผลิตคู่มือผู้ใช้ เอกสาร API และเอกสารอ้างอิงทางเทคนิค

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** documentation

---

## 2. การใช้งาน

workflow นี้รองรับการเรียกใช้งานผ่าน Command Line Interface (CLI) และ Kilo Code (สภาพแวดล้อมสคริปต์ภายใน)

### 🔗 การใช้งาน CLI

```bash
/smartspec_docs_generator \
  <spec_or_plan> \
  [--doc-type <user-guide|api-docs|technical-ref>] \
  [--apply]
```

### การใช้งาน Kilo Code

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องใส่ flag `--platform kilo` เสมอ

```bash
/smartspec_docs_generator.md \
  <spec_or_plan> \
  [--doc-type <user-guide|api-docs|technical-ref>] \
  [--apply] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: การสร้างคู่มือผู้ใช้จากสเปค (CLI)

**สถานการณ์:** นักเขียนเทคนิคต้องการสร้างคู่มือผู้ใช้จากสเปค

**คำสั่ง:**

```bash
/smartspec_docs_generator specs/auth/login_service/spec.md \
  --doc-type user-guide \
  --apply
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดไฟล์สเปค
2. สร้างคู่มือที่เข้าใจง่ายสำหรับผู้ใช้
3. เขียนเอกสารลงในโฟลเดอร์ docs
4. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: การสร้างเอกสาร API (Kilo Code)

**สถานการณ์:** ระบบ CI pipeline ต้องการสร้างเอกสาร API อัตโนมัติ

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_docs_generator.md \
  specs/api/rest_endpoints/spec.md \
  --doc-type api-docs \
  --apply \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow สร้างเอกสาร API
2. เอกสารประกอบด้วย endpoints, พารามิเตอร์ และตัวอย่าง
3. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: การสร้างเอกสารอ้างอิงทางเทคนิค (CLI)

**สถานการณ์:** นักพัฒนาต้องการเอกสารอ้างอิงทางเทคนิคสำหรับระบบที่ซับซ้อน

**คำสั่ง:**

```bash
/smartspec_docs_generator specs/system/architecture/spec.md \
  --doc-type technical-ref \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow สร้างเอกสารอ้างอิงทางเทคนิค
2. ผลลัพธ์เป็น metadata แบบ JSON ที่มีโครงสร้าง
3. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `<spec_or_plan>` | `<path>` | เส้นทางไปยังไฟล์ spec.md หรือ plan.md | ต้องอยู่ภายใต้โฟลเดอร์ `specs/**` |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--config` | เส้นทางไปยังไฟล์คอนฟิกของ SmartSpec | `.spec/smartspec.config.yaml` | `cli` \| `kilo` \| `ci` \| `other` |
| `--lang` | ภาษาสำหรับการสร้างรายงาน | (ค่าเริ่มต้นของระบบ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--platform` | บริบทของแพลตฟอร์มที่รัน **จำเป็นสำหรับ Kilo Code** | (สืบค้นอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--apply` | เปิดใช้งานการเขียนข้อมูลแบบควบคุม | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--out` | เส้นทางฐานสำหรับผลลัพธ์ที่ปลอดภัย | `.spec/reports/docs-generator/` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--quiet` | ปิดการแสดงผลล็อกมาตรฐาน | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะของ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--doc-type` | ประเภทเอกสาร: `user-guide`, `api-docs`, หรือ `technical-ref` | `user-guide` | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/docs-generator/<run-id>/...` | ผลลัพธ์ที่ปลอดภัย (เอกสารที่สร้างขึ้น) |
| `docs/**/*.md` | ไฟล์เอกสารที่สร้างขึ้น (เฉพาะเมื่อใช้ `--apply`) |

---

## 6. หมายเหตุ

- **Flag แพลตฟอร์ม:** เมื่อใช้ Kilo Code ต้องใส่ `--platform kilo` เสมอ  
- **ประเภทเอกสาร:** เลือกประเภท doc-type ให้เหมาะสมกับกลุ่มเป้าหมาย  
- **การตั้งค่า:** workflow จะอ่านค่าการตั้งค่าจาก `.spec/smartspec.config.yaml`  

---

**จบคู่มือ**