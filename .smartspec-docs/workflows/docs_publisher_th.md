| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_docs_publisher คู่มือ (TH) | 6.0 | /smartspec_docs_publisher | 6.0.x |

# /smartspec_docs_publisher คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

คู่มือนี้อธิบายวิธีการใช้งาน workflow:

workflow `/smartspec_docs_publisher` ใช้สำหรับเผยแพร่เอกสารที่ถูกสร้างขึ้นไปยังเป้าหมายที่ตั้งค่าไว้ (เว็บไซต์แบบสแตติก, วิกิ หรือแพลตฟอร์มเอกสาร)

**วัตถุประสงค์:** เผยแพร่เอกสารที่ถูกสร้างขึ้นไปยังเป้าหมายที่ตั้งค่าไว้ รวมถึงเว็บไซต์แบบสแตติก, วิกิ และแพลตฟอร์มเอกสาร

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** documentation

---

## 2. การใช้งาน

workflow นี้รองรับการเรียกใช้งานผ่าน Command Line Interface (CLI) และ Kilo Code (สภาพแวดล้อมสคริปต์ภายใน)

### 🔗 การใช้งาน CLI

```bash
/smartspec_docs_publisher \
  <docs_path> \
  [--target <static|wiki|platform>] \
  [--apply]
```

### การใช้งาน Kilo Code

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code จำเป็นต้องใส่ flag `--platform kilo` ด้วยเสมอ

```bash
/smartspec_docs_publisher.md \
  <docs_path> \
  [--target <static|wiki|platform>] \
  [--apply] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: การเผยแพร่ไปยังเว็บไซต์แบบสแตติก (CLI)

**สถานการณ์:** นักพัฒนาต้องการเผยแพร่เอกสารไปยังตัวสร้างเว็บไซต์แบบสแตติก

**คำสั่ง:**

```bash
/smartspec_docs_publisher docs/ \
  --target static \
  --apply
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดไฟล์เอกสาร
2. เผยแพร่ไปยังเป้าหมายเว็บไซต์แบบสแตติกที่ตั้งค่าไว้
3. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: การเผยแพร่ไปยังวิกิ (Kilo Code)

**สถานการณ์:** ระบบ CI pipeline ต้องการเผยแพร่เอกสารไปยังวิกิโดยอัตโนมัติ

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_docs_publisher.md \
  docs/ \
  --target wiki \
  --apply \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. เอกสารถูกเผยแพร่ไปยังวิกิ
2. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: การพรีวิวการเผยแพร่ (CLI)

**สถานการณ์:** ผู้ใช้ต้องการพรีวิวเนื้อหาที่จะถูกเผยแพร่โดยไม่ต้องการเผยแพร่จริง

**คำสั่ง:**

```bash
/smartspec_docs_publisher docs/ \
  --target platform \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. รายงานพรีวิวถูกสร้างขึ้น
2. ไม่มีการเผยแพร่จริงเกิดขึ้น
3. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `<docs_path>` | `<path>` | เส้นทางไปยังไดเรกทอรีเอกสาร | ต้องมีอยู่จริงและเข้าถึงได้ |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--config` | เส้นทางไปยังไฟล์การตั้งค่า SmartSpec | `.spec/smartspec.config.yaml` | `cli` \| `kilo` \| `ci` \| `other` |
| `--lang` | ภาษาในการสร้างรายงาน | (ค่าเริ่มต้นของระบบ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--platform` | บริบทแพลตฟอร์มที่ใช้งาน **จำเป็นสำหรับ Kilo Code** | (อนุมานค่า) | `cli` \| `kilo` \| `ci` \| `other` |
| `--apply` | เปิดใช้งานการเผยแพร่จริง | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--out` | เส้นทางฐานสำหรับผลลัพธ์ที่ปลอดภัย | `.spec/reports/docs-publisher/` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--quiet` | ปิดการแสดงผลล็อกมาตรฐาน | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--target` | เป้าหมายการเผยแพร่: `static`, `wiki` หรือ `platform` | `static` | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/docs-publisher/<run-id>/...` | รายงานและล็อกการเผยแพร่ |

---

## 6. หมายเหตุ

- **Platform Flag:** เมื่อใช้ Kilo Code ต้องใส่ `--platform kilo` เสมอ  
- **โหมดพรีวิว:** หากไม่ใส่ `--apply` จะเป็นการสร้างพรีวิวโดยไม่เผยแพร่จริง  
- **การตั้งค่า:** เป้าหมายการเผยแพร่ต้องถูกตั้งค่าใน `.spec/smartspec.config.yaml`

---

**จบคู่มือ**