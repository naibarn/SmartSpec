| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_generate_plan คู่มือ (TH) | 6.0 | /smartspec_generate_plan | 6.0.x |

# /smartspec_generate_plan คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

คู่มือนี้อธิบายวิธีการใช้งาน workflow:

`/smartspec_generate_plan` workflow จะสร้างหรือปรับปรุง `plan.md` จาก `spec.md` ในลักษณะที่ **ตระหนักถึงการพึ่งพา**, **เน้นการนำกลับมาใช้ซ้ำก่อน**, และ **ปลอดภัยโดยค่าเริ่มต้น**

**วัตถุประสงค์:** แปลง spec.md → plan.md (แสดงตัวอย่างก่อน; ตระหนักถึงการพึ่งพา; เน้นการนำกลับมาใช้ซ้ำก่อน; การใช้งานแบบมีการควบคุม) Workflow นี้อยู่ในสายงานหลักระหว่างการสร้าง spec และการสร้าง task เพื่อให้แน่ใจว่าแผนการดำเนินงานถูกจัดโครงสร้างและตรวจสอบอย่างถูกต้องก่อนการดำเนินการจริง

**เวอร์ชัน:** 6.0.5  
**หมวดหมู่:** core

---

## 2. การใช้งาน

Workflow นี้รองรับการเรียกใช้งานผ่าน Command Line Interface (CLI) และ Kilo Code (สภาพแวดล้อมสคริปต์ภายใน)

### 🔗 การใช้งาน CLI

การเรียกใช้งาน CLI จำเป็นต้องระบุไฟล์ spec.md เป้าหมายเป็น positional argument

```bash
/smartspec_generate_plan <spec_md> \
  [--apply] \
  [--ui-mode auto|json|inline] \
  [--safety-mode strict|dev] \
  [--plan-layout per-spec|consolidated] \
  [--run-label "..."] \
  [--json]
```

### การใช้งาน Kilo Code

การเรียกใช้งาน Kilo Code มีโครงสร้างเหมือนกับ CLI โดยปกติใช้ใน pipeline อัตโนมัติหรือสคริปต์ภายใน

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องใส่ flag `--platform kilo` ด้วยเสมอ

```bash
/smartspec_generate_plan.md \
  <spec_md> \
  [--apply] \
  [--ui-mode auto|json|inline] \
  [--safety-mode strict|dev] \
  [--plan-layout per-spec|consolidated] \
  [--run-label "..."] \
  [--json] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: สร้างแผนจากสเปคใหม่ (CLI)

**สถานการณ์:** ผู้จัดการผลิตภัณฑ์เพิ่งสร้างสเปคใหม่สำหรับ "Notification Service" (`specs/services/notification_service/spec.md`) และต้องการสร้างแผนการดำเนินงาน แผนควรถูกตรวจสอบก่อนนำไปใช้จริง

**คำสั่ง:**

```bash
/smartspec_generate_plan specs/services/notification_service/spec.md
```

**ผลลัพธ์ที่คาดหวัง:**

1. Workflow โหลดไฟล์ `specs/services/notification_service/spec.md`
2. วิเคราะห์เนื้อหาสเปคและระบุเฟสการดำเนินงาน
3. เขียน preview bundle ไปยัง `.spec/reports/generate-plan/<run-id>/`
4. ตัวอย่างแผนประกอบด้วย `plan_preview.md` ที่จัดลำดับเฟสตามการพึ่งพา
5. ไม่มีการเขียนแบบมีการควบคุม (ไม่มี flag `--apply`)
6. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: นำแผนไปใช้พร้อมตั้งค่าโหมด UI (Kilo Code)

**สถานการณ์:** pipeline CI ต้องการสร้างและนำแผนไปใช้สำหรับสเปคที่มีส่วนประกอบ UI แบบ JSON แผนควรบังคับใช้โหมด UI แบบ JSON และนำไปใช้ทันทีหลังการตรวจสอบ

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_generate_plan.md \
  specs/ui/dashboard/spec.md \
  --apply \
  --ui-mode json \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. Workflow โหลดไฟล์ `specs/ui/dashboard/spec.md`
2. สร้างแผนที่มีเฟส UI ตั้งค่าเป็นโหมด JSON
3. เขียน preview bundle ไปยัง `.spec/reports/generate-plan/<run-id>/`
4. ด้วย `--apply` แผนจะถูกเขียนลงใน `specs/ui/dashboard/plan.md`
5. แผนมีการระบุการควบคุม UI อย่างเหมาะสม
6. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: ตรวจจับโอกาสการนำกลับมาใช้ซ้ำ (CLI)

**สถานการณ์:** นักพัฒนากำลังสร้างแผนสำหรับฟีเจอร์ "Payment Processing" ใหม่ Workflow ควรตรวจจับว่ามีคอมโพเนนต์ "Payment Gateway" ที่คล้ายกันอยู่ในดัชนีสเปคและแนะนำให้ใช้ซ้ำ

**คำสั่ง:**

```bash
/smartspec_generate_plan specs/payments/payment_processing/spec.md \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. Workflow โหลด `.spec/SPEC_INDEX.json` และสแกนหาคอมโพเนนต์ที่คล้ายกัน
2. ตรวจพบคอมโพเนนต์ "Payment Gateway" ที่มีอยู่แล้ว
3. ตัวอย่างแผนมีส่วน **คำแนะนำการนำกลับมาใช้ซ้ำ**
4. ผลลัพธ์ `summary.json` มีคำแนะนำการใช้ซ้ำพร้อมรหัสคอมโพเนนต์
5. ไม่มีการเขียนแบบมีการควบคุม (ไม่มี flag `--apply`)
6. รหัสออก `0` (สำเร็จพร้อมคำแนะนำ)

### กรณีการใช้งาน 4: แผนแบบรวมสำหรับโปรเจกต์หลายสเปค (Kilo Code)

**สถานการณ์:** ทีมงานกำลังทำงานในโปรเจกต์ขนาดใหญ่ที่มีหลายสเปคที่เกี่ยวข้องกัน ต้องการแผนแบบรวมที่รวมทุกสเปคเป็นแผนการดำเนินงานเดียว

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_generate_plan.md \
  specs/project/main_spec.md \
  --apply \
  --plan-layout consolidated \
  --run-label "Q1-2024-Release" \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. Workflow โหลดไฟล์ `specs/project/main_spec.md` และสเปคที่เกี่ยวข้อง
2. สร้างแผนแบบรวมที่รวมทุกเฟสและจัดลำดับตามการพึ่งพา
3. แผนมีป้าย run label "Q1-2024-Release" สำหรับติดตาม
4. เขียน preview bundle ไปยัง `.spec/reports/generate-plan/<run-id>/`
5. ด้วย `--apply` แผนรวมจะถูกเขียนลงใน `specs/project/plan.md`
6. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 5: ปฏิเสธการนำไปใช้เนื่องจากการรั่วไหลของความลับ (CLI)

**สถานการณ์:** ผู้ใช้พยายามสร้างแผน แต่เนื้อหาสเปคมีข้อมูลการเชื่อมต่อฐานข้อมูลโดยไม่ได้ตั้งใจ Workflow ควรตรวจจับและปฏิเสธการนำแผนไปใช้

**คำสั่ง:**

```bash
/smartspec_generate_plan specs/backend/api_service/spec.md \
  --apply
```

**ผลลัพธ์ที่คาดหวัง:**

1. Workflow โหลดไฟล์ `specs/backend/api_service/spec.md`
2. สร้างตัวอย่างแผน
3. เนื้อหาตรงกับรูปแบบการปิดบังข้อมูลที่ตั้งค่าไว้ (เช่น ข้อมูลรับรองฐานข้อมูล)
4. Workflow ปิดบังค่าดังกล่าวในตัวอย่าง/รายงาน
5. Workflow **ปฏิเสธ** การดำเนินการ `--apply`
6. สร้างไฟล์ `report.md` รายละเอียดการตรวจจับความลับ
7. รหัสออก `1` (ตรวจสอบล้มเหลว)

---

## 4. พารามิเตอร์

พารามิเตอร์และ flags ต่อไปนี้ควบคุมการทำงานและพฤติกรรมของ `/smartspec_generate_plan` workflow

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `<spec_md>` | `<path>` | เส้นทางไปยังไฟล์ spec.md ที่จะสร้างแผนจาก | ต้องอยู่ภายใต้ `specs/**` และไม่อนุญาตให้หลุดออกนอกผ่าน symlink |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--config` | เส้นทางไปยังไฟล์กำหนดค่า SmartSpec | `.spec/smartspec.config.yaml` | `cli` \| `kilo` \| `ci` \| `other` |
| `--lang` | ภาษาในการสร้างรายงาน (เช่น `th`, `en`) | (ค่าเริ่มต้นของระบบ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--platform` | บริบทแพลตฟอร์มการทำงาน **จำเป็นสำหรับ Kilo Code** | (อนุมานโดยระบบ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--apply` | เปิดใช้งานการเขียนแบบมีการควบคุม (แก้ไข `specs/**/plan.md`) | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--out` | เส้นทางฐานสำหรับผลลัพธ์ที่ปลอดภัย (รายงาน/ตัวอย่าง) | `.spec/reports/generate-plan/` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ส่งออกสรุปหลักในรูปแบบ JSON (`summary.json`) | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--quiet` | ปิดการแสดงผลล็อกมาตรฐาน | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--ui-mode` | โหมดการควบคุม UI (`auto`, `json`, `inline`) | `auto` | `cli` \| `kilo` \| `ci` \| `other` |
| `--safety-mode` | ระดับการตรวจสอบความปลอดภัย (`strict`, `dev`) | `strict` | `cli` \| `kilo` \| `ci` \| `other` |
| `--plan-layout` | โครงสร้างแผน (`per-spec`, `consolidated`) | `per-spec` | `cli` \| `kilo` \| `ci` \| `other` |
| `--run-label` | ป้ายกำกับกำหนดเองสำหรับการรันสร้างแผนนี้ | (สร้างอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

Workflow สร้างผลลัพธ์สองประเภท: Safe Preview Bundles (สร้างเสมอ) และ Governed Artifacts (สร้างเฉพาะเมื่อใช้ `--apply`)

### Safe Preview Bundle (สร้างเสมอ)

จะมีการสร้างโฟลเดอร์รันที่ไม่ซ้ำกันภายใต้เส้นทางรายงาน (ค่าเริ่มต้น: `.spec/reports/generate-plan/<run-id>/`)

**เนื้อหา:**

| File Path | Description |
| :--- | :--- |
| `plan_preview.md` | เนื้อหาแผนที่สร้างขึ้น (ก่อนนำไปใช้) |
| `dependency_graph.md` | แผนภาพแสดงความสัมพันธ์การพึ่งพาของเฟสต่างๆ |
| `summary.json` | (ถ้าใช้ `--json`) เมตาดาต้าแผนและผลการตรวจสอบในรูปแบบโครงสร้าง |
| `report.md` | การวิเคราะห์โดยละเอียดรวมคำแนะนำการนำกลับมาใช้ซ้ำและผลการสแกนความปลอดภัย |
| `patch.diff` | (ถ้าเป็นการปรับปรุงแผนเดิม) ความแตกต่างแสดงการเปลี่ยนแปลงจากเวอร์ชันปัจจุบัน |

### Governed Artifacts (สร้างเฉพาะเมื่อใช้ `--apply`)

| File Path | Description |
| :--- | :--- |
| `specs/**/plan.md` | ไฟล์แผนที่สร้างหรือปรับปรุงแล้วถูกเขียนลงในตำแหน่งที่มีการควบคุม |

---

## 6. หมายเหตุ

- **แสดงตัวอย่างก่อน:** ควรตรวจสอบ preview bundle เสมอก่อนใช้ `--apply` เพื่อให้แน่ใจว่าแผนเป็นไปตามที่คาดหวัง
- **ตระหนักถึงการพึ่งพา:** Workflow จะจัดลำดับเฟสตามการพึ่งพาที่ระบุในสเปคโดยอัตโนมัติ
- **เน้นการนำกลับมาใช้ซ้ำก่อน:** Workflow จะตรวจจับโอกาสในการใช้ซ้ำคอมโพเนนต์ที่มีอยู่ในดัชนีสเปค
- **สอดคล้องกับโหมด UI:** การจัดลำดับแผนสอดคล้องกับโหมดการควบคุม UI (`auto`, `json`, `inline`)
- **ปกป้องความลับ:** เนื้อหาที่ตรงกับรูปแบบการปิดบังข้อมูลที่ตั้งค่าไว้จะบล็อกการดำเนินการ `--apply`
- **นโยบายเครือข่าย:** Workflow นี้เคารพ `safety.network_policy.default=deny` และไม่ทำการร้องขอเครือข่ายภายนอก
- **ความปลอดภัยของ symlink:** หากตั้งค่า `safety.disallow_symlink_writes=true` Workflow จะปฏิเสธการเขียนผ่าน symlink
- **แพลตฟอร์ม Kilo Code:** เมื่อใช้ Kilo Code ต้องใส่ `--platform kilo` เสมอเพื่อให้บริบทและการบันทึกถูกต้อง
- **สายงานหลัก:** Workflow นี้เป็นส่วนหนึ่งของสายงานหลัก SmartSpec: validate_index → generate_spec → **generate_plan** → generate_tasks → verify_tasks_progress_strict → sync_tasks_checkboxes → report_implement_prompter

---

**จบคู่มือ**