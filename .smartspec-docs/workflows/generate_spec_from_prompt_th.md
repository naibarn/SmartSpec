| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_generate_spec_from_prompt คู่มือ (TH) | 6.0 | /smartspec_generate_spec_from_prompt | 6.0.x |

# /smartspec_generate_spec_from_prompt คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

คู่มือนี้อธิบายวิธีการใช้งาน workflow:

`/smartspec_generate_spec_from_prompt` workflow สร้าง **spec เริ่มต้นหนึ่งรายการหรือมากกว่า** จากคำอธิบายความต้องการในรูปแบบภาษาธรรมชาติ **โดยใช้ความชาญฉลาดแบบ reuse-first**

**วัตถุประสงค์:** สร้าง spec เริ่มต้นหนึ่งรายการหรือมากกว่า จากคำอธิบายความต้องการในรูปแบบภาษาธรรมชาติ โดยใช้ความชาญฉลาดแบบ reuse-first ซึ่ง workflow นี้จะตรวจจับความซ้ำซ้อนกับดัชนี spec ที่มีอยู่, ให้ความสำคัญกับการนำกลับมาใช้ซ้ำแทนการทำซ้ำ และสร้าง spec ที่พร้อมสำหรับ UX/UI เหมาะสมกับการวางแผนผลิตภัณฑ์

**เวอร์ชัน:** 6.0.1  
**หมวดหมู่:** spec-gen

---

## 2. การใช้งาน

workflow นี้รองรับการเรียกใช้งานผ่าน Command Line Interface (CLI) และ Kilo Code (สภาพแวดล้อมสคริปต์ภายใน)

### 🔗 การใช้งาน CLI

การเรียกใช้งาน CLI จำเป็นต้องระบุคำอธิบายความต้องการในรูปแบบภาษาธรรมชาติที่ต้องการสร้าง specification

```bash
/smartspec_generate_spec_from_prompt \
  --prompt "<requirement_description>" \
  [--category <category>] \
  [--update-index] \
  [--json] \
  [--apply]
```

### การใช้งาน Kilo Code

การเรียกใช้งาน Kilo Code มีโครงสร้างเหมือนกับ CLI โดยทั่วไปใช้ใน pipeline อัตโนมัติหรือสคริปต์ภายใน

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องใส่ flag `--platform kilo` ด้วยเสมอ

```bash
/smartspec_generate_spec_from_prompt.md \
  --prompt "<requirement_description>" \
  [--category <category>] \
  [--update-index] \
  [--json] \
  [--apply] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: สร้าง Spec จากคำอธิบายง่าย ๆ (CLI)

**สถานการณ์:** ผู้จัดการผลิตภัณฑ์ต้องการสร้าง specification สำหรับฟีเจอร์ "รีเซ็ตรหัสผ่าน" ใหม่จากคำอธิบายสั้น ๆ

**คำสั่ง:**

```bash
/smartspec_generate_spec_from_prompt \
  --prompt "Users should be able to reset their password via email link with 24-hour expiration"
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow วิเคราะห์คำอธิบายและระบุความต้องการหลัก  
2. ตรวจสอบ `.spec/SPEC_INDEX.json` เพื่อหา spec ที่คล้ายกัน  
3. สร้าง preview bundle ลงใน `.spec/reports/spec-from-prompt/<run-id>/`  
4. preview ประกอบด้วย spec เริ่มต้นหนึ่งรายการหรือมากกว่า พร้อมส่วน UX/UI เบื้องต้น  
5. ไม่มีการเขียนข้อมูลจริง (ไม่มี flag `--apply`)  
6. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: สร้าง Spec พร้อมกำหนดหมวดหมู่และอัปเดตดัชนี (Kilo Code)

**สถานการณ์:** pipeline CI ต้องการสร้าง spec สำหรับฟีเจอร์ "การรวมระบบ Payment Gateway" ใหม่, จัดหมวดหมู่ภายใต้ "integrations" และอัปเดตดัชนี spec ทันที

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_generate_spec_from_prompt.md \
  --prompt "Integrate Stripe payment gateway with support for credit cards, Apple Pay, and Google Pay" \
  --category integrations \
  --update-index \
  --apply \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow สร้าง spec ตามคำอธิบาย  
2. spec ถูกจัดหมวดหมู่ใน `specs/integrations/`  
3. สร้าง preview bundle ลงใน `.spec/reports/spec-from-prompt/<run-id>/`  
4. ด้วย `--apply` จะสร้างโฟลเดอร์และไฟล์ spec จริง  
5. ด้วย `--update-index` จะอัปเดต `.spec/SPEC_INDEX.json` (ถ้าอนุญาต)  
6. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: ตรวจจับโอกาสการนำกลับมาใช้ซ้ำ (CLI)

**สถานการณ์:** นักพัฒนาต้องการสร้าง spec สำหรับ "การจัดการโปรไฟล์ผู้ใช้" แต่มี spec "User Profile Service" ที่คล้ายกันอยู่แล้ว workflow ควรตรวจจับและแนะนำให้ใช้ซ้ำ

**คำสั่ง:**

```bash
/smartspec_generate_spec_from_prompt \
  --prompt "Allow users to view and edit their profile information including name, email, and avatar" \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow วิเคราะห์คำอธิบายและสแกน `.spec/SPEC_INDEX.json`  
2. ตรวจพบ spec "User Profile Service" ที่มีวัตถุประสงค์คล้ายกัน  
3. preview รวมส่วน **คำแนะนำการนำกลับมาใช้ซ้ำ**  
4. ไฟล์ `summary.json` มีข้อเสนอแนะการใช้ซ้ำพร้อมรหัส spec  
5. ไม่มีการเขียนข้อมูลจริง (ไม่มี flag `--apply`)  
6. รหัสออก `0` (สำเร็จพร้อมคำแนะนำ)

### กรณีการใช้งาน 4: สร้าง Spec หลายรายการจากคำอธิบายซับซ้อน (CLI)

**สถานการณ์:** ผู้จัดการผลิตภัณฑ์ให้คำอธิบายซับซ้อนที่มีหลายฟีเจอร์ที่เกี่ยวข้อง workflow ควรสร้าง spec แยกสำหรับแต่ละฟีเจอร์

**คำสั่ง:**

```bash
/smartspec_generate_spec_from_prompt \
  --prompt "Build a notification system with email, SMS, and push notifications. Include user preferences for notification channels and delivery schedules." \
  --apply
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow วิเคราะห์คำอธิบายและระบุฟีเจอร์ที่แตกต่างกันหลายรายการ  
2. สร้าง spec แยก: "Notification Service", "Notification Preferences", "Notification Delivery"  
3. แต่ละ spec มีส่วน UX/UI เบื้องต้นและอ้างอิง spec ที่เกี่ยวข้อง  
4. สร้าง preview bundle ลงใน `.spec/reports/spec-from-prompt/<run-id>/`  
5. ด้วย `--apply` สร้างโฟลเดอร์และไฟล์ spec ทั้งหมด  
6. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 5: ปฏิเสธการใช้งานเนื่องจากการรั่วไหลของความลับ (CLI)

**สถานการณ์:** ผู้ใช้ให้คำอธิบายที่มีข้อมูลการเชื่อมต่อฐานข้อมูลโดยไม่ได้ตั้งใจ workflow ควรตรวจจับและปฏิเสธการสร้าง spec

**คำสั่ง:**

```bash
/smartspec_generate_spec_from_prompt \
  --prompt "Create API service connecting to postgres://user:pass@db.example.com/prod" \
  --apply
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow สร้าง preview spec  
2. เนื้อหาตรงกับรูปแบบการปกปิดข้อมูลที่ตั้งไว้ (เช่น ข้อมูลรับรองฐานข้อมูล)  
3. workflow ปกปิดค่าดังกล่าวใน preview/report  
4. workflow **ปฏิเสธ** การดำเนินการ `--apply`  
5. สร้างไฟล์ `report.md` รายงานรายละเอียดการตรวจจับความลับ  
6. รหัสออก `1` (ตรวจสอบไม่ผ่าน)

---

## 4. พารามิเตอร์

พารามิเตอร์และ flags ต่อไปนี้ควบคุมการทำงานและพฤติกรรมของ workflow `/smartspec_generate_spec_from_prompt`

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `--prompt` | `<string>` | คำอธิบายความต้องการในรูปแบบภาษาธรรมชาติของ specification ที่ต้องการ | ต้องไม่ว่างเปล่า |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--config` | เส้นทางไปยังไฟล์การตั้งค่า SmartSpec | `.spec/smartspec.config.yaml` | `cli` \| `kilo` \| `ci` \| `other` |
| `--lang` | ภาษาสำหรับการสร้างรายงาน (เช่น `th`, `en`) | (ค่าเริ่มต้นของระบบ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--platform` | บริบทแพลตฟอร์มการทำงาน **จำเป็นสำหรับ Kilo Code** | (อนุมานอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--apply` | เปิดใช้งานการเขียนข้อมูลที่ควบคุม (สร้างโฟลเดอร์และไฟล์ spec) | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--out` | เส้นทางฐานสำหรับผลลัพธ์ที่ปลอดภัย (รายงาน/preview) | `.spec/reports/spec-from-prompt/` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ส่งออกสรุปหลักในรูปแบบ JSON (`summary.json`) | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--quiet` | ปิดการแสดงผลบันทึกมาตรฐาน | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--category` | หมวดหมู่เป้าหมายสำหรับ spec ที่สร้าง | (อนุมานอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--update-index` | อัปเดต `.spec/SPEC_INDEX.json` ด้วยรายการ spec ใหม่ (ต้องใช้กับ `--apply`) | `false` | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

workflow สร้างผลลัพธ์สองประเภท: Safe Preview Bundles (สร้างเสมอ) และ Governed Artifacts (สร้างเฉพาะเมื่อใช้ `--apply`)

### Safe Preview Bundle (สร้างเสมอ)

จะสร้างโฟลเดอร์รันที่ไม่ซ้ำกันภายใต้เส้นทางรายงาน (ค่าเริ่มต้น: `.spec/reports/spec-from-prompt/<run-id>/`)

**เนื้อหา:**

| File Path | Description |
| :--- | :--- |
| `preview/<spec-id>/spec.md` | เนื้อหา spec ที่สร้าง (ก่อนการ apply) |
| `summary.json` | (ถ้าใช้ `--json`) เมตาดาต้า spec ที่มีโครงสร้างและคำแนะนำการใช้ซ้ำ |
| `report.md` | การวิเคราะห์โดยละเอียดรวมถึงการตรวจจับการใช้ซ้ำและผลการสแกนความปลอดภัย |
| `references.yaml` | การอ้างอิงที่มีโครงสร้างไปยัง spec ที่เกี่ยวข้องและแหล่งข้อมูลภายนอก |

### Governed Artifacts (สร้างเฉพาะเมื่อใช้ `--apply`)

| File Path | Description |
| :--- | :--- |
| `specs/<category>/<spec-id>/spec.md` | ไฟล์ spec ที่สร้างและเขียนลงในตำแหน่งควบคุม |
| `.spec/SPEC_INDEX.json` | ดัชนี spec ที่อัปเดต (เฉพาะเมื่อใช้ `--update-index` และได้รับอนุญาต) |

---

## 6. หมายเหตุ

- **ความชาญฉลาดแบบ reuse-first:** workflow จะตรวจจับความซ้ำซ้อนกับ spec ที่มีอยู่และแนะนำให้ใช้ซ้ำแทนการทำซ้ำ  
- **Spec พร้อม UX/UI:** spec ที่สร้างจะรวมทุกส่วนที่จำเป็นสำหรับการวางแผนผลิตภัณฑ์ (states, accessibility, responsive design, microcopy)  
- **หลาย spec:** คำอธิบายซับซ้อนอาจทำให้สร้าง spec หลายรายการที่เกี่ยวข้องกัน  
- **การจับอ้างอิง:** การอ้างอิงภายนอก (UX/UI/API) จะถูกจัดเก็บในรูปแบบที่มีโครงสร้างและตรวจสอบได้  
- **การปกป้องความลับ:** เนื้อหาที่ตรงกับรูปแบบการปกปิดข้อมูลที่ตั้งไว้จะบล็อกการดำเนินการ `--apply`  
- **นโยบายเครือข่าย:** workflow นี้เคารพ `safety.network_policy.default=deny` และไม่ทำคำขอเครือข่ายภายนอก  
- **การอัปเดตดัชนี:** ใช้ `--update-index` พร้อม `--apply` เพื่ออัปเดตดัชนี spec อัตโนมัติ (ต้องตั้งค่า allowlist)  
- **แพลตฟอร์ม Kilo Code:** เมื่อใช้ Kilo Code ต้องใส่ `--platform kilo` เสมอเพื่อบริบทและการบันทึกที่ถูกต้อง

---

**จบคู่มือ**