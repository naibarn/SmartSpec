| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| คู่มือ /smartspec_generate_spec (TH) | 6.0 | /smartspec_generate_spec | 6.0.x |

# คู่มือ /smartspec_generate_spec (v6.0, ภาษาไทย)

## 1. ภาพรวม

คู่มือนี้อธิบายวิธีการใช้งาน workflow:

workflow `/smartspec_generate_spec` เป็นจุดเริ่มต้นหลักสำหรับการสร้าง ปรับปรุง และตรวจสอบเอกสารข้อกำหนด (`spec.md`) ภายในระบบนิเวศ SmartSpec โดยปฏิบัติตามหลักการกำกับดูแลแบบ **SPEC-first** อย่างเคร่งครัด

**วัตถุประสงค์:** เพื่อให้แน่ใจว่าข้อกำหนดมีความสมบูรณ์ ปฏิบัติตามมาตรฐานที่กำหนด (UX/UI baseline, NFRs) และบังคับใช้พฤติกรรมแบบ reuse-first เพื่อป้องกันการทำซ้ำ ทำงานอย่างปลอดภัยโดยค่าเริ่มต้น โดยให้ตัวอย่างและ diffs ที่ตรวจสอบได้ก่อนที่จะมีการเขียน governed artifacts ใดๆ

**เวอร์ชัน:** 6.0.2  
**หมวดหมู่:** core

---

## 2. การใช้งาน

workflow นี้รองรับการเรียกใช้ผ่าน Command Line Interface (CLI) และ Kilo Code (สภาพแวดล้อมสคริปต์ภายใน)

### 🔗 การใช้งานผ่าน CLI

การเรียกใช้ผ่าน CLI ต้องระบุข้อกำหนดเป้าหมายด้วย file path หรือ ID(s)

```bash
/smartspec_generate_spec \
  (--spec <path/to/spec.md> | --spec-ids <id1,id2,...>) \
  [--json] \
  [--apply]
```

### การใช้งานผ่าน Kilo Code

การเรียกใช้ผ่าน Kilo Code มีโครงสร้างเหมือนกับ CLI โดยทั่วไปใช้ใน automated pipelines หรือสคริปต์ภายใน

**สำคัญ:** เมื่อใช้ Kilo Code คุณต้องใส่ flag `--platform kilo` เสมอ

```bash
/smartspec_generate_spec.md \
  (--spec <path/to/spec.md> | --spec-ids <id1,id2,...>) \
  [--json] \
  [--apply] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีที่ 1: ปรับปรุงข้อกำหนดที่มีอยู่แล้ว (CLI)

**สถานการณ์:** Product manager ต้องการอัปเดตข้อกำหนดที่มีอยู่สำหรับ "User Profile Service" (`specs/profile/profile_service.md`) และต้องการให้แน่ใจว่าตรงตามข้อกำหนด accessibility baseline ที่กำหนดไว้ใน configuration การเปลี่ยนแปลงควรถูก apply ทันทีหลังจากการตรวจสอบ

**คำสั่ง:**

```bash
/smartspec_generate_spec \
  --spec specs/profile/profile_service.md \
  --apply
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลด `specs/profile/profile_service.md`
2. วิเคราะห์เนื้อหาเทียบกับ Spec Completeness Contract (เช่น ตรวจสอบ accessibility notes, NFRs)
3. สร้าง safe preview bundle ใน `.spec/reports/generate-spec/<run-id>/...`
4. ถ้าไม่พบการละเมิดความปลอดภัย (secrets) หรือความล้มเหลวที่จำเป็น workflow จะดำเนินการ governed write (`--apply`)
5. `specs/profile/profile_service.md` ถูกอัปเดตในที่เดิมด้วยเนื้อหา/โครงสร้างที่ปรับปรุงแล้ว
6. `.spec/SPEC_INDEX.json` ถูกอัปเดต (ถ้าอยู่ใน allowlist) ด้วย metadata ล่าสุด
7. Exit code `0` (สำเร็จ)

### กรณีที่ 2: สร้าง Preview สำหรับข้อกำหนดหลายรายการ (Kilo Code)

**สถานการณ์:** CI pipeline ต้องการตรวจสอบข้อกำหนดที่สร้างใหม่สองรายการที่ระบุด้วย IDs `FEAT-1001` และ `BUG-045` ก่อนที่จะ merge pipeline ต้องการ output ในรูปแบบ JSON สำหรับการประมวลผลต่อ แต่ต้อง*ไม่* apply การเปลี่ยนแปลงใดๆ ไปยังไฟล์ต้นฉบับ

**คำสั่ง (Kilo Code Snippet):**

```bash
/smartspec_generate_spec.md \
  --spec-ids FEAT-1001,BUG-045 \
  --json \
  --out /tmp/spec_validation_report \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow แปลง paths ของ `FEAT-1001` และ `BUG-045` โดยใช้ `.spec/SPEC_INDEX.json`
2. ตรวจสอบความสมบูรณ์และการทำซ้ำสำหรับทั้งสองข้อกำหนด
3. สร้าง safe preview bundle ใน `/tmp/spec_validation_report/<run-id>/...`
4. output หลักคือไฟล์ `summary.json` ที่มีผลการตรวจสอบ การตรวจสอบความปลอดภัย และขั้นตอนถัดไป
5. ไม่มีไฟล์ใดภายใต้ `specs/**` หรือ `.spec/SPEC_INDEX.json` ถูกแก้ไข
6. Exit code `0` (สำเร็จ)

### กรณีที่ 3: ปฏิเสธการ Apply เนื่องจากการรั่วไหลของ Secret (CLI)

**สถานการณ์:** ผู้ใช้พยายามปรับปรุง spec แต่เนื้อหาที่สร้างขึ้น (preview) มี string ที่ตรงกับ redaction pattern ที่กำหนดไว้โดยไม่ตั้งใจ (เช่น test API key)

**คำสั่ง:**

```bash
/smartspec_generate_spec \
  --spec specs/new_feature.md \
  --apply
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow สร้างเนื้อหา preview
2. เนื้อหาตรงกับ redaction pattern ที่กำหนดไว้ (`safety.redaction`)
3. workflow ซ่อนค่าดังกล่าวใน preview/report output
4. workflow **ปฏิเสธ** การดำเนินการ `--apply` ตามที่กำหนดโดย Secret-blocking rule
5. สร้าง `report.md` โดยละเอียด โดยระบุการตรวจพบ secret และการปฏิเสธ apply
6. Exit code `1` (Validation Fail)

### กรณีที่ 4: สร้างข้อกำหนดใหม่ตั้งแต่ต้น (CLI)

**สถานการณ์:** นักพัฒนาต้องการสร้างข้อกำหนดใหม่สำหรับฟีเจอร์ "Payment Gateway Integration" ไฟล์ spec ยังไม่มีอยู่ ดังนั้น workflow จะสร้างขึ้นตาม completeness contract

**คำสั่ง:**

```bash
/smartspec_generate_spec \
  --spec specs/integrations/payment_gateway.md \
  --apply
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow ตรวจพบว่า `specs/integrations/payment_gateway.md` ไม่มีอยู่
2. สร้าง spec ใหม่พร้อมส่วนที่จำเป็นทั้งหมด (user stories, journeys, UI/UX baseline, NFRs ฯลฯ)
3. เขียน preview ไปยัง `.spec/reports/generate-spec/<run-id>/...`
4. ด้วย `--apply` ไฟล์ spec ใหม่ถูกสร้างที่ `specs/integrations/payment_gateway.md`
5. `.spec/SPEC_INDEX.json` ถูกอัปเดตด้วย spec entry ใหม่ (ถ้าอยู่ใน allowlist)
6. Exit code `0` (สำเร็จ)

### กรณีที่ 5: ตรวจจับข้อกำหนดที่ซ้ำกัน (Kilo Code)

**สถานการณ์:** สมาชิกในทีมพยายามสร้าง spec ใหม่สำหรับ "User Authentication" แต่มี spec ที่คล้ายกันอยู่แล้วใน index workflow ควรตรวจจับการทำซ้ำนี้และให้คำแนะนำ

**คำสั่ง (Kilo Code Snippet):**

```bash
/smartspec_generate_spec.md \
  --spec specs/auth/user_authentication.md \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลด `.spec/SPEC_INDEX.json` และคำนวณความคล้ายคลึงกับ specs ที่มีอยู่
2. ตรวจพบความตรงกันสูงกับ spec ที่มีอยู่ (เช่น `specs/security/authentication_service.md`)
3. เพิ่มส่วน **Reuse Warning** ใน preview
4. สร้าง decision record ใน `references/decisions.md` โดยแนะนำให้รวมกัน
5. workflow ไม่ merge specs อัตโนมัติ แต่ให้คำแนะนำสำหรับการตรวจสอบด้วยตนเอง
6. ไม่มี governed writes (ไม่มี flag `--apply`)
7. Exit code `0` (สำเร็จพร้อม warnings)

---

## 4. พารามิเตอร์

พารามิเตอร์และ flags ต่อไปนี้ควบคุมการทำงานและพฤติกรรมของ workflow `/smartspec_generate_spec`

### พารามิเตอร์ที่จำเป็น (เลือกอย่างใดอย่างหนึ่ง)

| พารามิเตอร์ | ชนิด | คำอธิบาย | การตรวจสอบ |
| :--- | :--- | :--- | :--- |
| `--spec` | `<path>` | เส้นทางไปยังไฟล์ spec เดียวที่ต้องการปรับปรุง | ต้อง resolve ภายใต้ `specs/**` ไม่สามารถใช้ร่วมกับ `--spec-ids` |
| `--spec-ids` | `<csv>` | รายการ spec IDs คั่นด้วยคอมมาที่ต้องการปรับปรุง | IDs ต้องตรงกับ `safety.spec_id_regex` และมีอยู่ใน `.spec/SPEC_INDEX.json` ไม่สามารถใช้ร่วมกับ `--spec` |

### Universal Flags

| Flag | คำอธิบาย | ค่าเริ่มต้น | การรองรับ Platform |
| :--- | :--- | :--- | :--- |
| `--config` | เส้นทางไปยังไฟล์ configuration ของ SmartSpec | `.spec/smartspec.config.yaml` | `cli` \| `kilo` \| `ci` \| `other` |
| `--lang` | ภาษาสำหรับการสร้างรายงาน (เช่น `th`, `en`) | (ค่าเริ่มต้นของระบบ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--platform` | บริบทของ platform ที่ใช้งาน **จำเป็นสำหรับ Kilo Code** | (อนุมานอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--apply` | เปิดใช้งาน governed writes (แก้ไข `specs/**` และอาจรวมถึง index) | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--out` | เส้นทางฐานสำหรับ safe outputs (รายงาน/previews) | `.spec/reports/generate-spec/` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | แสดงผลสรุปหลักในรูปแบบ JSON (`summary.json`) | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--quiet` | ปิดการแสดง log ออกทาง standard output | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Workflow-Specific Flags

| Flag | คำอธิบาย | การรองรับ Platform |
| :--- | :--- | :--- |
| `--spec` | เส้นทางไปยังไฟล์ spec เดียวที่ต้องการปรับปรุง (ดู Required Inputs) | `cli` \| `kilo` \| `ci` \| `other` |
| `--spec-ids` | รายการ spec IDs คั่นด้วยคอมมาที่ต้องการปรับปรุง (ดู Required Inputs) | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

workflow สร้าง output artifacts สองประเภท: Safe Preview Bundles (เสมอ) และ Governed Artifacts (เฉพาะเมื่อใช้ `--apply`)

### Safe Preview Bundle (สร้างเสมอ)

โฟลเดอร์ run ที่ไม่ซ้ำกันถูกสร้างขึ้นภายใต้เส้นทางรายงาน (ค่าเริ่มต้น: `.spec/reports/generate-spec/<run-id>/`)

**เนื้อหา:**

| เส้นทางไฟล์ | คำอธิบาย |
| :--- | :--- |
| `preview.md` | เนื้อหา spec ที่ปรับปรุงแล้ว (ก่อน apply) |
| `diff.md` | diff ที่แสดงการเปลี่ยนแปลงจากเวอร์ชันปัจจุบัน |
| `summary.json` | (ถ้าใช้ `--json`) ผลการตรวจสอบในรูปแบบโครงสร้าง |
| `report.md` | การวิเคราะห์โดยละเอียด รวมถึงการตรวจสอบความสมบูรณ์ คำเตือนการ reuse และผลการสแกนความปลอดภัย |

### Governed Artifacts (เฉพาะเมื่อใช้ `--apply`)

| เส้นทางไฟล์ | คำอธิบาย |
| :--- | :--- |
| `specs/**/*.md` | ไฟล์ spec ที่ปรับปรุงแล้วเขียนไปยังตำแหน่ง governed |
| `.spec/SPEC_INDEX.json` | spec index ที่อัปเดต (เฉพาะเมื่ออยู่ใน allowlist ใน config) |

---

## 6. หมายเหตุ

- **SPEC-first Governance:** workflow นี้บังคับใช้หลักการกำกับดูแลอย่างเคร่งครัด ตรวจสอบ preview bundle เสมอก่อนใช้ `--apply`
- **Reuse-first Behavior:** workflow ตรวจจับการทำซ้ำอย่างแข็งขันและส่งเสริมการรวมกันแทนการสร้าง specs ที่ซ้ำซ้อน
- **Secret Protection:** เนื้อหาใดๆ ที่ตรงกับ redaction patterns ที่กำหนดไว้จะบล็อกการดำเนินการ `--apply` เพื่อป้องกันการรั่วไหลของ secret โดยไม่ตั้งใจ
- **Network Policy:** workflow นี้เคารพ `safety.network_policy.default=deny` และไม่ทำ external network requests
- **Symlink Safety:** ถ้า `safety.disallow_symlink_writes=true` workflow จะปฏิเสธการเขียนผ่าน symlinks
- **Kilo Code Platform:** เมื่อใช้ Kilo Code ต้องใส่ `--platform kilo` เสมอเพื่อให้แน่ใจว่ามี context และ logging ที่เหมาะสม

---

**สิ้นสุดคู่มือ**
