| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_generate_tasks คู่มือ (TH) | 6.0 | /smartspec_generate_tasks | 6.0.x |

# /smartspec_generate_tasks คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

คู่มือนี้อธิบายวิธีการใช้งาน workflow:

`/smartspec_generate_tasks` workflow ใช้สำหรับสร้างหรือปรับปรุง `tasks.md` จาก `spec.md` (หรือ `plan.md`) ให้อยู่ในรูปแบบ **พร้อมสำหรับการตรวจสอบ** 

**วัตถุประสงค์:** แปลง spec.md (หรือ plan.md) → tasks.md (พร้อมสำหรับการตรวจสอบ; รักษา ID/checkboxes; รายงานจะถูกเขียนเสมอ) workflow นี้เป็นแหล่งข้อมูลหลักสำหรับการสร้าง tasks ที่ workflow downstream สามารถไว้วางใจได้ รวมถึง verify_tasks_progress_strict, report_implement_prompter และ quality_gate

**เวอร์ชัน:** 6.0.2  
**หมวดหมู่:** core

---

## 2. การใช้งาน

workflow นี้รองรับการเรียกใช้งานผ่าน Command Line Interface (CLI) และ Kilo Code (สภาพแวดล้อมสคริปต์ภายใน)

### 🔗 การใช้งาน CLI

การเรียกใช้งาน CLI ต้องระบุไฟล์ spec.md หรือ plan.md เป้าหมายเป็น positional argument

```bash
/smartspec_generate_tasks <spec_or_plan_md> \
  [--json] \
  [--apply]
```

### การใช้งาน Kilo Code

การเรียกใช้งาน Kilo Code มีโครงสร้างเหมือนกับ CLI โดยมักใช้ใน pipeline อัตโนมัติหรือสคริปต์ภายใน

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องใส่ flag `--platform kilo` ด้วยเสมอ

```bash
/smartspec_generate_tasks.md \
  <spec_or_plan_md> \
  [--json] \
  [--apply] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: สร้าง Tasks จาก Specification (CLI)

**สถานการณ์:** นักพัฒนาได้จัดทำ specification สำหรับ "User Authentication Service" (`specs/auth/user_auth_service/spec.md`) เสร็จแล้ว และต้องการสร้างรายการ task ที่สามารถนำไปปฏิบัติได้

**คำสั่ง:**

```bash
/smartspec_generate_tasks specs/auth/user_auth_service/spec.md
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดไฟล์ `specs/auth/user_auth_service/spec.md`
2. วิเคราะห์เนื้อหา spec และสร้าง tasks ที่พร้อมสำหรับการตรวจสอบ
3. เขียน preview bundle ไปยัง `.spec/reports/generate-tasks/<run-id>/`
4. preview ประกอบด้วย `tasks_preview.md` ที่มี task ID เฉพาะและ evidence hooks
5. ไม่มีการเขียนไฟล์จริง (ไม่มี flag `--apply`)
6. exit code `0` (สำเร็จ)

### กรณีการใช้งาน 2: สร้าง Tasks จาก Plan (Kilo Code)

**สถานการณ์:** pipeline CI สร้างไฟล์ plan (`specs/payments/checkout/plan.md`) แล้ว ต้องการสร้าง tasks ตามแต่ละ phase ของ plan โดย tasks เหล่านี้จะถูกนำไปใช้ทันที

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_generate_tasks.md \
  specs/payments/checkout/plan.md \
  --apply \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดไฟล์ `specs/payments/checkout/plan.md`
2. ค้นหาไฟล์ `spec.md` ที่อยู่ในโฟลเดอร์เดียวกันเพื่อใช้บริบทเพิ่มเติม
3. สร้าง tasks ตาม phase ของ plan พร้อม dependencies ที่เหมาะสม
4. เขียน preview bundle ไปยัง `.spec/reports/generate-tasks/<run-id>/`
5. เนื่องจากใช้ `--apply` ไฟล์ tasks จะถูกเขียนจริงที่ `specs/payments/checkout/tasks.md`
6. exit code `0` (สำเร็จ)

### กรณีการใช้งาน 3: ปรับปรุง Tasks ที่มีอยู่แล้ว (รักษา Checkboxes) (CLI)

**สถานการณ์:** ทีมงานได้ทำงานบางส่วนใน tasks ที่อยู่ใน `specs/notifications/email_service/tasks.md` แล้ว spec มีการอัปเดตด้วยข้อกำหนดใหม่ จึงต้องสร้าง tasks ใหม่โดยยังคงรักษา checkboxes ที่ทำเสร็จแล้วไว้

**คำสั่ง:**

```bash
/smartspec_generate_tasks specs/notifications/email_service/spec.md \
  --apply
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดไฟล์ `specs/notifications/email_service/tasks.md` ที่มีอยู่แล้ว
2. ระบุ tasks ที่ทำเสร็จ (checkboxes ที่ถูกติ๊ก) โดยใช้ task ID เฉพาะ
3. สร้าง tasks ใหม่ตาม spec ที่อัปเดต
4. รักษา tasks ที่ทำเสร็จไว้พร้อมกับ checkboxes ที่ถูกติ๊ก
5. tasks ที่ล้าสมัยจะถูกทำเครื่องหมายว่า deprecated แทนการลบ
6. เขียนไฟล์ `tasks.md` ที่อัปเดตด้วย flag `--apply`
7. exit code `0` (สำเร็จ)

### กรณีการใช้งาน 4: ผลลัพธ์ JSON สำหรับการประมวลผลโปรแกรม (CLI)

**สถานการณ์:** นักพัฒนาต้องการผนวกการสร้าง tasks เข้ากับเครื่องมือเฉพาะที่ต้องการผลลัพธ์ในรูปแบบ JSON เพื่อประมวลผลต่อ

**คำสั่ง:**

```bash
/smartspec_generate_tasks specs/api/rest_api/spec.md \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดไฟล์ `specs/api/rest_api/spec.md`
2. สร้าง tasks ในรูปแบบพร้อมตรวจสอบ
3. เขียน preview bundle ไปยัง `.spec/reports/generate-tasks/<run-id>/`
4. ผลลัพธ์ประกอบด้วย `summary.json` ที่มี metadata ของ tasks และผลการตรวจสอบในรูปแบบโครงสร้าง
5. ไม่มีการเขียนไฟล์จริง (ไม่มี flag `--apply`)
6. exit code `0` (สำเร็จ)

### กรณีการใช้งาน 5: ปฏิเสธการนำไปใช้เนื่องจากการรั่วไหลของความลับ (CLI)

**สถานการณ์:** ผู้ใช้พยายามสร้าง tasks แต่เนื้อหา spec มี API key รั่วไหลโดยไม่ตั้งใจ workflow ต้องตรวจจับและปฏิเสธการนำ tasks ไปใช้

**คำสั่ง:**

```bash
/smartspec_generate_tasks specs/integrations/third_party_api/spec.md \
  --apply
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดไฟล์ `specs/integrations/third_party_api/spec.md`
2. สร้าง preview tasks
3. เนื้อหาตรงกับรูปแบบการปกปิดข้อมูลที่ตั้งค่าไว้ (เช่น API key)
4. workflow ทำการปกปิดค่าที่เป็นความลับใน preview/รายงาน
5. workflow **ปฏิเสธ** การดำเนินการ `--apply`
6. สร้างไฟล์ `report.md` รายงานรายละเอียดการตรวจจับความลับ
7. exit code `1` (ตรวจสอบไม่ผ่าน)

---

## 4. พารามิเตอร์

พารามิเตอร์และ flags ต่อไปนี้ใช้ควบคุมการทำงานและพฤติกรรมของ `/smartspec_generate_tasks` workflow

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `<spec_or_plan_md>` | `<path>` | เส้นทางไปยังไฟล์ spec.md หรือ plan.md ที่จะสร้าง tasks | ต้องอยู่ภายใต้ `specs/**` และไม่อนุญาตให้หลุดออกนอกผ่าน symlink |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--config` | เส้นทางไปยังไฟล์ตั้งค่า SmartSpec | `.spec/smartspec.config.yaml` | `cli` \| `kilo` \| `ci` \| `other` |
| `--lang` | ภาษาในการสร้างรายงาน (เช่น `th`, `en`) | (ค่าเริ่มต้นระบบ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--platform` | บริบทแพลตฟอร์มการทำงาน **จำเป็นสำหรับ Kilo Code** | (อนุมานอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--apply` | เปิดใช้งานการเขียนที่มีการควบคุม (แก้ไข `specs/**/tasks.md`) | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--out` | เส้นทางฐานสำหรับผลลัพธ์ที่ปลอดภัย (รายงาน/preview) | `.spec/reports/generate-tasks/` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ส่งออกสรุปหลักในรูปแบบ JSON (`summary.json`) | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--quiet` | ปิดการแสดงผล log ปกติ | `false` | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

workflow สร้างผลลัพธ์ออกมา 2 ประเภท: Safe Preview Bundles (สร้างเสมอ) และ Governed Artifacts (สร้างเฉพาะเมื่อใช้ `--apply`)

### Safe Preview Bundle (สร้างเสมอ)

จะสร้างโฟลเดอร์รันที่ไม่ซ้ำกันภายใต้เส้นทางรายงาน (ค่าเริ่มต้น: `.spec/reports/generate-tasks/<run-id>/`)

**เนื้อหา:**

| File Path | Description |
| :--- | :--- |
| `tasks_preview.md` | เนื้อหา tasks ที่สร้างขึ้น (ก่อนการ apply) |
| `summary.json` | (ถ้าใช้ `--json`) metadata ของ tasks และผลการตรวจสอบในรูปแบบโครงสร้าง |
| `report.md` | รายงานวิเคราะห์รายละเอียด รวมจำนวน tasks, evidence hooks และผลสแกนความปลอดภัย |
| `diff.md` | (ถ้าปรับปรุง tasks ที่มีอยู่) แสดงความแตกต่างจากเวอร์ชันปัจจุบัน |

### Governed Artifacts (สร้างเฉพาะเมื่อใช้ `--apply`)

| File Path | Description |
| :--- | :--- |
| `specs/**/tasks.md` | ไฟล์ tasks ที่สร้างหรือปรับปรุงแล้วถูกเขียนไปยังตำแหน่งที่ควบคุม |

---

## 6. หมายเหตุ

- **รูปแบบพร้อมตรวจสอบ:** Tasks ถูกสร้างพร้อม ID เฉพาะและ evidence hooks สำหรับ workflow ตรวจสอบ downstream
- **การรักษา Checkboxes:** เมื่อปรับปรุง tasks ที่มีอยู่แล้ว จะรักษา checkboxes ที่ทำเสร็จตาม task ID
- **การอัปเดตแบบไม่ทำลาย:** Tasks ที่ล้าสมัยจะถูกทำเครื่องหมาย deprecated แทนการลบ เพื่อรักษาประวัติการตรวจสอบ
- **การปกป้องความลับ:** เนื้อหาที่ตรงกับรูปแบบการปกปิดข้อมูลที่ตั้งค่าไว้จะบล็อกการดำเนินการ `--apply`
- **นโยบายเครือข่าย:** workflow นี้เคารพ `safety.network_policy.default=deny` และไม่ทำการร้องขอเครือข่ายภายนอก
- **ความปลอดภัยของ Symlink:** หากตั้งค่า `safety.disallow_symlink_writes=true` workflow จะปฏิเสธการเขียนผ่าน symlink
- **แพลตฟอร์ม Kilo Code:** เมื่อใช้ Kilo Code ต้องใส่ `--platform kilo` เสมอเพื่อให้บริบทและการบันทึกถูกต้อง
- **สายงาน Canonical:** workflow นี้เป็นส่วนหนึ่งของสายงานหลัก SmartSpec: validate_index → generate_spec → generate_plan → **generate_tasks** → verify_tasks_progress_strict → sync_tasks_checkboxes → report_implement_prompter

---

**จบคู่มือ**