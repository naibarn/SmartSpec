| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_generate_tests คู่มือ (TH) | 6.1 | /smartspec_generate_tests | 6.1.x |

# /smartspec_generate_tests คู่มือ (v6.1, ภาษาไทย)

## 1. ภาพรวม

คู่มือนี้อธิบายวิธีการใช้งาน workflow:

`/smartspec_generate_tests` workflow สร้าง **แผนการทดสอบ** อย่างครบถ้วนจากไฟล์ `spec.md` (และไฟล์ `tasks.md` ที่อยู่ติดกัน รวมถึง `ui.json` ที่เป็นทางเลือก) โดยสอดคล้องกับ registries และการกำกับดูแลของ SmartSpec

**วัตถุประสงค์:** สร้างแผนการทดสอบที่ถูกกำกับโดย SmartSpec (test matrix + acceptance criteria + หลักฐานที่ต้องการ) ให้สอดคล้องกับการกำกับดูแลของ SmartSpec v6 โดยผลลัพธ์เริ่มต้นจะเป็นรายงานเท่านั้น; การใช้ตัวเลือก `--apply` จะเขียนแผนการทดสอบลงในโฟลเดอร์ spec ภายใต้ specs/**

**เวอร์ชัน:** 6.1.1  
**หมวดหมู่:** การวางแผนการทดสอบ

---

## 2. การใช้งาน

workflow นี้รองรับการเรียกใช้งานผ่าน Command Line Interface (CLI) และ Kilo Code (สภาพแวดล้อมสคริปต์ภายใน)

### 🔗 การใช้งาน CLI

การเรียกใช้งาน CLI จำเป็นต้องระบุไฟล์ spec.md ที่ต้องการเป็นอาร์กิวเมนต์ตำแหน่ง

```bash
/smartspec_generate_tests <spec_md> \
  [--apply] \
  [--test-target <path>] \
  [--json]
```

### การใช้งาน Kilo Code

การเรียกใช้งาน Kilo Code มีโครงสร้างเหมือนกับ CLI โดยปกติใช้ใน pipeline อัตโนมัติหรือสคริปต์ภายใน

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องใส่ flag `--platform kilo` ด้วยเสมอ

```bash
/smartspec_generate_tests.md \
  <spec_md> \
  [--apply] \
  [--test-target <path>] \
  [--json] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: สร้างแผนการทดสอบจากสเปค (CLI)

**สถานการณ์:** วิศวกร QA ต้องการสร้างแผนการทดสอบอย่างครบถ้วนสำหรับฟีเจอร์ "Shopping Cart" โดยอิงจากสเปค (`specs/ecommerce/shopping_cart/spec.md`)

**คำสั่ง:**

```bash
/smartspec_generate_tests specs/ecommerce/shopping_cart/spec.md
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดไฟล์ `specs/ecommerce/shopping_cart/spec.md`
2. วิเคราะห์เนื้อหาสเปคและสร้าง test matrix พร้อม acceptance criteria
3. เขียน preview bundle ลงใน `.spec/reports/generate-tests/<run-id>/`
4. preview ประกอบด้วยไฟล์ `tests_preview.md` ที่มี test cases และความต้องการหลักฐาน
5. ไม่มีการเขียนไฟล์ภายใต้การกำกับ (ไม่มี flag `--apply`)
6. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: ใช้แผนการทดสอบพร้อมกำหนดเส้นทางเป้าหมายแบบกำหนดเอง (Kilo Code)

**สถานการณ์:** pipeline CI ต้องการสร้างและใช้แผนการทดสอบสำหรับสเปค โดยเก็บไว้ในตำแหน่งที่กำหนดเองภายในโฟลเดอร์ spec

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_generate_tests.md \
  specs/api/rest_endpoints/spec.md \
  --apply \
  --test-target specs/api/rest_endpoints/testplan/tests.md \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดไฟล์ `specs/api/rest_endpoints/spec.md`
2. สร้างแผนการทดสอบอย่างครบถ้วน
3. เขียน preview bundle ลงใน `.spec/reports/generate-tests/<run-id>/`
4. ด้วย `--apply` แผนการทดสอบจะถูกเขียนลงใน `specs/api/rest_endpoints/testplan/tests.md`
5. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: สร้างแผนการทดสอบพร้อมรวมไฟล์ UI.json (CLI)

**สถานการณ์:** นักพัฒนามีสเปคที่มีไฟล์ `ui.json` ประกอบอยู่ด้วย ซึ่งกำหนดส่วนประกอบ UI แผนการทดสอบควรรวม test cases เฉพาะ UI ด้วย

**คำสั่ง:**

```bash
/smartspec_generate_tests specs/dashboard/analytics_dashboard/spec.md \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดไฟล์ `specs/dashboard/analytics_dashboard/spec.md`
2. ตรวจพบและโหลดไฟล์ `ui.json` ที่อยู่ติดกัน
3. สร้าง test cases สำหรับทั้งฟังก์ชันและความต้องการ UI
4. เขียน preview bundle ลงใน `.spec/reports/generate-tests/<run-id>/`
5. ผลลัพธ์ประกอบด้วย `summary.json` ที่มี metadata การทดสอบแบบมีโครงสร้าง
6. ไม่มีการเขียนไฟล์ภายใต้การกำกับ (ไม่มี flag `--apply`)
7. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 4: ดูตัวอย่างแผนการเปลี่ยนแปลงก่อน (CLI)

**สถานการณ์:** หัวหน้าทีมต้องการตรวจสอบว่ามีการเปลี่ยนแปลงอะไรในแผนการทดสอบก่อนที่จะนำไปใช้จริง

**คำสั่ง:**

```bash
/smartspec_generate_tests specs/auth/oauth_integration/spec.md
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดไฟล์ `specs/auth/oauth_integration/spec.md`
2. สร้าง preview แผนการทดสอบ
3. สร้าง **Change Plan** อธิบายว่ามีอะไรจะถูกเขียนลงในเส้นทางที่ถูกกำกับ
4. preview bundle ประกอบด้วย `change_plan.md` ที่มีรายละเอียดความแตกต่าง
5. ไม่มีการเขียนไฟล์ภายใต้การกำกับ (ไม่มี flag `--apply`)
6. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 5: ปฏิเสธการใช้แผนเนื่องจากพยายามใช้งาน path traversal (CLI)

**สถานการณ์:** ผู้ใช้พยายามระบุเส้นทาง test target ที่หลุดออกจากขอบเขตที่อนุญาต workflow ควรตรวจจับและปฏิเสธการทำงาน

**คำสั่ง:**

```bash
/smartspec_generate_tests specs/feature/new_feature/spec.md \
  --apply \
  --test-target ../../outside/tests.md
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow ตรวจสอบเส้นทาง `--test-target`
2. ตรวจพบความพยายาม path traversal (`..`)
3. workflow **ปฏิเสธ** การทำงานพร้อมแจ้งข้อผิดพลาดการตรวจสอบเส้นทาง
4. สร้างข้อความแสดงข้อผิดพลาดอย่างละเอียด
5. รหัสออก `2` (ตรวจสอบเส้นทางล้มเหลว)

---

## 4. พารามิเตอร์

พารามิเตอร์และ flags ต่อไปนี้ควบคุมการทำงานและพฤติกรรมของ workflow `/smartspec_generate_tests`

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `<spec_md>` | `<path>` | เส้นทางไปยังไฟล์ spec.md ที่จะสร้างการทดสอบจากไฟล์นี้ | ต้องอยู่ภายใต้ `specs/**` และไม่หลุดออกผ่าน symlink |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--config` | เส้นทางไปยังไฟล์กำหนดค่า SmartSpec | `.spec/smartspec.config.yaml` | `cli` \| `kilo` \| `ci` \| `other` |
| `--lang` | ภาษาในการสร้างรายงาน (เช่น `th`, `en`) | (ค่าปริยายของระบบ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--platform` | บริบทแพลตฟอร์มการทำงาน **จำเป็นสำหรับ Kilo Code** | (คาดเดาอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--apply` | เปิดใช้งานการเขียนภายใต้การกำกับ (สร้างไฟล์แผนการทดสอบ) | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--out` | เส้นทางฐานสำหรับผลลัพธ์ที่ปลอดภัย (รายงาน/preview) | `.spec/reports/generate-tests/` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ส่งออกสรุปหลักในรูปแบบ JSON (`summary.json`) | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--quiet` | ปิดการแสดงผล log ปกติ | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--test-target` | เส้นทางกำหนดเองสำหรับไฟล์แผนการทดสอบ (ต้องใช้กับ `--apply`) | `specs/<category>/<spec-id>/testplan/tests.md` | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

workflow สร้างผลลัพธ์สองประเภท: Safe Preview Bundles (สร้างเสมอ) และ Governed Artifacts (สร้างเฉพาะเมื่อใช้ `--apply`)

### Safe Preview Bundle (สร้างเสมอ)

จะสร้างโฟลเดอร์รันที่ไม่ซ้ำกันภายใต้เส้นทางรายงาน (ค่าปริยาย: `.spec/reports/generate-tests/<run-id>/`)

**เนื้อหา:**

| File Path | Description |
| :--- | :--- |
| `tests_preview.md` | เนื้อหาแผนการทดสอบที่สร้าง (ก่อนใช้ `--apply`) |
| `change_plan.md` | คำอธิบายว่ามีอะไรจะถูกเขียนลงในเส้นทางที่ถูกกำกับ |
| `summary.json` | (ถ้าใช้ `--json`) metadata การทดสอบแบบมีโครงสร้างและการวิเคราะห์ความครอบคลุม |
| `report.md` | การวิเคราะห์โดยละเอียดรวมถึง test matrix และความต้องการหลักฐาน |

### Governed Artifacts (สร้างเฉพาะเมื่อใช้ `--apply`)

| File Path | Description |
| :--- | :--- |
| `specs/<category>/<spec-id>/testplan/tests.md` | ไฟล์แผนการทดสอบที่ถูกเขียนลงในตำแหน่งที่ถูกกำกับ |

---

## 6. หมายเหตุ

- **Preview-first:** สร้าง Change Plan อธิบายสิ่งที่จะถูกเขียนก่อนเสมอ ก่อนนำไปใช้จริง
- **Test Matrix:** แผนการทดสอบที่สร้างมี test matrix ครอบคลุมพร้อม acceptance criteria
- **Evidence Requirements:** test cases รวม hook หลักฐานที่ต้องการสำหรับ workflow การตรวจสอบ
- **UI Integration:** ตรวจจับและรวมไฟล์ `ui.json` ที่อยู่ติดกันโดยอัตโนมัติสำหรับ test cases เฉพาะ UI
- **Path Security:** บังคับใช้การตรวจสอบเส้นทางอย่างเข้มงวดเพื่อป้องกัน path traversal และการหลบหนีผ่าน symlink
- **Atomic Writes:** เมื่อใช้ `--apply` การเขียนไฟล์แผนการทดสอบจะใช้วิธี atomic write (เขียนชั่วคราว + เปลี่ยนชื่อ)
- **Network Policy:** workflow นี้เคารพ `safety.network_policy.default=deny` และไม่ทำการเรียกเครือข่ายภายนอก
- **Kilo Code Platform:** เมื่อใช้ Kilo Code ต้องใส่ `--platform kilo` เสมอเพื่อให้บริบทและการบันทึกถูกต้อง

---

**จบคู่มือ**