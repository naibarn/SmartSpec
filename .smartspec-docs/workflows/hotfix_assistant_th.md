| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_hotfix_assistant คู่มือ (TH) | 6.0 | /smartspec_hotfix_assistant | 6.0.x |

# /smartspec_hotfix_assistant คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

workflow `/smartspec_hotfix_assistant` ช่วยในการสร้างและจัดการ hotfix สำหรับปัญหาในระบบ production

**วัตถุประสงค์:** ช่วยในการสร้างและจัดการ hotfix พร้อมให้คำแนะนำสำหรับการแก้ไขปัญหาเร่งด่วนใน production

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** operations-deployment

---

## 2. การใช้งาน

### 🔗 การใช้งาน CLI

```bash
/smartspec_hotfix_assistant \
  --issue <description> \
  [--severity <critical|high|medium>] \
  [--apply]
```

### การใช้งาน Kilo Code

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องระบุ flag `--platform kilo` เสมอ

```bash
/smartspec_hotfix_assistant.md \
  --issue <description> \
  [--severity <critical|high|medium>] \
  [--apply] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: การสร้างแผน hotfix (CLI)

**สถานการณ์:** สร้าง hotfix สำหรับบั๊ก production ที่มีความรุนแรงระดับ critical

**คำสั่ง:**

```bash
/smartspec_hotfix_assistant \
  --issue "Payment processing timeout" \
  --severity critical
```

**ผลลัพธ์ที่คาดหวัง:**

1. แผน hotfix ถูกสร้างขึ้น
2. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: Hotfix ความรุนแรงสูง (Kilo Code)

**สถานการณ์:** การช่วยเหลือ hotfix อัตโนมัติ

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_hotfix_assistant.md \
  --issue "Authentication service down" \
  --severity high \
  --apply \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. แผน hotfix ถูกสร้างและบันทึกไว้
2. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: ปัญหาระดับความรุนแรงกลาง (CLI)

**สถานการณ์:** วางแผน hotfix สำหรับปัญหาระดับความรุนแรง medium

**คำสั่ง:**

```bash
/smartspec_hotfix_assistant \
  --issue "UI rendering glitch" \
  --severity medium \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. แผน hotfix พร้อมผลลัพธ์ในรูปแบบ JSON
2. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `--issue` | `<string>` | คำอธิบายของปัญหาที่ต้องการ hotfix | ต้องไม่ว่างเปล่า |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--platform` | แพลตฟอร์มที่ใช้รันคำสั่ง **จำเป็นสำหรับ Kilo Code** | (อนุมานค่า) | `cli` \| `kilo` \| `ci` \| `other` |
| `--apply` | บันทึกแผน hotfix | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | แสดงผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--severity` | ความรุนแรงของปัญหา: `critical`, `high`, `medium` | `high` | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/hotfix-assistant/<run-id>/hotfix_plan.md` | แผน hotfix |

---

## 6. หมายเหตุ

- **Flag แพลตฟอร์ม:** เมื่อใช้ Kilo Code ต้องระบุ `--platform kilo` เสมอ

---

**จบคู่มือ**