# คู่มือภาษาไทย — /smartspec_implement_tasks (v5.7.2)

คู่มือนี้เป็นเวอร์ชัน **ภาษาไทยเต็มรูปแบบ** สอดคล้องกับ workflow รุ่นล่าสุดที่เพิ่ม:
- `--require-orchestrator` (บังคับใช้ Orchestrator)
- พฤติกรรม Orchestrator ที่เข้มขึ้น
- กติกา fallback / strict แบบใหม่
- กติกาการจัดการ error ของ Kilo
- ตัวอย่างการใช้งานที่อัปเดตทั้งหมด

---

## 1. ภาพรวม

`/smartspec_implement_tasks` คือ workflow สำหรับ **ลงมือ implement โค้ดจริงจากไฟล์ `tasks.md`** โดยต้องปฏิบัติตามกติกา SmartSpec ดังนี้:
- ใช้ SPEC_INDEX + registry เป็นแหล่งอ้างอิงกลาง
- เคารพ boundary ของ multi-repo
- ปฏิบัติตามกฎ UI governance หากมี `ui.json`
- ปฏิบัติตาม guardrail ของ Web stack, AI, และข้อมูลอ่อนไหว
- รองรับ Kilo Orchestrator Mode (`--kilocode`)

ในเวอร์ชันใหม่ **คุณสามารถบังคับให้ Orchestrator ต้องเปิดก่อนเริ่มงาน** ด้วย `--require-orchestrator`.

---

## 2. สิ่งที่อัปเดตในเวอร์ชัน 5.7.2

### 🔥 2.1 Flag ใหม่: `--require-orchestrator`
เมื่อใช้คู่กับ `--kilocode` หมายความว่า:
> "ต้องเปิด Orchestrator ก่อนเริ่ม implement ถ้าเปิดไม่ได้ให้หยุดทันที (strict mode)"

พฤติกรรม:
- ถ้าอยู่ใน Kilo:
  - Orchestrator เปิด → ทำงานต่อ
  - Orchestrator ปิด →
    - strict → **แจ้ง error และหยุดทำงานทันที**
    - dev → เตือนเสียงดัง อาจ fallback หรือหยุด

- ถ้า *ไม่ได้อยู่ใน Kilo* → แค่แจ้งโน้ต ไม่มีผลอะไร

### 🔥 2.2 วิธีจัดการเมื่อ Kilo Edit ไม่สำเร็จ (ปรับปรุงใหม่)
- ห้ามหยุดทันที
- ลด scope ให้เล็กลง
- แนะนำวิธี retry อัตโนมัติ
- ให้ Orchestrator ทำงานทีละ task แทน mega-edit

### 🔥 2.3 อัปเดต Step-by-step Flow
มีขั้นตอนใหม่ **Step 1.a** สำหรับตรวจ Orchestrator ก่อนเริ่ม run.

---

## 3. Input / Output

### Input ที่ต้องมี
- เส้นทางไปยัง `tasks.md`

### Input เสริม
- `spec.md`, `plan.md`, `ui.json`, SPEC_INDEX, registry, multi-repo config

### Output
- โค้ดที่ปรับเปลี่ยนแล้ว
- task checkbox อัปเดต
- สรุปงานท้าย run
- รายงานภายใต้ `.spec/reports/implement-tasks/` (ถ้ามีระบุ)

---

## 4. Flag สำคัญ (อัปเดต)

### 🔹 Orchestrator
```
--kilocode
--require-orchestrator
```

### 🔹 Safety
```
--safety-mode=strict | dev
--strict
```

### 🔹 Validation
```
--validate-only
--dry-run
```

### 🔹 เลือกงานที่ต้องการทำ
```
--task
--tasks
--range
--from
--skip-completed
--resume
```

### 🔹 Multi-repo / registry
```
--workspace-roots
--repos-config
--registry-dir
--registry-roots
```

---

## 5. พฤติกรรม Orchestrator (อัปเดต)

### 5.1 ใช้ `--kilocode` อย่างเดียว
- ระบบพยายามเปิด Orchestrator
- ถ้าเปิดไม่ได้ → fallback พร้อมคำเตือน

### 5.2 ใช้ `--kilocode --require-orchestrator`
- **ต้องเปิด Orchestrator เท่านั้น**
- ถ้าไม่เปิด:
  - strict → error + หยุด
  - dev → เตือนแรง อาจ fallback หรือหยุด

### 5.3 เมื่อ Kilo แก้ไฟล์ไม่สำเร็จ
- อย่าให้ล้มทั้ง workflow
- ให้ลดขอบเขตการแก้ไฟล์
- แนะนำ split task / retry / reduce file-range

---

## 6. ตัวอย่างการใช้งาน

### ตัวอย่าง 1 — บังคับ Orchestrator
```
/smartspec_implement_tasks specs/feature/spec-001/tasks.md \
  --kilocode --require-orchestrator --safety-mode=strict
```

### ตัวอย่าง 2 — ใช้ Orchestrator แต่ fallback ได้
```
/smartspec_implement_tasks specs/feature/spec-001/tasks.md --kilocode
```

### ตัวอย่าง 3 — ตรวจสอบเฉย ๆ (ไม่แก้โค้ด)
```
/smartspec_implement_tasks specs/feature/spec-001/tasks.md --validate-only
```

---

## 7. Troubleshooting (อัปเดต)

### ❗ Orchestrator ไม่เปิด แต่ใช้ `--require-orchestrator`
ระบบแสดง error ชัดเจน:
> "โหมด Orchestrator จำเป็นต้องเปิด (`--require-orchestrator`) แต่ไม่สามารถเปิดได้ กรุณาเปิดจาก UI ของ Kilo แล้วรันใหม่"

### ❗ ทำไมต้องเข้มกับ Orchestrator?
เพราะถ้าไม่ใช้ Orchestrator มักพบว่า:
- context ไม่พอ → แก้ไฟล์ผิดตำแหน่ง
- scope ใหญ่เกินเก็บความเข้าใจ
- งานหลายไฟล์ conflict กันง่าย

---

# END OF THAI MANUAL

