| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_nfr_perf_planner คู่มือ (TH) | 6.0 | /smartspec_nfr_perf_planner | 6.0.x |

# /smartspec_nfr_perf_planner คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

workflow `/smartspec_nfr_perf_planner` สร้างข้อกำหนดที่ไม่ใช่ฟังก์ชัน (NFR) และแผนประสิทธิภาพจากสเปค

**วัตถุประสงค์:** สร้างข้อกำหนดที่ไม่ใช่ฟังก์ชัน (NFR) และแผนประสิทธิภาพ กำหนดเป้าหมายประสิทธิภาพและกลยุทธ์การทดสอบ

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** nfr-performance

---

## 2. การใช้งาน

### 🔗 การใช้งาน CLI

```bash
/smartspec_nfr_perf_planner \
  <spec_md> \
  [--nfr-type <performance|scalability|reliability>] \
  [--apply]
```

### การใช้งาน Kilo Code

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องระบุ flag `--platform kilo` เสมอ

```bash
/smartspec_nfr_perf_planner.md \
  <spec_md> \
  [--nfr-type <performance|scalability|reliability>] \
  [--apply] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: สร้างแผนประสิทธิภาพ (CLI)

**สถานการณ์:** สร้างข้อกำหนดประสิทธิภาพสำหรับ API

**คำสั่ง:**

```bash
/smartspec_nfr_perf_planner specs/api/search_engine/spec.md \
  --nfr-type performance
```

**ผลลัพธ์ที่คาดหวัง:**

1. สร้าง NFR ด้านประสิทธิภาพ
2. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: วางแผนความสามารถในการขยายตัว (Kilo Code)

**สถานการณ์:** วางแผนข้อกำหนดความสามารถในการขยายตัว

**คำสั่ง (Kilo Code Snippet):**

```bash
/smartspec_nfr_perf_planner.md \
  specs/system/load_balancer/spec.md \
  --nfr-type scalability \
  --apply \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. สร้างแผนความสามารถในการขยายตัวและบันทึกไฟล์
2. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: ข้อกำหนดความน่าเชื่อถือ (CLI)

**สถานการณ์:** กำหนด NFR ด้านความน่าเชื่อถือ

**คำสั่ง:**

```bash
/smartspec_nfr_perf_planner specs/services/payment/spec.md \
  --nfr-type reliability \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. NFR ด้านความน่าเชื่อถือพร้อมผลลัพธ์ในรูปแบบ JSON
2. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `<spec_md>` | `<path>` | เส้นทางไปยังไฟล์ spec.md | ต้องอยู่ภายใต้ `specs/**` |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--platform` | แพลตฟอร์มที่ใช้รัน **จำเป็นสำหรับ Kilo Code** | (อนุมานค่า) | `cli` \| `kilo` \| `ci` \| `other` |
| `--apply` | บันทึกแผน NFR ลงในโฟลเดอร์สเปค | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | แสดงผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--nfr-type` | ประเภท NFR: `performance`, `scalability`, `reliability` | `performance` | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/nfr-perf-planner/<run-id>/nfr_plan.md` | แผน NFR |
| `specs/**/nfr/performance.md` | แผน NFR ที่บันทึกไว้ (เมื่อใช้ `--apply`) |

---

## 6. หมายเหตุ

- **Flag แพลตฟอร์ม:** เมื่อใช้ Kilo Code ต้องระบุ `--platform kilo` เสมอ

---

**จบคู่มือ**