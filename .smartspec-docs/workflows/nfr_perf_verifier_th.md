| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_nfr_perf_verifier คู่มือ (TH) | 6.0 | /smartspec_nfr_perf_verifier | 6.0.x |

# /smartspec_nfr_perf_verifier คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

workflow `/smartspec_nfr_perf_verifier` ใช้สำหรับตรวจสอบว่า implementations ตรงตามข้อกำหนดที่ไม่ใช่ฟังก์ชัน (non-functional requirements) และเป้าหมายด้านประสิทธิภาพที่กำหนดไว้หรือไม่

**วัตถุประสงค์:** ตรวจสอบว่า implementations ตรงตาม NFRs และเป้าหมายด้านประสิทธิภาพที่กำหนดไว้ผ่านการทดสอบและการวัดผล

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** nfr-performance

---

## 2. การใช้งาน

### 🔗 การใช้งาน CLI

```bash
/smartspec_nfr_perf_verifier \
  <nfr_plan> \
  [--test-results <path>] \
  [--strict]
```

### การใช้งาน Kilo Code

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องระบุ flag `--platform kilo` เสมอ

```bash
/smartspec_nfr_perf_verifier.md \
  <nfr_plan> \
  [--test-results <path>] \
  [--strict] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: การตรวจสอบ Performance NFRs (CLI)

**สถานการณ์:** ตรวจสอบประสิทธิภาพเทียบกับเป้าหมาย NFR

**คำสั่ง:**

```bash
/smartspec_nfr_perf_verifier specs/api/search_engine/nfr/performance.md \
  --test-results perf-results/load-test.json
```

**ผลลัพธ์ที่คาดหวัง:**

1. ตรวจสอบ NFR เทียบกับผลการทดสอบ  
2. สร้างรายงานการตรวจสอบ  
3. รหัสออก `0` หากผ่าน, `1` หากล้มเหลว

### กรณีการใช้งาน 2: การตรวจสอบแบบเข้มงวด (Kilo Code)

**สถานการณ์:** การตรวจสอบ NFR แบบเข้มงวดใน pipeline CI

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_nfr_perf_verifier.md \
  specs/system/load_balancer/nfr/scalability.md \
  --strict \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. ดำเนินการตรวจสอบแบบเข้มงวด  
2. รหัสออก `0` หากผ่านทั้งหมด, `1` หากล้มเหลวอย่างน้อยหนึ่งรายการ

### กรณีการใช้งาน 3: ผลลัพธ์แบบ JSON (CLI)

**สถานการณ์:** ผลลัพธ์การตรวจสอบในรูปแบบโครงสร้าง JSON

**คำสั่ง:**

```bash
/smartspec_nfr_perf_verifier specs/services/payment/nfr/reliability.md \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. การตรวจสอบพร้อมผลลัพธ์ในรูปแบบ JSON  
2. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `<nfr_plan>` | `<path>` | เส้นทางไปยังไฟล์แผน NFR | ต้องมีอยู่จริง |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--platform` | แพลตฟอร์มที่ใช้รัน **จำเป็นสำหรับ Kilo Code** | (อนุมานค่า) | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--test-results` | เส้นทางไปยังไฟล์ผลการทดสอบประสิทธิภาพ | (ตรวจจับอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--strict` | เปิดโหมดตรวจสอบแบบเข้มงวด | `false` | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/nfr-perf-verifier/<run-id>/verification.md` | รายงานการตรวจสอบ |

---

## 6. หมายเหตุ

- **Platform Flag:** เมื่อใช้ Kilo Code ต้องระบุ `--platform kilo` เสมอ

---

**จบคู่มือ**