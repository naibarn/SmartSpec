| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_optimize_ui_catalog คู่มือ (TH) | 6.0 | /smartspec_optimize_ui_catalog | 6.0.x |

# /smartspec_optimize_ui_catalog คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

คู่มือนี้อธิบายวิธีการใช้งาน workflow:

`/smartspec_optimize_ui_catalog` workflow เพิ่มประสิทธิภาพของ UI component catalog ผ่านกลไก caching และ indexing ทำให้การค้นหา component เร็วขึ้น 10-100 เท่าสำหรับ catalog ขนาดใหญ่

**วัตถุประสงค์:** ปรับปรุงประสิทธิภาพของ UI catalog ด้วย intelligent caching, search indexing และ dependency graph optimization เพื่อการค้นหา component ที่รวดเร็วและลดการใช้หน่วยความจำ

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** ui_optimization_and_analytics

---

## 2. การใช้งาน

workflow นี้รองรับการเรียกใช้งานผ่าน Command Line Interface (CLI) และ Kilo Code (สภาพแวดล้อมสคริปต์ภายใน)

### 🔗 การใช้งาน CLI

การเรียกใช้งาน CLI จำเป็นต้องระบุไฟล์ catalog และตำแหน่ง cache

```bash
/smartspec_optimize_ui_catalog \
  --catalog <path/to/ui-catalog.json> \
  --cache <path/to/cache.json> \
  [--rebuild-index] \
  [--invalidate-cache] \
  [--verify]
```

### การใช้งาน Kilo Code

การเรียกใช้งาน Kilo Code มีโครงสร้างเหมือนกับ CLI โดยมักใช้ใน pipeline อัตโนมัติหรือสคริปต์ภายใน

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องระบุ flag `--platform kilo` ด้วยเสมอ

```bash
/smartspec_optimize_ui_catalog.md \
  --catalog <path/to/ui-catalog.json> \
  --cache <path/to/cache.json> \
  [--rebuild-index] \
  [--invalidate-cache] \
  [--verify] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: เพิ่มประสิทธิภาพ Catalog ครั้งแรก (CLI)

**สถานการณ์:** โปรเจกต์มี UI catalog ขนาดใหญ่ที่มี component มากกว่า 50 ตัว และการค้นหาเริ่มช้าลง

**คำสั่ง:**

```bash
/smartspec_optimize_ui_catalog \
  --catalog .spec/ui-catalog.json \
  --cache .spec/cache/ui-catalog-cache.json \
  --rebuild-index
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow วิเคราะห์โครงสร้างของ catalog
2. สร้าง cache ที่เพิ่มประสิทธิภาพพร้อม metadata ของ component
3. สร้าง search indexes เพื่อการค้นหาที่รวดเร็ว
4. สร้าง dependency graph สำหรับ query ความสัมพันธ์
5. เขียน cache ไปยัง `.spec/cache/ui-catalog-cache.json`
6. รายงานการปรับปรุงประสิทธิภาพ (เร็วขึ้น 10-100 เท่า)
7. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: อัปเดต Cache หลังแก้ไข Catalog (Kilo Code)

**สถานการณ์:** UI catalog ได้รับการอัปเดตด้วย component ใหม่และ cache ต้องการการรีเฟรช

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_optimize_ui_catalog.md \
  --catalog .spec/ui-catalog.json \
  --cache .spec/cache/ui-catalog-cache.json \
  --invalidate-cache \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow ทำให้ cache เดิมไม่ถูกต้อง
2. สร้าง cache ใหม่ด้วยข้อมูล catalog ที่อัปเดต
3. อัปเดต search indexes
4. รีเฟรช dependency graph
5. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: ตรวจสอบความถูกต้องของ Cache (CLI)

**สถานการณ์:** นักพัฒนาต้องการตรวจสอบว่า cache ถูกต้องและเป็นปัจจุบัน

**คำสั่ง:**

```bash
/smartspec_optimize_ui_catalog \
  --catalog .spec/ui-catalog.json \
  --cache .spec/cache/ui-catalog-cache.json \
  --verify \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow เปรียบเทียบ cache กับ catalog
2. ตรวจสอบความถูกต้องและความสดของ cache
3. รายงานความแตกต่างหรือปัญหาใด ๆ
4. ผลลัพธ์รวมไฟล์ `verification.json` พร้อมผลลัพธ์
5. รหัสออก `0` ถ้าถูกต้อง, `1` ถ้าพบปัญหา

---

## 4. พารามิเตอร์

พารามิเตอร์และ flags ต่อไปนี้ใช้ควบคุมการทำงานและพฤติกรรมของ `/smartspec_optimize_ui_catalog` workflow

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `--catalog` | `<string>` | เส้นทางไปยังไฟล์ UI catalog JSON | ต้องมีอยู่จริงและเป็น JSON ที่ถูกต้อง |
| `--cache` | `<string>` | เส้นทางไปยังไฟล์ cache (จะถูกสร้างถ้ายังไม่มี) | ต้องเป็นตำแหน่งที่เขียนได้ |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--config` | เส้นทางไปยังไฟล์กำหนดค่า SmartSpec | `.spec/smartspec.config.yaml` | `cli` \| `kilo` \| `ci` \| `other` |
| `--lang` | ภาษาสำหรับการสร้างรายงาน (เช่น `th`, `en`) | (ค่าเริ่มต้นของระบบ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--platform` | บริบทแพลตฟอร์มการทำงาน **จำเป็นสำหรับ Kilo Code** | (อนุมานอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--out` | เส้นทางฐานสำหรับผลลัพธ์ที่ปลอดภัย | `.spec/reports/ui-optimization/` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--quiet` | ปิดการแสดงผลลัพธ์ล็อกปกติ | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Platform Support |
| :--- | :--- | :--- |
| `--rebuild-index` | บังคับสร้าง search indexes ใหม่ | `cli` \| `kilo` \| `ci` \| `other` |
| `--invalidate-cache` | ทำให้ cache ไม่ถูกต้องและสร้างใหม่ | `cli` \| `kilo` \| `ci` \| `other` |
| `--verify` | ตรวจสอบความถูกต้องของ cache โดยไม่แก้ไข | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

workflow สร้างผลลัพธ์ตามการตั้งค่าของมัน

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `<cache-path>` | ไฟล์ cache ที่เพิ่มประสิทธิภาพพร้อม metadata และ indexes ของ component |
| `.spec/reports/ui-optimization/<run-id>/optimization-report.md` | รายงานการเพิ่มประสิทธิภาพ |
| `.spec/reports/ui-optimization/<run-id>/summary.json` | สรุป JSON ของผลลัพธ์การเพิ่มประสิทธิภาพ |

### โครงสร้าง Cache

ไฟล์ cache ประกอบด้วย:
- **Component metadata:** ข้อมูล component สำหรับการเข้าถึงเร็ว
- **Search indexes:** indexes สำหรับ ID, name, tag, category
- **Dependency graph:** การแมปความสัมพันธ์ของ component
- **Performance metrics:** อัตราการ hit ของ cache และเวลาการค้นหา

---

## 6. การปรับปรุงประสิทธิภาพ

### ผลลัพธ์ประสิทธิภาพที่คาดหวัง

| ขนาด Catalog | ก่อนเพิ่มประสิทธิภาพ | หลังเพิ่มประสิทธิภาพ | การปรับปรุง |
| :--- | :--- | :--- | :--- |
| 10 components | 50ms | 5ms | เร็วขึ้น 10 เท่า |
| 50 components | 500ms | 10ms | เร็วขึ้น 50 เท่า |
| 100 components | 2000ms | 20ms | เร็วขึ้น 100 เท่า |

### คุณสมบัติการเพิ่มประสิทธิภาพ

- **Component Caching:** cache ในหน่วยความจำสำหรับ component ที่เข้าถึงบ่อย
- **Search Indexing:** การค้นหาเร็วด้วย ID, name, tags, category
- **Dependency Graph:** query ความสัมพันธ์ของ component ที่เพิ่มประสิทธิภาพ
- **Memory Efficiency:** ลดการใช้หน่วยความจำผ่าน smart caching

---

## 7. หมายเหตุ

- **Platform Flag:** เมื่อใช้ Kilo Code ต้องระบุ `--platform kilo` เสมอเพื่อให้บริบทและการบันทึกถูกต้อง
- **ตำแหน่ง Cache:** เก็บ cache ในไดเรกทอรี `.spec/cache/` (gitignore โดยค่าเริ่มต้น)
- **ความถี่การสร้างใหม่:** สร้าง cache ใหม่หลังการเปลี่ยนแปลง catalog ที่สำคัญ (10+ components)
- **การตรวจสอบ:** รัน `--verify` เป็นระยะเพื่อให้แน่ใจว่า cache ถูกต้อง
- **ประสิทธิภาพ:** มีประโยชน์มากที่สุดสำหรับ catalog ที่มี 50+ components
- **การตั้งค่า:** workflow ปฏิบัติตามการตั้งค่าใน `.spec/smartspec.config.yaml`

---

**จบคู่มือ**
