| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_reindex_specs คู่มือ (TH) | 6.0 | /smartspec_reindex_specs | 6.0.x |

# /smartspec_reindex_specs คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

คู่มือนี้อธิบายวิธีการใช้งาน workflow:

`/smartspec_reindex_specs` workflow ทำการสร้างใหม่ไฟล์ `.spec/SPEC_INDEX.json` โดยการสแกน specs ทั้งหมดภายใต้ `specs/**` และอัปเดตดัชนีด้วย metadata ปัจจุบัน

**วัตถุประสงค์:** สร้างดัชนี spec ใหม่โดยการสแกน specification ทั้งหมดและอัปเดต metadata เพื่อให้ดัชนีสอดคล้องกับไฟล์ spec จริง

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** การจัดการดัชนี

---

## 2. การใช้งาน

workflow นี้รองรับการเรียกใช้งานผ่าน Command Line Interface (CLI) และ Kilo Code (สภาพแวดล้อมสคริปต์ภายใน)

### 🔗 การใช้งาน CLI

```bash
/smartspec_reindex_specs \
  [--apply] \
  [--validate]
```

### การใช้งาน Kilo Code

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องระบุ flag `--platform kilo` เสมอ

```bash
/smartspec_reindex_specs.md \
  [--apply] \
  [--validate] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: การสร้างดัชนี Spec ใหม่ (CLI)

**สถานการณ์:** นักพัฒนาเพิ่ม specs ใหม่หลายรายการและต้องการสร้างดัชนีใหม่

**คำสั่ง:**

```bash
/smartspec_reindex_specs --apply
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow สแกน specs ทั้งหมดภายใต้ `specs/**`  
2. ดึง metadata จากแต่ละ spec  
3. สร้างไฟล์ `.spec/SPEC_INDEX.json` ใหม่ด้วยข้อมูลปัจจุบัน  
4. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: การตรวจสอบดัชนีโดยไม่เปลี่ยนแปลง (Kilo Code)

**สถานการณ์:** pipeline CI ต้องการตรวจสอบว่าดัชนีเป็นปัจจุบันโดยไม่แก้ไขใดๆ

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_reindex_specs.md \
  --validate \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow สแกน specs ทั้งหมดและเปรียบเทียบกับดัชนี  
2. รายงานความแตกต่างที่พบ  
3. ไม่มีการเปลี่ยนแปลงใดๆ กับดัชนี  
4. รหัสออก `0` หากสอดคล้องกัน, `1` หากไม่สอดคล้อง

### กรณีการใช้งาน 3: ดูตัวอย่างการสร้างดัชนีใหม่ (CLI)

**สถานการณ์:** ผู้ใช้ต้องการดูว่าการเปลี่ยนแปลงใดจะเกิดขึ้นกับดัชนีก่อนที่จะนำไปใช้จริง

**คำสั่ง:**

```bash
/smartspec_reindex_specs --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow สร้างตัวอย่างการเปลี่ยนแปลงดัชนี  
2. ผลลัพธ์ประกอบด้วยไฟล์ `summary.json` ที่แสดงการเปลี่ยนแปลงที่เสนอ  
3. ไม่มีการเปลี่ยนแปลงใดถูกนำไปใช้จริง  
4. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

### Flags ทั่วไป

| Flag | คำอธิบาย | ค่าเริ่มต้น | รองรับแพลตฟอร์ม |
| :--- | :--- | :--- | :--- |
| `--config` | เส้นทางไปยังไฟล์การตั้งค่า SmartSpec | `.spec/smartspec.config.yaml` | `cli` \| `kilo` \| `ci` \| `other` |
| `--lang` | ภาษาในการสร้างรายงาน | (ค่าเริ่มต้นของระบบ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--platform` | บริบทแพลตฟอร์มที่ใช้งาน **จำเป็นสำหรับ Kilo Code** | (อนุมานอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--apply` | เปิดใช้งานการเขียนที่ควบคุม (อัปเดต SPEC_INDEX.json) | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--out` | เส้นทางฐานสำหรับผลลัพธ์ที่ปลอดภัย | `.spec/reports/reindex-specs/` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--quiet` | ปิดการแสดงผลล็อกมาตรฐาน | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะของ Workflow

| Flag | คำอธิบาย | ค่าเริ่มต้น | รองรับแพลตฟอร์ม |
| :--- | :--- | :--- | :--- |
| `--validate` | ตรวจสอบความสอดคล้องของดัชนีโดยไม่เปลี่ยนแปลง | `false` | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

### ไฟล์ผลลัพธ์

| File Path | คำอธิบาย |
| :--- | :--- |
| `.spec/reports/reindex-specs/<run-id>/...` | รายงานและล็อกการสร้างดัชนีใหม่ |
| `.spec/SPEC_INDEX.json` | ดัชนี spec ที่อัปเดต (เฉพาะเมื่อใช้ `--apply`) |

---

## 6. หมายเหตุ

- **Flag แพลตฟอร์ม:** เมื่อใช้ Kilo Code ต้องระบุ `--platform kilo` เสมอ  
- **โหมดตรวจสอบ:** ใช้ `--validate` เพื่อตรวจสอบความสอดคล้องโดยไม่เปลี่ยนแปลง  
- **การเขียนที่ควบคุม:** ใช้ `--apply` เพื่ออัปเดตดัชนีจริง  
- **การตั้งค่า:** workflow จะอ่านค่าการตั้งค่าจาก `.spec/smartspec.config.yaml`

---

**จบคู่มือ**