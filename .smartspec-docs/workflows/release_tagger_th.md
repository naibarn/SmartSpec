| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_release_tagger คู่มือ (TH) | 6.0 | /smartspec_release_tagger | 6.0.x |

# /smartspec_release_tagger คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

workflow `/smartspec_release_tagger` จัดการการติดแท็ก release และการจัดการเวอร์ชัน

**วัตถุประสงค์:** จัดการการติดแท็ก release และการจัดการเวอร์ชัน โดยการสร้าง release tag และอัปเดต metadata ของเวอร์ชัน

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** operations-deployment

---

## 2. การใช้งาน

### 🔗 การใช้งาน CLI

```bash
/smartspec_release_tagger \
  --version <semver> \
  [--release-notes <path>] \
  [--apply]
```

### การใช้งาน Kilo Code

**หมายเหตุ:** เมื่อใช้ Kilo Code จำเป็นต้องใส่ flag `--platform kilo` เสมอ

```bash
/smartspec_release_tagger.md \
  --version <semver> \
  [--release-notes <path>] \
  [--apply] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: การสร้าง Release Tag (CLI)

**สถานการณ์:** สร้าง release tag สำหรับเวอร์ชันใหม่

**คำสั่ง:**

```bash
/smartspec_release_tagger \
  --version 2.1.0 \
  --release-notes RELEASE_NOTES.md \
  --apply
```

**ผลลัพธ์ที่คาดหวัง:**

1. สร้าง release tag เรียบร้อยแล้ว  
2. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: การปล่อยอัตโนมัติ (Kilo Code)

**สถานการณ์:** pipeline CI สร้าง release อัตโนมัติ

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_release_tagger.md \
  --version 3.0.0 \
  --apply \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. สร้าง release tag อัตโนมัติ  
2. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: การพรีวิว Release (CLI)

**สถานการณ์:** พรีวิว release โดยไม่สร้าง tag

**คำสั่ง:**

```bash
/smartspec_release_tagger \
  --version 1.5.2 \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. แสดงพรีวิว release ในรูปแบบ JSON  
2. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `--version` | `<semver>` | เวอร์ชันแบบ semantic สำหรับ release | ต้องเป็นรูปแบบ semver ที่ถูกต้อง |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--platform` | แพลตฟอร์มที่ใช้รันคำสั่ง **จำเป็นสำหรับ Kilo Code** | (อนุมานค่า) | `cli` \| `kilo` \| `ci` \| `other` |
| `--apply` | สร้าง release tag | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | แสดงผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--release-notes` | ที่อยู่ไฟล์ release notes | (สร้างอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/release-tagger/<run-id>/release_info.md` | ข้อมูล release |

---

## 6. หมายเหตุ

- **Flag แพลตฟอร์ม:** เมื่อใช้ Kilo Code ต้องใส่ `--platform kilo` เสมอ

---

**จบคู่มือ**