| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_report_implement_prompter คู่มือ (TH) | 6.0 | /smartspec_report_implement_prompter | 6.0.x |

# /smartspec_report_implement_prompter คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

คู่มือนี้อธิบายวิธีการใช้งาน workflow:

workflow `/smartspec_report_implement_prompter` จะสร้าง implementation prompts และรายงานจากงานที่ได้รับการตรวจสอบแล้ว ช่วยให้นักพัฒนาทราบว่าสิ่งใดที่ต้องดำเนินการต่อไป

**วัตถุประสงค์:** สร้าง implementation prompts และรายงานจากงานที่ได้รับการตรวจสอบแล้ว เพื่อแนะนำให้นักพัฒนาทำงานตามขั้นตอนด้วยคำแนะนำที่ชัดเจนและสามารถปฏิบัติได้

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** implementation

---

## 2. การใช้งาน

workflow นี้รองรับการเรียกใช้งานผ่าน Command Line Interface (CLI) และ Kilo Code (สภาพแวดล้อมสคริปต์ภายใน)

### 🔗 การใช้งาน CLI

การเรียกใช้งาน CLI ต้องระบุไฟล์ tasks.md ที่ต้องการ

```bash
/smartspec_report_implement_prompter \
  <tasks_md> \
  [--format <markdown|json>] \
  [--filter <pending|all>]
```

### การใช้งาน Kilo Code

การเรียกใช้งาน Kilo Code มีโครงสร้างเหมือนกับ CLI โดยปกติใช้ใน pipeline อัตโนมัติหรือสคริปต์ภายใน

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องใส่ flag `--platform kilo` ด้วยเสมอ

```bash
/smartspec_report_implement_prompter.md \
  <tasks_md> \
  [--format <markdown|json>] \
  [--filter <pending|all>] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: สร้าง Implementation Prompts สำหรับงานที่รอดำเนินการ (CLI)

**สถานการณ์:** นักพัฒนาต้องการดูว่างานใดที่ต้องดำเนินการต่อไปพร้อมคำแนะนำอย่างละเอียด

**คำสั่ง:**

```bash
/smartspec_report_implement_prompter specs/auth/login_service/tasks.md \
  --filter pending
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดไฟล์ `specs/auth/login_service/tasks.md`  
2. กรองงานที่ยังไม่เสร็จ (pending)  
3. สร้าง implementation prompts สำหรับงานที่รอดำเนินการแต่ละงาน  
4. เขียนรายงานลงในโฟลเดอร์ `.spec/reports/implement-prompter/<run-id>/`  
5. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: สร้างรายงาน JSON สำหรับ CI Pipeline (Kilo Code)

**สถานการณ์:** pipeline CI ต้องการข้อมูล implementation ในรูปแบบโครงสร้างสำหรับแสดงบนแดชบอร์ด

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_report_implement_prompter.md \
  specs/payments/checkout/tasks.md \
  --format json \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดไฟล์ tasks  
2. สร้าง implementation prompts ในรูปแบบ JSON  
3. ผลลัพธ์ประกอบด้วยไฟล์ `prompts.json` ที่มีข้อมูลโครงสร้าง  
4. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: รายงานครบถ้วนสำหรับงานทั้งหมด (CLI)

**สถานการณ์:** ผู้จัดการโครงการต้องการรายงานงานทั้งหมด (ทั้งที่รอดำเนินการและเสร็จแล้ว) พร้อมรายละเอียด implementation

**คำสั่ง:**

```bash
/smartspec_report_implement_prompter specs/dashboard/analytics/tasks.md \
  --filter all \
  --format markdown
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดงานทั้งหมดจากไฟล์  
2. สร้าง implementation prompts สำหรับงานทั้งหมด  
3. สร้างรายงาน markdown ครบถ้วน  
4. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

พารามิเตอร์และ flags ต่อไปนี้ควบคุมการทำงานและพฤติกรรมของ workflow `/smartspec_report_implement_prompter`

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `<tasks_md>` | `<path>` | เส้นทางไปยังไฟล์ tasks.md | ต้องอยู่ภายใต้ `specs/**` |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--config` | เส้นทางไปยังไฟล์ตั้งค่า SmartSpec | `.spec/smartspec.config.yaml` | `cli` \| `kilo` \| `ci` \| `other` |
| `--lang` | ภาษาในการสร้างรายงาน (เช่น `th`, `en`) | (ค่าเริ่มต้นของระบบ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--platform` | บริบทแพลตฟอร์มที่ใช้รัน **จำเป็นสำหรับ Kilo Code** | (อนุมานค่าอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--out` | เส้นทางฐานสำหรับผลลัพธ์ที่ปลอดภัย | `.spec/reports/implement-prompter/` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--quiet` | ปิดการแสดงผลล็อกมาตรฐาน | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะของ Workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--format` | รูปแบบผลลัพธ์: `markdown` หรือ `json` | `markdown` | `cli` \| `kilo` \| `ci` \| `other` |
| `--filter` | ตัวกรองงาน: `pending` หรือ `all` | `pending` | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

workflow จะสร้างผลลัพธ์ตามการตั้งค่าที่กำหนด

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/implement-prompter/<run-id>/prompts.md` | implementation prompts ในรูปแบบ markdown |
| `.spec/reports/implement-prompter/<run-id>/prompts.json` | implementation prompts ในรูปแบบ JSON (ถ้าใช้ `--format json`) |

---

## 6. หมายเหตุ

- **Flag แพลตฟอร์ม:** เมื่อใช้ Kilo Code ต้องใส่ `--platform kilo` เสมอเพื่อให้บริบทและการบันทึกถูกต้อง  
- **การกรองงาน:** ใช้ `--filter pending` เพื่อโฟกัสเฉพาะงานที่ยังไม่เสร็จ  
- **ตัวเลือกฟอร์แมต:** เลือกได้ระหว่าง markdown สำหรับอ่านโดยมนุษย์ หรือ JSON สำหรับประมวลผลโดยโปรแกรม  
- **การตั้งค่า:** workflow จะเคารพการตั้งค่าใน `.spec/smartspec.config.yaml`

---

**จบคู่มือ**