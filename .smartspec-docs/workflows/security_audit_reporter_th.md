| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_security_audit_reporter คู่มือ (TH) | 6.0 | /smartspec_security_audit_reporter | 6.0.x |

# /smartspec_security_audit_reporter คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

workflow `/smartspec_security_audit_reporter` สร้างรายงานตรวจสอบความปลอดภัยอย่างครบถ้วนโดยอ้างอิงจาก threat models และการสแกนความปลอดภัย

**วัตถุประสงค์:** สร้างรายงานตรวจสอบความปลอดภัยอย่างครบถ้วนโดยอ้างอิงจาก threat models, การสแกนหาช่องโหว่ และการตรวจสอบความสอดคล้อง

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** security-audit

---

## 2. การใช้งาน

### 🔗 การใช้งาน CLI

```bash
/smartspec_security_audit_reporter \
  <spec_md> \
  [--include-recommendations] \
  [--compliance <standard>]
```

### การใช้งาน Kilo Code

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องใส่ flag `--platform kilo` เสมอ

```bash
/smartspec_security_audit_reporter.md \
  <spec_md> \
  [--include-recommendations] \
  [--compliance <standard>] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: การสร้างรายงานตรวจสอบความปลอดภัย (CLI)

**สถานการณ์:** สร้างรายงานตรวจสอบความปลอดภัยอย่างครบถ้วน

**คำสั่ง:**

```bash
/smartspec_security_audit_reporter specs/api/payment_gateway/spec.md \
  --include-recommendations
```

**ผลลัพธ์ที่คาดหวัง:**

1. ดำเนินการตรวจสอบความปลอดภัย  
2. สร้างรายงานพร้อมคำแนะนำ  
3. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: การตรวจสอบความสอดคล้อง (Kilo Code)

**สถานการณ์:** ตรวจสอบความสอดคล้องกับมาตรฐานความปลอดภัย

**คำสั่ง (Kilo Code Snippet):**

```bash
/smartspec_security_audit_reporter.md \
  specs/data/pii_storage/spec.md \
  --compliance GDPR \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. ดำเนินการตรวจสอบความสอดคล้อง  
2. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: ผลลัพธ์ JSON (CLI)

**สถานการณ์:** ข้อมูลตรวจสอบในรูปแบบโครงสร้าง JSON

**คำสั่ง:**

```bash
/smartspec_security_audit_reporter specs/auth/mfa/spec.md \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. การตรวจสอบพร้อมผลลัพธ์ในรูปแบบ JSON  
2. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

### พารามิเตอร์ที่ต้องระบุ

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `<spec_md>` | `<path>` | เส้นทางไปยังไฟล์ spec.md | ต้องอยู่ภายใต้ `specs/**` |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--platform` | แพลตฟอร์มที่ใช้รัน **จำเป็นสำหรับ Kilo Code** | (อนุมานค่า) | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--include-recommendations` | รวมคำแนะนำด้านความปลอดภัยในรายงาน | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--compliance` | ตรวจสอบความสอดคล้องกับมาตรฐาน (เช่น GDPR, HIPAA, PCI-DSS) | (ไม่มี) | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/security-audit-reporter/<run-id>/audit_report.md` | รายงานตรวจสอบความปลอดภัย |

---

## 6. หมายเหตุ

- **Platform Flag:** เมื่อใช้ Kilo Code ต้องใส่ `--platform kilo` เสมอ  
- **มาตรฐานความสอดคล้อง:** รองรับ GDPR, HIPAA, PCI-DSS และมาตรฐานอื่น ๆ

---

**สิ้นสุดคู่มือ**