| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_security_threat_modeler คู่มือ (TH) | 6.0 | /smartspec_security_threat_modeler | 6.0.x |

# /smartspec_security_threat_modeler คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

workflow `/smartspec_security_threat_modeler` ทำการวิเคราะห์สเปคเพื่อระบุภัยคุกคามด้านความปลอดภัยและสร้าง threat models

**วัตถุประสงค์:** วิเคราะห์สเปคเพื่อระบุภัยคุกคามด้านความปลอดภัย ช่องโหว่ และสร้าง threat models อย่างครบถ้วน

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** security-audit

---

## 2. การใช้งาน

### 🔗 การใช้งาน CLI

```bash
/smartspec_security_threat_modeler \
  <spec_md> \
  [--threat-level <low|medium|high>] \
  [--apply]
```

### การใช้งาน Kilo Code

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องใส่ flag `--platform kilo` เสมอ

```bash
/smartspec_security_threat_modeler.md \
  <spec_md> \
  [--threat-level <low|medium|high>] \
  [--apply] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: การสร้าง Threat Model (CLI)

**สถานการณ์:** นักวิเคราะห์ความปลอดภัยสร้าง threat model สำหรับ API ใหม่

**คำสั่ง:**

```bash
/smartspec_security_threat_modeler specs/api/payment_gateway/spec.md \
  --threat-level high
```

**ผลลัพธ์ที่คาดหวัง:**

1. สเปคถูกวิเคราะห์เพื่อหาภัยคุกคามด้านความปลอดภัย  
2. สร้าง threat model เรียบร้อย  
3. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: การตรวจสอบความปลอดภัยใน CI Pipeline (Kilo Code)

**สถานการณ์:** การสร้าง threat model อัตโนมัติใน CI

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_security_threat_modeler.md \
  specs/auth/oauth/spec.md \
  --apply \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. สร้าง threat model และบันทึกไฟล์  
2. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: ผลลัพธ์ JSON (CLI)

**สถานการณ์:** ต้องการข้อมูลภัยคุกคามในรูปแบบโครงสร้างสำหรับแดชบอร์ดความปลอดภัย

**คำสั่ง:**

```bash
/smartspec_security_threat_modeler specs/data/user_data/spec.md \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. สร้าง threat model พร้อมผลลัพธ์ในรูปแบบ JSON  
2. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `<spec_md>` | `<path>` | เส้นทางไปยังไฟล์ spec.md | ต้องอยู่ภายใต้ `specs/**` |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--config` | เส้นทางไปยังไฟล์ config | `.spec/smartspec.config.yaml` | `cli` \| `kilo` \| `ci` \| `other` |
| `--platform` | แพลตฟอร์มที่ใช้รัน **จำเป็นสำหรับ Kilo Code** | (อนุมานอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--apply` | บันทึก threat model ลงในโฟลเดอร์สเปค | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | แสดงผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--threat-level` | ระดับภัยคุกคามขั้นต่ำที่ต้องรายงาน: `low`, `medium`, `high` | `medium` | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/security-threat-modeler/<run-id>/threat_model.md` | threat model ที่สร้างขึ้น |
| `specs/**/security/threat_model.md` | threat model ที่บันทึกไว้ (เมื่อใช้ `--apply`) |

---

## 6. หมายเหตุ

- **Flag แพลตฟอร์ม:** เมื่อใช้ Kilo Code ต้องใส่ `--platform kilo` เสมอ  
- **ระดับภัยคุกคาม:** ปรับ `--threat-level` ตามความต้องการด้านความปลอดภัย

---

**จบคู่มือ**