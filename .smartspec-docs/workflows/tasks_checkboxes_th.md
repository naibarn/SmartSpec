# คู่มือการใช้งาน Workflow
## `/smartspec_verify_tasks_progress` และ `/smartspec_sync_tasks_checkboxes`
เวอร์ชันภาษาไทย

---
# 1. ภาพรวมของระบบ
Workflow ชุดนี้ถูกออกแบบมาเพื่อช่วยตรวจสอบความคืบหน้าการพัฒนาโดยใช้หลักการ **evidence-first** คือเชื่อเฉพาะหลักฐานจริงในโค้ด, เทส, เอกสาร และส่วนของระบบจริงเท่านั้น ไม่ใช่การติ๊ก checkbox ใน `tasks.md` ตามความรู้สึก

ประกอบด้วย 2 workflow:

### ✔ `/smartspec_verify_tasks_progress`
ตรวจหลักฐานจริงจากโค้ดและสร้างรายงานความคืบหน้าแบบแม่นยำ (JSON/Markdown)

### ✔ `/smartspec_sync_tasks_checkboxes`
ซิงก์ checkbox ใน `tasks.md` ให้ตรงกับหลักฐานจริง หากงานยังไม่พบหลักฐาน → จะถูกปลดเครื่องหมาย `[x]` ออกทันที

---
# 2. Workflow ที่ 1 — `/smartspec_verify_tasks_progress`
## 2.1 หน้าที่
- ตรวจสอบโค้ดจริง, route, services, tests, docs
- ใช้สคริปต์ evidence เสริมได้ เช่น `check_tasks.py`
- สร้างรายงาน JSON ที่มี verdict:
  - `complete`
  - `unsynced_complete`
  - `false_positive`
  - `partial`
  - `incomplete`

## 2.2 คำสั่งที่ใช้บ่อย
### ▶ ตัวอย่างการตรวจแบบ evidence-first
```bash
/smartspec_verify_tasks_progress \
  --spec specs/feature/spec-002-user-management/spec.md \
  --report-format=json --report=summary --dry-run \
  --evidence-script=scripts/check_tasks_evidence.py \
  > .spec/reports/verify-tasks-progress/spec-002.json
```

## 2.3 ผลลัพธ์
- ไฟล์ JSON ภายใต้ `.spec/reports/verify-tasks-progress/`
- ใช้เพื่ออัปเดต `tasks.md` ใน workflow ถัดไป

---
# 3. Workflow ที่ 2 — `/smartspec_sync_tasks_checkboxes`
## 3.1 หน้าที่
- อัปเดต checkbox ใน `tasks.md` ให้ตรงกับหลักฐานจริง
- ทำงานร่วมกับรายงานจาก Verify workflow

## 3.2 กฎการ Sync
| Verdict | Checkbox ที่ต้องเป็น |
|--------|------------------------|
| `complete` | `[x]` |
| `unsynced_complete` | `[x]` |
| `false_positive` | `[ ]` |
| `partial` | `[ ]` |
| `incomplete` | `[ ]` |

## 3.3 คำสั่งตัวอย่าง
### ▶ อัปเดต `tasks.md` ด้วยหลักฐานล่าสุด
```bash
/smartspec_sync_tasks_checkboxes \
  --tasks specs/feature/spec-002-user-management/tasks.md \
  --report .spec/reports/verify-tasks-progress/spec-002.json \
  --mode=safe
```

### ▶ ดูผลลัพธ์เฉย ๆ ไม่เขียนไฟล์
```bash
/smartspec_sync_tasks_checkboxes \
  --tasks specs/.../tasks.md \
  --report .spec/reports/...json \
  --dry-run
```

---
# 4. กระบวนการทำงานที่แนะนำ (Standard Flow)
## ขั้นตอนทำงานแบบครบ Loop เพื่อให้ข้อมูลตรง 100%

1. **พัฒนาโค้ดให้ตรงตาม tasks**
2. รัน
```bash
/smartspec_verify_tasks_progress --report-format=json
```
3. ตรวจสอบรายงานที่ได้
4. รัน
```bash
/smartspec_sync_tasks_checkboxes --mode=safe
```
5. เปิด `tasks.md` → จะเห็นว่า checkbox ถูกต้องตามหลักฐาน
6. วางแผนงานรอบถัดไปจากข้อมูลจริง ไม่คลาดเคลื่อน

---
# 5. หมายเหตุสำคัญ
- Workflow นี้ **ไม่แตะ SPEC_INDEX / registry / โค้ด / spec.md** ตามกฎ governance
- ปรับเฉพาะ checkbox ของ `tasks.md` เท่านั้น
- ใช้ evidence-first เพื่อป้องกันความเข้าใจผิด เช่น “คิดว่าทำเสร็จแล้ว แต่ในโค้ดไม่มีจริง”

---
# 6. ถ้าต้องการเพิ่มเติม
พร้อมช่วยสร้าง:
- ตัวอย่างรายงานแบบ JSON
- สคริปต์ evidence ตัวอย่าง (นอกจาก check_tasks.py)
- Flow อัตโนมัติ CI/CD (เช่น auto-sync ทุกครั้งที่ push)

แจ้งได้เลยครับ 🚀

