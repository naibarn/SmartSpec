| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_tasks_checkboxes คู่มือ (TH) | 6.0 | /smartspec_tasks_checkboxes | 6.0.x |

# /smartspec_tasks_checkboxes คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

คู่มือนี้อธิบายวิธีการใช้งาน workflow:

`/smartspec_tasks_checkboxes` workflow จะทำการซิงโครไนซ์ช่องทำเครื่องหมาย (checkbox) ของงานระหว่างไฟล์ tasks.md กับหลักฐานการดำเนินงาน เพื่อให้การติดตามสถานะการทำงานถูกต้องแม่นยำ

**วัตถุประสงค์:** ซิงโครไนซ์ช่องทำเครื่องหมายใน tasks.md ตามหลักฐานการดำเนินงานที่ได้รับการยืนยัน เพื่อรักษาสถานะการทำงานที่ถูกต้องแม่นยำทั่วทั้งโปรเจกต์

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** verify

---

## 2. การใช้งาน

workflow นี้รองรับการเรียกใช้งานผ่าน Command Line Interface (CLI) และ Kilo Code (สภาพแวดล้อมสคริปต์ภายใน)

### 🔗 การใช้งาน CLI

การเรียกใช้งาน CLI จำเป็นต้องระบุไฟล์ tasks.md เป้าหมาย

```bash
/smartspec_tasks_checkboxes \
  <tasks_md> \
  [--apply] \
  [--strict]
```

### การใช้งาน Kilo Code

การเรียกใช้งาน Kilo Code มีโครงสร้างเหมือนกับ CLI โดยปกติใช้ใน pipeline อัตโนมัติหรือสคริปต์ภายใน

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องระบุ flag `--platform kilo` ด้วยเสมอ

```bash
/smartspec_tasks_checkboxes.md \
  <tasks_md> \
  [--apply] \
  [--strict] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: ซิงโครไนซ์ช่องทำเครื่องหมายตามหลักฐาน (CLI)

**สถานการณ์:** นักพัฒนาได้ทำงานหลายรายการเสร็จแล้ว ต้องการอัปเดตช่องทำเครื่องหมายตามหลักฐานที่ได้รับการยืนยัน

**คำสั่ง:**

```bash
/smartspec_tasks_checkboxes specs/auth/login_service/tasks.md \
  --apply
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดไฟล์ `specs/auth/login_service/tasks.md`  
2. สแกนหาหลักฐานการดำเนินงานสำหรับแต่ละงาน  
3. อัปเดตช่องทำเครื่องหมายตามหลักฐานที่ได้รับการยืนยัน  
4. ด้วย `--apply` จะเขียนไฟล์ tasks.md ที่อัปเดตแล้ว  
5. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: โหมดตรวจสอบเข้มงวด (Kilo Code)

**สถานการณ์:** pipeline CI ต้องการให้แน่ใจว่าช่องทำเครื่องหมายทั้งหมดมีหลักฐานที่สามารถแยกวิเคราะห์ได้

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_tasks_checkboxes.md \
  specs/payments/checkout/tasks.md \
  --strict \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดไฟล์ tasks  
2. ตรวจสอบหลักฐานสำหรับงานที่ถูกทำเครื่องหมายทั้งหมดในโหมดเข้มงวด  
3. งานที่ถูกทำเครื่องหมายแต่ไม่มีหลักฐานที่ถูกต้องจะถูกแจ้งเตือน  
4. สร้างรายงานผลการตรวจสอบ  
5. รหัสออก `0` หากผ่านทั้งหมด, `1` หากพบข้อผิดพลาด

### กรณีการใช้งาน 3: โหมดพรีวิวโดยไม่เปลี่ยนแปลง (CLI)

**สถานการณ์:** หัวหน้าทีมต้องการดูว่าช่องทำเครื่องหมายจะเปลี่ยนแปลงอย่างไรบ้างก่อนที่จะนำไปใช้จริง

**คำสั่ง:**

```bash
/smartspec_tasks_checkboxes specs/dashboard/analytics/tasks.md
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดไฟล์ tasks  
2. วิเคราะห์หลักฐานและกำหนดการอัปเดตช่องทำเครื่องหมาย  
3. สร้างรายงานพรีวิวแสดงการเปลี่ยนแปลงที่เสนอ  
4. ไม่มีการเปลี่ยนแปลงจริง (ไม่มี flag `--apply`)  
5. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

พารามิเตอร์และ flags ต่อไปนี้ควบคุมการทำงานและพฤติกรรมของ `/smartspec_tasks_checkboxes` workflow

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `<tasks_md>` | `<path>` | เส้นทางไปยังไฟล์ tasks.md | ต้องอยู่ภายใต้ `specs/**` |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--config` | เส้นทางไปยังไฟล์กำหนดค่า SmartSpec | `.spec/smartspec.config.yaml` | `cli` \| `kilo` \| `ci` \| `other` |
| `--lang` | ภาษาสำหรับการสร้างรายงาน (เช่น `th`, `en`) | (ค่าเริ่มต้นของระบบ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--platform` | บริบทของแพลตฟอร์มที่รัน **จำเป็นสำหรับ Kilo Code** | (อนุมานค่า) | `cli` \| `kilo` \| `ci` \| `other` |
| `--apply` | เปิดใช้งานการเขียนที่ควบคุม (อัปเดตช่องทำเครื่องหมายใน tasks.md) | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--out` | เส้นทางฐานสำหรับผลลัพธ์ที่ปลอดภัย | `.spec/reports/tasks-checkboxes/` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--quiet` | ปิดการแสดงผลลัพธ์ใน stdout | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--strict` | เปิดใช้งานโหมดตรวจสอบหลักฐานเข้มงวด | `false` | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

workflow จะสร้างผลลัพธ์ตามการตั้งค่าที่กำหนด

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/tasks-checkboxes/<run-id>/sync_report.md` | รายงานการซิงโครไนซ์ช่องทำเครื่องหมาย |
| `specs/**/tasks.md` | ไฟล์ tasks ที่อัปเดตช่องทำเครื่องหมาย (เฉพาะเมื่อใช้ `--apply`) |

---

## 6. หมายเหตุ

- **Platform Flag:** เมื่อใช้ Kilo Code ต้องระบุ `--platform kilo` เสมอเพื่อให้บริบทและการบันทึกถูกต้อง  
- **โหมดพรีวิว:** หากไม่ใช้ `--apply` workflow จะสร้างพรีวิวที่ปลอดภัยโดยไม่แก้ไขไฟล์ tasks.md  
- **การเขียนที่ควบคุม:** ใช้ flag `--apply` เพื่อเปิดใช้งานการอัปเดตช่องทำเครื่องหมาย  
- **โหมดเข้มงวด:** ใช้ flag `--strict` เพื่อบังคับให้ตรวจสอบหลักฐานสำหรับงานที่ถูกทำเครื่องหมายทั้งหมด  
- **การตั้งค่า:** workflow จะเคารพการตั้งค่าใน `.spec/smartspec.config.yaml`

---

**จบคู่มือ**