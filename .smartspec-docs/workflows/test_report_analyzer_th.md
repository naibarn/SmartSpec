| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_test_report_analyzer คู่มือ (TH) | 6.0 | /smartspec_test_report_analyzer | 6.0.x |

# /smartspec_test_report_analyzer คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

workflow `/smartspec_test_report_analyzer` ทำการวิเคราะห์รายงานการรันเทสต์และให้ข้อมูลเชิงลึกเกี่ยวกับการครอบคลุมการทดสอบและคุณภาพของการทดสอบ

**วัตถุประสงค์:** วิเคราะห์รายงานการรันเทสต์ โดยให้ข้อมูลเชิงลึกเกี่ยวกับการครอบคลุม, ความล้มเหลว และตัวชี้วัดคุณภาพ

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** quality-testing

---

## 2. การใช้งาน

### 🔗 การใช้งาน CLI

```bash
/smartspec_test_report_analyzer \
  <test_report> \
  [--format <junit|tap|json>]
```

### การใช้งาน Kilo Code

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code จำเป็นต้องใส่ flag `--platform kilo` เสมอ

```bash
/smartspec_test_report_analyzer.md \
  <test_report> \
  [--format <junit|tap|json>] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: วิเคราะห์รายงาน JUnit (CLI)

**สถานการณ์:** วิเคราะห์ผลลัพธ์การทดสอบจากรายงาน JUnit

**คำสั่ง:**

```bash
/smartspec_test_report_analyzer test-results/junit.xml \
  --format junit
```

**ผลลัพธ์ที่คาดหวัง:**

1. รายงานถูกวิเคราะห์แล้ว  
2. ข้อมูลเชิงลึกถูกสร้างขึ้น  
3. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: วิเคราะห์ CI Pipeline (Kilo Code)

**สถานการณ์:** pipeline ของ CI ทำการวิเคราะห์ผลลัพธ์การทดสอบ

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_test_report_analyzer.md \
  test-results/report.json \
  --format json \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. ทำการวิเคราะห์เรียบร้อย  
2. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: ผลลัพธ์ JSON (CLI)

**สถานการณ์:** ผลลัพธ์ในรูปแบบโครงสร้างสำหรับแดชบอร์ด

**คำสั่ง:**

```bash
/smartspec_test_report_analyzer test-results/tap.txt \
  --format tap \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. วิเคราะห์พร้อมผลลัพธ์ในรูปแบบ JSON  
2. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `<test_report>` | `<path>` | เส้นทางไปยังไฟล์รายงานการทดสอบ | ต้องมีอยู่จริง |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--platform` | แพลตฟอร์มที่ใช้รัน **จำเป็นสำหรับ Kilo Code** | (ตรวจจับอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--format` | รูปแบบรายงาน: `junit`, `tap` หรือ `json` | (ตรวจจับอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/test-report-analyzer/<run-id>/analysis.md` | รายงานการวิเคราะห์ |

---

## 6. หมายเหตุ

- **Flag แพลตฟอร์ม:** เมื่อใช้ Kilo Code ต้องใส่ `--platform kilo` เสมอ

---

**สิ้นสุดคู่มือ**