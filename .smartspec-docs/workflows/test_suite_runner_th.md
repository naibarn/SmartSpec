| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_test_suite_runner คู่มือ (TH) | 6.0 | /smartspec_test_suite_runner | 6.0.x |

# /smartspec_test_suite_runner คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

workflow `/smartspec_test_suite_runner` ทำการรันชุดทดสอบตามแผนทดสอบและสเปคที่กำหนด

**วัตถุประสงค์:** รันชุดทดสอบตามแผนทดสอบ ดำเนินการทดสอบและเก็บรวบรวมผลลัพธ์

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** การทดสอบคุณภาพ

---

## 2. การใช้งาน

### 🔗 การใช้งาน CLI

```bash
/smartspec_test_suite_runner \
  <test_plan> \
  [--suite <name>] \
  [--parallel]
```

### การใช้งาน Kilo Code

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องระบุ flag `--platform kilo` เสมอ

```bash
/smartspec_test_suite_runner.md \
  <test_plan> \
  [--suite <name>] \
  [--parallel] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: รันชุดทดสอบทั้งหมด (CLI)

**สถานการณ์:** ดำเนินการทดสอบทั้งหมดในแผนทดสอบ

**คำสั่ง:**

```bash
/smartspec_test_suite_runner specs/auth/testplan/tests.md
```

**ผลลัพธ์ที่คาดหวัง:**

1. ดำเนินการทดสอบทั้งหมด
2. เก็บรวบรวมผลลัพธ์
3. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: รันแบบขนาน (Kilo Code)

**สถานการณ์:** pipeline CI รันทดสอบแบบขนาน

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_test_suite_runner.md \
  specs/api/testplan/tests.md \
  --parallel \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. ทดสอบรันแบบขนาน
2. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: รันชุดทดสอบเฉพาะ (CLI)

**สถานการณ์:** รันชุดทดสอบเฉพาะ

**คำสั่ง:**

```bash
/smartspec_test_suite_runner specs/payments/testplan/tests.md \
  --suite integration
```

**ผลลัพธ์ที่คาดหวัง:**

1. ชุดทดสอบ integration ถูกดำเนินการ
2. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `<test_plan>` | `<path>` | เส้นทางไปยังไฟล์แผนทดสอบ | ต้องมีอยู่จริง |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--platform` | แพลตฟอร์มที่ใช้รัน **จำเป็นสำหรับ Kilo Code** | (อนุมานค่า) | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--suite` | ชุดทดสอบเฉพาะที่ต้องการรัน | (รันทุกชุด) | `cli` \| `kilo` \| `ci` \| `other` |
| `--parallel` | เปิดใช้งานการรันทดสอบแบบขนาน | `false` | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/test-suite-runner/<run-id>/results.json` | ผลลัพธ์การรันทดสอบ |

---

## 6. หมายเหตุ

- **Flag แพลตฟอร์ม:** เมื่อใช้ Kilo Code ต้องระบุ `--platform kilo` เสมอ

---

**จบคู่มือ**