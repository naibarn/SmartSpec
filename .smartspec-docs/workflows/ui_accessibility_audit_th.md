| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_ui_accessibility_audit คู่มือ (TH) | 6.0 | /smartspec_ui_accessibility_audit | 6.0.x |

# /smartspec_ui_accessibility_audit คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

คู่มือนี้อธิบายวิธีการใช้งาน workflow:

`/smartspec_ui_accessibility_audit` workflow ทำการตรวจสอบการปฏิบัติตามมาตรฐาน WCAG 2.1 accessibility อย่างครอบคลุมบน UI components เพื่อให้แน่ใจว่าการออกแบบมีความครอบคลุมและปฏิบัติตามกฎหมายด้าน accessibility

**วัตถุประสงค์:** ตรวจสอบ UI components สำหรับการปฏิบัติตาม WCAG 2.1 Level A/AA/AAA ระบุปัญหา accessibility และให้คำแนะนำที่สามารถดำเนินการได้สำหรับการแก้ไข

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** ui_optimization_and_analytics

---

## 2. การใช้งาน

workflow นี้รองรับการเรียกใช้งานผ่าน Command Line Interface (CLI) และ Kilo Code (สภาพแวดล้อมสคริปต์ภายใน)

### 🔗 การใช้งาน CLI

การเรียกใช้งาน CLI จำเป็นต้องระบุ UI spec, implementation และ compliance level

```bash
/smartspec_ui_accessibility_audit \
  --spec <path/to/ui-spec.json> \
  --implementation <path/to/implementation> \
  --level <A|AA|AAA> \
  [--auto-fix] \
  [--ignore-minor]
```

### การใช้งาน Kilo Code

การเรียกใช้งาน Kilo Code มีโครงสร้างเหมือนกับ CLI โดยมักใช้ใน pipeline อัตโนมัติหรือสคริปต์ภายใน

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องระบุ flag `--platform kilo` ด้วยเสมอ

```bash
/smartspec_ui_accessibility_audit.md \
  --spec <path/to/ui-spec.json> \
  --implementation <path/to/implementation> \
  --level <A|AA|AAA> \
  [--auto-fix] \
  [--ignore-minor] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: การตรวจสอบ WCAG AA Compliance (CLI)

**สถานการณ์:** นักพัฒนาต้องการตรวจสอบว่าฟอร์มติดต่อตรงตามมาตรฐาน WCAG AA accessibility ก่อน deployment

**คำสั่ง:**

```bash
/smartspec_ui_accessibility_audit \
  --spec specs/feature/spec-005-contact/ui-spec.json \
  --implementation src/ui/ContactForm.tsx \
  --level AA
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow วิเคราะห์ UI implementation
2. ตรวจสอบ semantic HTML, ARIA attributes, keyboard navigation
3. ตรวจสอบ color contrast, focus management, form labels
4. สร้างรายงาน accessibility โดยละเอียดพร้อมปัญหา
5. รายงานอัตราผ่าน (ต้องการ ≥80% เพื่อผ่าน)
6. รหัสออก `0` ถ้าผ่าน (≥80%), `1` ถ้าไม่ผ่าน (<80%)

### กรณีการใช้งาน 2: การตรวจสอบอัตโนมัติพร้อม Auto-Fix (Kilo Code)

**สถานการณ์:** CI pipeline รันการตรวจสอบ accessibility และแก้ไขปัญหาง่าย ๆ อัตโนมัติ

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_ui_accessibility_audit.md \
  --spec specs/feature/spec-005-contact/ui-spec.json \
  --implementation src/ui/ContactForm.tsx \
  --level AA \
  --auto-fix \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow ตรวจสอบ accessibility compliance
2. ระบุปัญหาที่แก้ไขได้ (alt text ที่ขาดหายไป, ARIA labels)
3. ด้วย `--auto-fix` ใช้การแก้ไขอัตโนมัติกับโค้ด
4. สร้างรายงานพร้อมปัญหาที่แก้ไขแล้วและที่เหลืออยู่
5. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: การตรวจสอบ AAA แบบครอบคลุม (CLI)

**สถานการณ์:** โปรเจกต์ของรัฐบาลต้องการการปฏิบัติตาม WCAG AAA accessibility สูงสุด

**คำสั่ง:**

```bash
/smartspec_ui_accessibility_audit \
  --spec specs/feature/spec-005-contact/ui-spec.json \
  --implementation src/ui/ContactForm.tsx \
  --level AAA \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow ทำการตรวจสอบ AAA แบบครอบคลุม
2. ตรวจสอบเกณฑ์ความสำเร็จ WCAG AAA ทั้งหมด
3. รายงานปัญหา critical, serious, moderate และ minor
4. ผลลัพธ์รวมไฟล์ `audit-report.json` พร้อมผลลัพธ์โดยละเอียด
5. รหัสออก `0` ถ้าผ่าน, `1` ถ้าไม่ผ่าน

---

## 4. พารามิเตอร์

พารามิเตอร์และ flags ต่อไปนี้ใช้ควบคุมการทำงานและพฤติกรรมของ `/smartspec_ui_accessibility_audit` workflow

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `--spec` | `<string>` | เส้นทางไปยังไฟล์ UI specification JSON | ต้องมีอยู่จริงและเป็น JSON ที่ถูกต้อง |
| `--implementation` | `<string>` | เส้นทางไปยังไฟล์หรือไดเรกทอรี implementation | ต้องมีอยู่จริงและเข้าถึงได้ |
| `--level` | `<string>` | ระดับ WCAG compliance: `A`, `AA` หรือ `AAA` | ต้องเป็นหนึ่งในค่าที่อนุญาต |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--config` | เส้นทางไปยังไฟล์กำหนดค่า SmartSpec | `.spec/smartspec.config.yaml` | `cli` \| `kilo` \| `ci` \| `other` |
| `--lang` | ภาษาสำหรับการสร้างรายงาน (เช่น `th`, `en`) | (ค่าเริ่มต้นของระบบ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--platform` | บริบทแพลตฟอร์มการทำงาน **จำเป็นสำหรับ Kilo Code** | (อนุมานอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--out` | เส้นทางฐานสำหรับผลลัพธ์ที่ปลอดภัย | `.spec/reports/ui-accessibility/` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--quiet` | ปิดการแสดงผลลัพธ์ล็อกปกติ | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Platform Support |
| :--- | :--- | :--- |
| `--auto-fix` | แก้ไขปัญหา accessibility ง่าย ๆ อัตโนมัติ | `cli` \| `kilo` \| `ci` \| `other` |
| `--ignore-minor` | ละเว้นปัญหาความรุนแรงเล็กน้อยในการคำนวณผ่าน/ไม่ผ่าน | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

workflow สร้างผลลัพธ์ตามการตั้งค่าของมัน

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/ui-accessibility/<run-id>/audit-report.md` | รายงานการตรวจสอบ accessibility โดยละเอียด |
| `.spec/reports/ui-accessibility/<run-id>/summary.json` | สรุป JSON ของผลลัพธ์การตรวจสอบ |
| `.spec/reports/ui-accessibility/<run-id>/issues.csv` | ส่งออก CSV ของปัญหาทั้งหมดสำหรับการติดตาม |

### โครงสร้างรายงาน

รายงานการตรวจสอบรวมถึง:
- **Summary:** ปัญหาทั้งหมด, อัตราผ่าน, สถานะ
- **Critical Issues:** อุปสรรคสำหรับ accessibility
- **Serious Issues:** ปัญหา accessibility ที่สำคัญ
- **Moderate Issues:** การปรับปรุงที่สำคัญที่จำเป็น
- **Minor Issues:** การปรับปรุงที่ดีมีไว้
- **Recommendations:** รายการการดำเนินการที่จัดลำดับความสำคัญ
- **WCAG Mapping:** ปัญหาที่แมปกับเกณฑ์ WCAG

---

## 6. การตรวจสอบ Accessibility

### พื้นที่ WCAG 2.1 Compliance

workflow ตรวจสอบพื้นที่ต่อไปนี้:

#### Perceivable (รับรู้ได้)
- **Text Alternatives:** Alt text สำหรับรูปภาพ
- **Time-based Media:** คำบรรยายและ transcripts
- **Adaptable:** โครงสร้าง semantic HTML
- **Distinguishable:** Color contrast, ขนาดข้อความ

#### Operable (ใช้งานได้)
- **Keyboard Accessible:** การนำทางด้วยคีย์บอร์ดเต็มรูปแบบ
- **Enough Time:** ไม่มีขอบเขตเวลาหรือปรับได้
- **Seizures:** ไม่มีเนื้อหากระพริบ
- **Navigable:** Skip links, ตัวบ่งชี้ focus

#### Understandable (เข้าใจได้)
- **Readable:** ภาษาที่ชัดเจน, ข้อความที่อ่านได้
- **Predictable:** การนำทางที่สม่ำเสมอ
- **Input Assistance:** Form labels, ข้อความแสดงข้อผิดพลาด

#### Robust (แข็งแรง)
- **Compatible:** HTML ที่ถูกต้อง, การใช้ ARIA
- **Parsing:** ไม่มีข้อผิดพลาด markup
- **Name, Role, Value:** การใช้งาน ARIA ที่เหมาะสม

---

## 7. เกณฑ์ผ่าน/ไม่ผ่าน

### เกณฑ์ผ่าน

- **ผ่าน:** ≥ 80% compliance
- **ไม่ผ่าน:** < 80% compliance

### น้ำหนักความรุนแรงของปัญหา

| ความรุนแรง | น้ำหนัก | ผลกระทบต่อคะแนน |
| :--- | :--- | :--- |
| Critical | 10 | หักคะแนนมาก |
| Serious | 5 | หักคะแนนอย่างมีนัยสำคัญ |
| Moderate | 2 | หักคะแนนปานกลาง |
| Minor | 1 | หักคะแนนเล็กน้อย |

---

## 8. หมายเหตุ

- **Platform Flag:** เมื่อใช้ Kilo Code ต้องระบุ `--platform kilo` เสมอเพื่อให้บริบทและการบันทึกถูกต้อง
- **ความปลอดภัยของ Auto-Fix:** Auto-fix ใช้เฉพาะกับการแก้ไขที่ง่ายและปลอดภัย (alt text, ARIA labels) ปัญหาที่ซับซ้อนต้องการการตรวจสอบด้วยตนเอง
- **การเลือก Level:** ใช้ AA สำหรับโปรเจกต์ส่วนใหญ่ (แนะนำ), AAA สำหรับรัฐบาล/สุขภาพ
- **การรวม CI:** รันการตรวจสอบใน CI pipeline เพื่อจับปัญหา accessibility ตั้งแต่เนิ่น ๆ
- **การทดสอบ Screen Reader:** การตรวจสอบอัตโนมัติเสริมแต่ไม่ทดแทนการทดสอบ screen reader ด้วยตนเอง
- **การตั้งค่า:** workflow ปฏิบัติตามการตั้งค่าใน `.spec/smartspec.config.yaml`

---

**จบคู่มือ**
