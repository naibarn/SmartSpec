| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_ui_analytics_reporter คู่มือ (TH) | 6.0 | /smartspec_ui_analytics_reporter | 6.0.x |

# /smartspec_ui_analytics_reporter คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

คู่มือนี้อธิบายวิธีการใช้งาน workflow:

`/smartspec_ui_analytics_reporter` workflow ติดตามการใช้งาน UI component, adoption metrics และ quality indicators ทั่วทั้งโปรเจกต์ ให้ข้อมูลเชิงลึกที่ขับเคลื่อนด้วยข้อมูลสำหรับกลยุทธ์การพัฒนา UI

**วัตถุประสงค์:** สร้างรายงาน analytics แบบครอบคลุมเกี่ยวกับการใช้งาน UI component, catalog adoption, quality metrics และแนวโน้มเพื่อแนะนำการตัดสินใจและการปรับปรุงการพัฒนา UI

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** ui_optimization_and_analytics

---

## 2. การใช้งาน

workflow นี้รองรับการเรียกใช้งานผ่าน Command Line Interface (CLI) และ Kilo Code (สภาพแวดล้อมสคริปต์ภายใน)

### 🔗 การใช้งาน CLI

การเรียกใช้งาน CLI จำเป็นต้องระบุ catalog และไดเรกทอรี implementation

```bash
/smartspec_ui_analytics_reporter \
  --catalog <path/to/ui-catalog.json> \
  --implementation <path/to/implementation-dir> \
  [--time-range <7d|30d|90d|all>] \
  [--compare-to <previous-report.json>]
```

### การใช้งาน Kilo Code

การเรียกใช้งาน Kilo Code มีโครงสร้างเหมือนกับ CLI โดยมักใช้ใน pipeline อัตโนมัติหรือสคริปต์ภายใน

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องระบุ flag `--platform kilo` ด้วยเสมอ

```bash
/smartspec_ui_analytics_reporter.md \
  --catalog <path/to/ui-catalog.json> \
  --implementation <path/to/implementation-dir> \
  [--time-range <7d|30d|90d|all>] \
  [--compare-to <previous-report.json>] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: รายงาน Analytics รายเดือน (CLI)

**สถานการณ์:** หัวหน้าทีมต้องการทบทวนการใช้งาน UI component และ quality metrics สำหรับเดือนที่ผ่านมา

**คำสั่ง:**

```bash
/smartspec_ui_analytics_reporter \
  --catalog .spec/ui-catalog.json \
  --implementation src/ui/ \
  --time-range 30d
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow วิเคราะห์การใช้งาน component ตลอด 30 วัน
2. คำนวณ adoption metrics (catalog coverage)
3. รวม quality indicators (accessibility, performance, tests)
4. ระบุ components ที่ใช้มากที่สุด/ไม่ได้ใช้
5. สร้างรายงาน analytics แบบครอบคลุม
6. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: การวิเคราะห์แนวโน้มพร้อมการเปรียบเทียบ (Kilo Code)

**สถานการณ์:** CI pipeline สร้างรายงาน analytics รายสัปดาห์และเปรียบเทียบกับสัปดาห์ก่อนหน้าเพื่อติดตามแนวโน้ม

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_ui_analytics_reporter.md \
  --catalog .spec/ui-catalog.json \
  --implementation src/ui/ \
  --time-range 7d \
  --compare-to .spec/reports/ui-analytics/2025-12-15/summary.json \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow สร้าง analytics สำหรับ 7 วันที่ผ่านมา
2. เปรียบเทียบ metrics กับรายงานสัปดาห์ก่อนหน้า
3. คำนวณเปอร์เซ็นต์การเปลี่ยนแปลงสำหรับ metrics ทั้งหมด
4. ระบุแนวโน้ม (ปรับปรุง, ลดลง, คงที่)
5. สร้างรายงานการวิเคราะห์แนวโน้ม
6. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: Analytics โปรเจกต์แบบครอบคลุม (CLI)

**สถานการณ์:** ผู้จัดการโปรเจกต์ต้องการภาพรวมที่สมบูรณ์ของ UI components ทั้งหมดตั้งแต่เริ่มโปรเจกต์

**คำสั่ง:**

```bash
/smartspec_ui_analytics_reporter \
  --catalog .spec/ui-catalog.json \
  --implementation src/ui/ \
  --time-range all \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow วิเคราะห์ข้อมูลประวัติศาสตร์ทั้งหมด
2. สร้างสถิติการใช้งานที่สมบูรณ์
3. รายงาน quality metrics ตลอดอายุโปรเจกต์
4. ระบุสุขภาพของ component ทั่วทั้งโปรเจกต์
5. ผลลัพธ์รวมไฟล์ `analytics-report.json` พร้อมข้อมูลเต็ม
6. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

พารามิเตอร์และ flags ต่อไปนี้ใช้ควบคุมการทำงานและพฤติกรรมของ `/smartspec_ui_analytics_reporter` workflow

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `--catalog` | `<string>` | เส้นทางไปยังไฟล์ UI catalog JSON | ต้องมีอยู่จริงและเป็น JSON ที่ถูกต้อง |
| `--implementation` | `<string>` | เส้นทางไปยังไดเรกทอรี implementation | ต้องมีอยู่จริงและเข้าถึงได้ |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--config` | เส้นทางไปยังไฟล์กำหนดค่า SmartSpec | `.spec/smartspec.config.yaml` | `cli` \| `kilo` \| `ci` \| `other` |
| `--lang` | ภาษาสำหรับการสร้างรายงาน (เช่น `th`, `en`) | (ค่าเริ่มต้นของระบบ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--platform` | บริบทแพลตฟอร์มการทำงาน **จำเป็นสำหรับ Kilo Code** | (อนุมานอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--out` | เส้นทางฐานสำหรับผลลัพธ์ที่ปลอดภัย | `.spec/reports/ui-analytics/` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--quiet` | ปิดการแสดงผลลัพธ์ล็อกปกติ | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Platform Support |
| :--- | :--- | :--- |
| `--time-range` | ช่วงเวลาสำหรับ metrics: `7d`, `30d`, `90d` หรือ `all` (ค่าเริ่มต้น: `30d`) | `cli` \| `kilo` \| `ci` \| `other` |
| `--compare-to` | เส้นทางไปยังรายงาน JSON ก่อนหน้าสำหรับการวิเคราะห์แนวโน้ม | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

workflow สร้างผลลัพธ์ตามการตั้งค่าของมัน

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/ui-analytics/<run-id>/report.md` | รายงาน analytics แบบครอบคลุม |
| `.spec/reports/ui-analytics/<run-id>/summary.json` | สรุป JSON ของข้อมูล analytics |
| `.spec/reports/ui-analytics/<run-id>/components.csv` | ส่งออก CSV ของ metrics ระดับ component |

### โครงสร้างรายงาน

รายงาน analytics รวมถึง:
- **Executive Summary:** ภาพรวม metrics สำคัญ
- **Component Usage:** Components ที่ใช้มากที่สุด/ไม่ได้ใช้, ความถี่การใช้งาน
- **Adoption Metrics:** Catalog coverage, components ใหม่/เลิกใช้
- **Quality Indicators:** Accessibility, performance, test coverage
- **Trends Analysis:** การเปลี่ยนแปลงตามเวลา (ถ้ามีการเปรียบเทียบ)
- **Component Health:** สถานะสุขภาพดี/ต้องการความสนใจ/วิกฤต
- **Recommendations:** รายการการดำเนินการที่จัดลำดับความสำคัญ

---

## 6. Analytics Metrics

### การใช้งาน Component

- **Usage Count:** จำนวนครั้งที่ component ถูกใช้
- **Usage Percentage:** เปอร์เซ็นต์ของการใช้งาน component ทั้งหมด
- **Top Components:** Components ที่ใช้บ่อยที่สุด
- **Unused Components:** Components ใน catalog แต่ไม่ได้ใช้

### Adoption Metrics

- **Catalog Coverage:** เปอร์เซ็นต์ของ catalog components ที่ implement แล้ว
- **Implementation Rate:** Components ที่ implement เทียบกับ catalog ทั้งหมด
- **New Components:** Components ที่เพิ่มในช่วงเวลา
- **Deprecated Components:** Components ที่ทำเครื่องหมายเพื่อลบ

### Quality Indicators

- **Accessibility Score:** คะแนน WCAG compliance เฉลี่ย (0-100)
- **Performance Score:** คะแนน performance metrics เฉลี่ย (0-100)
- **Test Coverage:** เปอร์เซ็นต์ของโค้ดที่ครอบคลุมด้วยการทดสอบ
  - Unit test coverage
  - Integration test coverage
  - E2E test coverage

### Component Health

- **Healthy:** metrics ทั้งหมดสูงกว่าเป้าหมาย
- **Needs Attention:** บาง metrics ต่ำกว่าเป้าหมาย
- **Critical:** หลาย metrics ต่ำกว่าเป้าหมายอย่างมีนัยสำคัญ

---

## 7. การวิเคราะห์แนวโน้ม

เมื่อมี `--compare-to` รายงานจะรวมถึง:

### Trend Metrics

- **Usage Trends:** การเปลี่ยนแปลงการใช้งาน component ตามเวลา
- **Quality Trends:** การเปลี่ยนแปลงคะแนน accessibility และ performance
- **Adoption Trends:** การเปลี่ยนแปลง catalog coverage
- **Health Trends:** การเปลี่ยนแปลงสถานะสุขภาพของ component

### Trend Indicators

- 📈 **Improving:** metrics เพิ่มขึ้นในทางบวก
- 📉 **Declining:** metrics ลดลงในทางลบ
- ➡️ **Stable:** metrics ไม่เปลี่ยนแปลงหรือผันผวนเล็กน้อย

---

## 8. หมายเหตุ

- **Platform Flag:** เมื่อใช้ Kilo Code ต้องระบุ `--platform kilo` เสมอเพื่อให้บริบทและการบันทึกถูกต้อง
- **Time Range:** ใช้ `30d` สำหรับการทบทวนรายเดือน, `7d` สำหรับการติดตามรายสัปดาห์, `all` สำหรับการวิเคราะห์แบบครอบคลุม
- **Comparison Reports:** บันทึก summary.json จากแต่ละรันเพื่อเปิดใช้งานการวิเคราะห์แนวโน้มในรันในอนาคต
- **Regular Reporting:** รัน analytics รายสัปดาห์หรือรายเดือนเพื่อติดตามความคืบหน้าและระบุปัญหาตั้งแต่เนิ่น ๆ
- **Team Sharing:** แชร์รายงานกับทีมเพื่อปรับปรุงการรับรู้และขับเคลื่อนการปรับปรุง
- **Action Items:** มุ่งเน้นที่คำแนะนำลำดับความสำคัญสูงจากรายงาน
- **การตั้งค่า:** workflow ปฏิบัติตามการตั้งค่าใน `.spec/smartspec.config.yaml`

---

**จบคู่มือ**
