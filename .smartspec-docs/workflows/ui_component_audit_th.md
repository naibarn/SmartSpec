| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_ui_component_audit คู่มือ (TH) | 6.0 | /smartspec_ui_component_audit | 6.0.x |

# /smartspec_ui_component_audit คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

workflow `/smartspec_ui_component_audit` ทำการตรวจสอบ (audit) ส่วนประกอบ UI เทียบกับข้อกำหนดการออกแบบและมาตรฐานการเข้าถึง (accessibility)

**วัตถุประสงค์:** ตรวจสอบส่วนประกอบ UI เทียบกับข้อกำหนดการออกแบบ เพื่อให้มั่นใจในความสอดคล้องและการปฏิบัติตามมาตรฐานการเข้าถึง

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** การทดสอบคุณภาพ

---

## 2. การใช้งาน

### 🔗 การใช้งาน CLI

```bash
/smartspec_ui_component_audit \
  <spec_md> \
  [--ui-file <path>] \
  [--a11y]
```

### การใช้งาน Kilo Code

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องใส่ flag `--platform kilo` เสมอ

```bash
/smartspec_ui_component_audit.md \
  <spec_md> \
  [--ui-file <path>] \
  [--a11y] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: ตรวจสอบส่วนประกอบ UI (CLI)

**สถานการณ์:** ตรวจสอบส่วนประกอบ UI เทียบกับ spec

**คำสั่ง:**

```bash
/smartspec_ui_component_audit specs/ui/dashboard/spec.md \
  --ui-file ui/dashboard.json
```

**ผลลัพธ์ที่คาดหวัง:**

1. ส่วนประกอบ UI ถูกตรวจสอบ
2. รายงานถูกสร้างขึ้น
3. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 2: ตรวจสอบการเข้าถึง (Accessibility Audit) (Kilo Code)

**สถานการณ์:** pipeline CI ตรวจสอบการเข้าถึง

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_ui_component_audit.md \
  specs/ui/forms/spec.md \
  --a11y \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. ดำเนินการตรวจสอบการเข้าถึง
2. รหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: ผลลัพธ์ JSON (CLI)

**สถานการณ์:** ผลลัพธ์การตรวจสอบในรูปแบบโครงสร้าง JSON

**คำสั่ง:**

```bash
/smartspec_ui_component_audit specs/ui/navigation/spec.md \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. ตรวจสอบพร้อมผลลัพธ์ในรูปแบบ JSON
2. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `<spec_md>` | `<path>` | เส้นทางไปยังไฟล์ spec.md | ต้องอยู่ภายใต้ `specs/**` |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--platform` | แพลตฟอร์มที่ใช้รัน **จำเป็นสำหรับ Kilo Code** | (อนุมานค่า) | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--ui-file` | เส้นทางไปยังไฟล์กำหนด UI | (ตรวจจับอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--a11y` | เปิดใช้งานการตรวจสอบการเข้าถึง | `false` | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/ui-component-audit/<run-id>/audit.md` | รายงานการตรวจสอบ |

---

## 6. หมายเหตุ

- **Platform Flag:** เมื่อใช้ Kilo Code ต้องใส่ `--platform kilo` เสมอ

---

**จบคู่มือ**