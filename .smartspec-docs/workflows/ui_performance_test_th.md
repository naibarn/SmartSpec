| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_ui_performance_test คู่มือ (TH) | 6.0 | /smartspec_ui_performance_test | 6.0.x |

# /smartspec_ui_performance_test คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

คู่มือนี้อธิบายวิธีการใช้งาน workflow:

`/smartspec_ui_performance_test` workflow ทำการทดสอบประสิทธิภาพแบบครอบคลุมบน UI components วัด Core Web Vitals, bundle size, render time และ performance metrics ที่สำคัญอื่น ๆ

**วัตถุประสงค์:** ทดสอบประสิทธิภาพของ UI component เทียบกับเป้าหมายที่กำหนด วัด Core Web Vitals (LCP, FID, CLS) วิเคราะห์ bundle size และระบุคอขวดด้านประสิทธิภาพ

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** ui_optimization_and_analytics

---

## 2. การใช้งาน

workflow นี้รองรับการเรียกใช้งานผ่าน Command Line Interface (CLI) และ Kilo Code (สภาพแวดล้อมสคริปต์ภายใน)

### 🔗 การใช้งาน CLI

การเรียกใช้งาน CLI จำเป็นต้องระบุ UI spec, implementation และ target platform

```bash
/smartspec_ui_performance_test \
  --spec <path/to/ui-spec.json> \
  --implementation <path/to/implementation> \
  --platform <web-lit|web-react|web-angular|mobile-flutter> \
  [--compare-to <baseline.json>] \
  [--runs <number>]
```

### การใช้งาน Kilo Code

การเรียกใช้งาน Kilo Code มีโครงสร้างเหมือนกับ CLI โดยมักใช้ใน pipeline อัตโนมัติหรือสคริปต์ภายใน

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องระบุ flag `--platform kilo` นอกเหนือจาก UI platform

```bash
/smartspec_ui_performance_test.md \
  --spec <path/to/ui-spec.json> \
  --implementation <path/to/implementation> \
  --ui-platform <web-lit|web-react|web-angular|mobile-flutter> \
  [--compare-to <baseline.json>] \
  [--runs <number>] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: การทดสอบประสิทธิภาพพื้นฐาน (CLI)

**สถานการณ์:** นักพัฒนาต้องการวัดประสิทธิภาพของ component ฟอร์มติดต่อ

**คำสั่ง:**

```bash
/smartspec_ui_performance_test \
  --spec specs/feature/spec-005-contact/ui-spec.json \
  --implementation src/ui/ContactForm.tsx \
  --platform web-lit
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow วิเคราะห์ bundle size
2. วัดเวลา initial render
3. คำนวณ Core Web Vitals (LCP, FID, CLS)
4. สร้างรายงานประสิทธิภาพพร้อม metrics
5. เปรียบเทียบกับเป้าหมาย (bundle < 50KB, render < 200ms, ฯลฯ)
6. รหัสออก `0` ถ้า metrics ทั้งหมดผ่าน, `1` ถ้ามีใดไม่ผ่าน

### กรณีการใช้งาน 2: การทดสอบ Performance Regression (Kilo Code)

**สถานการณ์:** CI pipeline รันการทดสอบประสิทธิภาพและเปรียบเทียบกับ baseline เพื่อตรวจจับ regressions

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_ui_performance_test.md \
  --spec specs/feature/spec-005-contact/ui-spec.json \
  --implementation src/ui/ContactForm.tsx \
  --ui-platform web-lit \
  --compare-to .spec/reports/ui-performance/baseline.json \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow รันการทดสอบประสิทธิภาพ
2. เปรียบเทียบผลลัพธ์กับ baseline metrics
3. ระบุ performance regressions
4. รายงานเปอร์เซ็นต์การเปลี่ยนแปลงสำหรับแต่ละ metric
5. รหัสออก `0` ถ้าไม่มี regressions, `1` ถ้าตรวจพบ regressions

### กรณีการใช้งาน 3: ประสิทธิภาพเฉลี่ยจากหลายรัน (CLI)

**สถานการณ์:** นักพัฒนาต้องการ performance metrics ที่แม่นยำโดยการเฉลี่ยจากหลายรัน

**คำสั่ง:**

```bash
/smartspec_ui_performance_test \
  --spec specs/feature/spec-005-contact/ui-spec.json \
  --implementation src/ui/ContactForm.tsx \
  --platform web-react \
  --runs 5 \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow รันการทดสอบประสิทธิภาพ 5 ครั้ง
2. คำนวณ metrics เฉลี่ยจากทุกรัน
3. รายงาน standard deviation สำหรับความแปรปรวน
4. ผลลัพธ์รวมไฟล์ `performance-report.json` พร้อมผลลัพธ์เฉลี่ย
5. รหัสออก `0` (สำเร็จ)

---

## 4. พารามิเตอร์

พารามิเตอร์และ flags ต่อไปนี้ใช้ควบคุมการทำงานและพฤติกรรมของ `/smartspec_ui_performance_test` workflow

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `--spec` | `<string>` | เส้นทางไปยังไฟล์ UI specification JSON | ต้องมีอยู่จริงและเป็น JSON ที่ถูกต้อง |
| `--implementation` | `<string>` | เส้นทางไปยังไฟล์หรือไดเรกทอรี implementation | ต้องมีอยู่จริงและเข้าถึงได้ |
| `--platform` (หรือ `--ui-platform` สำหรับ Kilo) | `<string>` | UI platform: `web-lit`, `web-react`, `web-angular` หรือ `mobile-flutter` | ต้องเป็นหนึ่งในค่าที่อนุญาต |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--config` | เส้นทางไปยังไฟล์กำหนดค่า SmartSpec | `.spec/smartspec.config.yaml` | `cli` \| `kilo` \| `ci` \| `other` |
| `--lang` | ภาษาสำหรับการสร้างรายงาน (เช่น `th`, `en`) | (ค่าเริ่มต้นของระบบ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--platform` | บริบทแพลตฟอร์มการทำงาน **จำเป็นสำหรับ Kilo Code** | (อนุมานอัตโนมัติ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--out` | เส้นทางฐานสำหรับผลลัพธ์ที่ปลอดภัย | `.spec/reports/ui-performance/` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--quiet` | ปิดการแสดงผลลัพธ์ล็อกปกติ | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Platform Support |
| :--- | :--- | :--- |
| `--compare-to` | เส้นทางไปยัง baseline performance JSON สำหรับการทดสอบ regression | `cli` \| `kilo` \| `ci` \| `other` |
| `--runs` | จำนวนรันการทดสอบเพื่อเฉลี่ย (ค่าเริ่มต้น: 1) | `cli` \| `kilo` \| `ci` \| `other` |
| `--all-platforms` | ทดสอบบนทุก platforms ที่รองรับ | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

workflow สร้างผลลัพธ์ตามการตั้งค่าของมัน

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/ui-performance/<run-id>/performance-report.md` | รายงานการทดสอบประสิทธิภาพโดยละเอียด |
| `.spec/reports/ui-performance/<run-id>/summary.json` | สรุป JSON ของ performance metrics |
| `.spec/reports/ui-performance/<run-id>/metrics.csv` | ส่งออก CSV ของ metrics สำหรับการวิเคราะห์ |

### โครงสร้างรายงาน

รายงานประสิทธิภาพรวมถึง:
- **Summary:** สถานะผ่าน/ไม่ผ่านโดยรวม
- **Bundle Size Analysis:** ขนาดรวม, JavaScript, CSS, assets
- **Render Performance:** Initial render, re-render, mount time
- **Core Web Vitals:** LCP, FID, CLS พร้อมเป้าหมาย
- **Memory Usage:** Heap size, allocations
- **Network Performance:** Load time, requests
- **Recommendations:** ข้อเสนอแนะการเพิ่มประสิทธิภาพ

---

## 6. Performance Metrics

### เป้าหมายประสิทธิภาพ

| Metric | เป้าหมาย | คำอธิบาย |
| :--- | :--- | :--- |
| **Bundle Size** | < 50 KB | ขนาด bundle รวมของ component |
| **Initial Render** | < 200ms | เวลาถึง render ครั้งแรก |
| **LCP** | < 2.5s | Largest Contentful Paint |
| **FID** | < 100ms | First Input Delay |
| **CLS** | < 0.1 | Cumulative Layout Shift |

### Core Web Vitals

#### LCP (Largest Contentful Paint)
- **ดี:** < 2.5s
- **ต้องปรับปรุง:** 2.5s - 4.0s
- **แย่:** > 4.0s

#### FID (First Input Delay)
- **ดี:** < 100ms
- **ต้องปรับปรุง:** 100ms - 300ms
- **แย่:** > 300ms

#### CLS (Cumulative Layout Shift)
- **ดี:** < 0.1
- **ต้องปรับปรุง:** 0.1 - 0.25
- **แย่:** > 0.25

---

## 7. เกณฑ์ผ่าน/ไม่ผ่าน

### เงื่อนไขผ่าน

metrics ทั้งหมดต้องตรงตามเป้าหมาย:
- Bundle size < 50 KB
- Initial render < 200ms
- LCP < 2.5s
- FID < 100ms
- CLS < 0.1

### ระดับสถานะ

- ✅ **PASS:** metrics ทั้งหมดอยู่ในเป้าหมาย
- ⚠️ **WARNING:** 1-2 metrics เกินเป้าหมายเล็กน้อย
- ❌ **FAIL:** 3+ metrics เกินหรือ metric สำคัญเกินอย่างมีนัยสำคัญ

---

## 8. หมายเหตุ

- **Platform Flag:** เมื่อใช้ Kilo Code ใช้ `--ui-platform` สำหรับ UI framework และ `--platform kilo` สำหรับบริบทการทำงาน
- **Baseline Comparison:** สร้าง baseline ด้วยรันแรก จากนั้นใช้ `--compare-to` สำหรับการทดสอบ regression
- **Multiple Runs:** ใช้ `--runs 5` หรือมากกว่าสำหรับค่าเฉลี่ยที่แม่นยำ เนื่องจากประสิทธิภาพอาจแปรปรวน
- **Real Devices:** ทดสอบบนอุปกรณ์จริงเมื่อเป็นไปได้ ไม่ใช่แค่ emulators
- **Network Conditions:** พิจารณาทดสอบภายใต้สภาวะเครือข่ายต่าง ๆ (3G, 4G, WiFi)
- **การรวม CI:** รันการทดสอบประสิทธิภาพใน CI เพื่อจับ regressions ตั้งแต่เนิ่น ๆ
- **การตั้งค่า:** workflow ปฏิบัติตามการตั้งค่าใน `.spec/smartspec.config.yaml`

---

**จบคู่มือ**
