| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_validate_index คู่มือ (TH) | 6.0 | /smartspec_validate_index | 6.0.x |

# /smartspec_validate_index คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

คู่มือนี้อธิบายวิธีการใช้งาน workflow:

`/smartspec_validate_index` workflow ใช้ตรวจสอบความสมบูรณ์และความสอดคล้องของไฟล์ `.spec/SPEC_INDEX.json` เทียบกับไฟล์สเปคจริง

**วัตถุประสงค์:** ตรวจสอบความสมบูรณ์และความสอดคล้องของดัชนีสเปค เพื่อให้แน่ใจว่าการอ้างอิงทั้งหมดถูกต้องและ metadata มีความถูกต้อง

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** การจัดการดัชนี

---

## 2. การใช้งาน

workflow นี้รองรับการเรียกใช้งานผ่าน Command Line Interface (CLI) และ Kilo Code (สภาพแวดล้อมสคริปต์ภายใน)

### 🔗 การใช้งาน CLI

```bash
/smartspec_validate_index \
  [--fix] \
  [--strict]
```

### การใช้งาน Kilo Code

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code จำเป็นต้องใส่ flag `--platform kilo`

```bash
/smartspec_validate_index.md \
  [--fix] \
  [--strict] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: ตรวจสอบความสมบูรณ์ของดัชนี (CLI)

**สถานการณ์:** นักพัฒนาต้องการตรวจสอบว่าดัชนีสเปคถูกต้องและสอดคล้องหรือไม่

**คำสั่ง:**

```bash
/smartspec_validate_index
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow จะตรวจสอบไฟล์ `.spec/SPEC_INDEX.json`  
2. ตรวจสอบการอ้างอิงที่เสียหายและ metadata ที่ไม่ถูกต้อง  
3. สร้างรายงานการตรวจสอบ  
4. ส่งรหัสออก `0` หากถูกต้อง, `1` หากพบข้อผิดพลาด

### กรณีการใช้งาน 2: แก้ไขปัญหาดัชนีโดยอัตโนมัติ (Kilo Code)

**สถานการณ์:** pipeline CI ต้องการตรวจสอบและแก้ไขปัญหาดัชนีโดยอัตโนมัติ

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_validate_index.md \
  --fix \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow ตรวจสอบดัชนี  
2. แก้ไขปัญหาที่ตรวจพบโดยอัตโนมัติ  
3. เขียนดัชนีที่อัปเดตแล้ว  
4. ส่งรหัสออก `0` (สำเร็จ)

### กรณีการใช้งาน 3: โหมดตรวจสอบเข้มงวด (CLI)

**สถานการณ์:** ผู้ใช้ต้องการการตรวจสอบแบบเข้มงวดที่ล้มเหลวเมื่อพบคำเตือนหรือความไม่สอดคล้องใด ๆ

**คำสั่ง:**

```bash
/smartspec_validate_index \
  --strict \
  --json
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow ทำการตรวจสอบแบบเข้มงวด  
2. คำเตือนใด ๆ จะถูกนับเป็นข้อผิดพลาด  
3. ผลลัพธ์ออกมาในรูปแบบไฟล์ `validation.json` ที่มีรายละเอียด  
4. ส่งรหัสออก `0` หากสมบูรณ์แบบ, `1` หากพบปัญหาใด ๆ

---

## 4. พารามิเตอร์

### Flags ทั่วไป

| Flag | คำอธิบาย | ค่าเริ่มต้น | รองรับแพลตฟอร์ม |
| :--- | :--- | :--- | :--- |
| `--config` | เส้นทางไปยังไฟล์การตั้งค่า SmartSpec | `.spec/smartspec.config.yaml` | `cli` \| `kilo` \| `ci` \| `other` |
| `--lang` | ภาษาในการสร้างรายงาน | (ค่าเริ่มต้นของระบบ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--platform` | บริบทแพลตฟอร์มที่ใช้รัน **จำเป็นสำหรับ Kilo Code** | (อนุมานค่า) | `cli` \| `kilo` \| `ci` \| `other` |
| `--out` | เส้นทางฐานสำหรับผลลัพธ์ที่ปลอดภัย | `.spec/reports/validate-index/` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--quiet` | ปิดการแสดงผลล็อกปกติ | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | คำอธิบาย | ค่าเริ่มต้น | รองรับแพลตฟอร์ม |
| :--- | :--- | :--- | :--- |
| `--fix` | แก้ไขปัญหาที่ตรวจพบโดยอัตโนมัติ | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--strict` | เปิดโหมดตรวจสอบเข้มงวด (ล้มเหลวเมื่อพบคำเตือน) | `false` | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

### ไฟล์ผลลัพธ์

| File Path | คำอธิบาย |
| :--- | :--- |
| `.spec/reports/validate-index/<run-id>/validation.md` | รายงานการตรวจสอบ |
| `.spec/SPEC_INDEX.json` | ดัชนีที่ถูกแก้ไข (เฉพาะเมื่อใช้ `--fix`) |

---

## 6. หมายเหตุ

- **Flag แพลตฟอร์ม:** เมื่อใช้ Kilo Code ต้องใส่ `--platform kilo` เสมอ  
- **โหมดแก้ไข:** ใช้ `--fix` เพื่อซ่อมแซมปัญหาที่ตรวจพบโดยอัตโนมัติ  
- **โหมดเข้มงวด:** ใช้ `--strict` เพื่อบังคับการตรวจสอบแบบไม่ยอมให้มีข้อผิดพลาดหรือคำเตือน  
- **การตั้งค่า:** workflow จะใช้การตั้งค่าจากไฟล์ `.spec/smartspec.config.yaml`

---

**จบคู่มือ**