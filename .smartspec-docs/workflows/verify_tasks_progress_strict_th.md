# คู่มือ Workflow SmartSpec แบบเข้มงวด (Strict Verification)
## `/smartspec_verify_tasks_progress_strict` — ระบบตรวจสอบความคืบหน้าจากหลักฐานจริง 100%
**เวอร์ชันภาษาไทย — คู่มือเฉพาะสำหรับ Workflow แบบ Strict**

---
# 1. วัตถุประสงค์ (Purpose)
`/smartspec_verify_tasks_progress_strict` คือ workflow รุ่นใหม่ที่ถูกออกแบบให้ **เข้มงวดที่สุด**, **เชื่อหลักฐานเท่านั้น**, และ **ไม่สน checkbox** ใน `tasks.md` อีกต่อไป

Workflow ตัวนี้จะ:
- ตรวจสอบความคืบหน้าของงานจาก **โค้ดจริง + เทสจริง + เอกสาร + ไฟล์ deploy**
- ไม่พึ่ง script ภายนอก (ไม่ต้องใช้ `check_tasks_evidence.py`)
- ใช้ตัวตรวจสอบหลักฐาน (Evidence Providers) ที่ฝังอยู่ใน workflow โดยตรง
- ให้ผลลัพธ์เป็น verdict ราย task ว่าเสร็จจริงหรือไม่
- ให้เหตุผลครบถ้วนเมื่อไม่พบหลักฐาน (missing evidence reasons)
- ป้องกันปัญหา verify → implement → verify วนลูป

Workflow นี้เป็น **read-only** (ไม่แก้ spec, ไม่แก้ tasks, ไม่แก้โค้ด)

---
# 2. คุณสมบัติเด่น (Key Features)
### ✔ 2.1 ระบบ Evidence-First 100%
งานจะถือว่า “เสร็จจริง” เมื่อพบหลักฐานดังต่อไปนี้:
- ไฟล์ implementation
- ไฟล์ unit test / integration test / performance test
- เส้นทาง API (routes)
- เอกสารประกอบ (docs)
- ไฟล์ระบบ deployment / CI / monitoring

ไม่มีหลักฐาน = ไม่ผ่าน

---
### ✔ 2.2 Verdict ที่เข้มงวด (Strict Verdict System)
| Verdict | ความหมาย |
|--------|-----------|
| **complete** | หลักฐานครบทุกอย่างตามที่ task ต้องมี |
| **unsynced_complete** | ทำเสร็จจริง แต่ checkbox ไม่ถูกติ๊ก |
| **false_positive** | Checkbox ติ๊กว่าเสร็จ แต่ไม่มีหลักฐานจริง |
| **partial** | มีบางส่วน แต่ยังไม่ครบ |
| **incomplete** | ไม่มีหลักฐานเลย |

เหมาะสำหรับงานสำคัญ, งาน EPIC, งานที่ต้องอ้างอิงการตรวจสอบแบบ audit

---
### ✔ 2.3 Evidence Provider แบบ Built-in (ไม่ต้องใช้ script เพิ่ม)
Workflow จะตรวจหลักฐานโดยอัตโนมัติจากโครงสร้างโปรเจกต์ทั่วไป เช่น:

#### ● Route Detector
ค้นหาไฟล์เส้นทาง (API routes):
- `src/routes/**/*`
- `src/http/**/*`

ใช้ regex เพื่อจับคู่คำสั่ง HTTP เช่น GET, POST, PATCH

#### ● Service Detector
ค้นหา service class/function:
- `src/services/**/*`
- `src/modules/**/services/**/*`

#### ● Test Detector
ค้นหาไฟล์เทส:
- `tests/unit/**/*`
- `tests/integration/**/*`
- `tests/performance/**/*`
- `__tests__/**/*`

#### ● Documentation Detector
ค้นหาใน `docs/**/*`:
- เอกสาร API (Swagger / OpenAPI)
- เอกสาร deployment

#### ● Deployment Detector
ค้นหาไฟล์ระบบ:
- `k8s/**/*`
- `deploy/**/*`
- `.github/workflows/**/*`

---
### ✔ 2.4 Evidence Config (ไม่บังคับ แต่ช่วยให้แม่นยำมากขึ้น)
ไฟล์ config ราย spec เช่น:
```
specs/.../evidence.yaml
```
ตัวอย่างเนื้อหา:
```yaml
tasks:
  T047:
    endpoints:
      - method: POST
        path: /api/v1/users/me/phone
    tests:
      - tests/integration/users.phone.test.ts
```

ถ้ามี config → workflow จะตรวจแบบละเอียด  
ถ้าไม่มี → ใช้ heuristic อัตโนมัติ

---
### ✔ 2.5 การตรวจจับปัญหา Loop (Anti-Loop Diagnostics)
ถ้า task ใดไม่มี evidence ซ้ำหลายรอบ  
workflow จะออกคำเตือนแบบนี้:

```
WARNING: T047 ไม่มีหลักฐานที่ตรวจพบแม้ตรวจซ้ำหลายรอบ
อาจเกิดจาก mapping task ผิด หรือ implementation อยู่ใน path นอกมาตรฐาน
```

ช่วยป้องกันการวนลูป implement → verify แบบไม่รู้จบ

---
# 3. วิธีใช้งาน (Inputs & Flags)
## 3.1 Inputs หลัก
```
--spec=<path>        # ไฟล์ spec.md
--tasks=<path>       # ไม่บังคับ หากไม่มีก็หาอัตโนมัติ
```

## 3.2 กำหนด Evidence Config
```
--evidence-config=<path>
```
ตัวเลือกนี้ช่วยเพิ่มความแม่นยำของการตรวจสอบ

## 3.3 ตัวเลือกในการออก Report
```
--report-dir=<path>
--report=<summary|detailed>
--report-format=<md|json|both>
--dry-run
```

## 3.4 โหมด Strict
```
--safety-mode=strict
--strict
```
Strict = ขาดหลักฐาน → incomplete ทันที

---
# 4. ขั้นตอนการตรวจสอบ (Evaluation Flow)
### ขั้นที่ 1 — โหลด Spec และ Tasks
### ขั้นที่ 2 — โหลด evidence-config (ถ้ามี)
### ขั้นที่ 3 — แปลง tasks.md เป็นโครงสร้างข้อมูล
### ขั้นที่ 4 — เก็บหลักฐานสำหรับแต่ละ task
### ขั้นที่ 5 — คำนวณ verdict แบบเข้มงวด
### ขั้นที่ 6 — สร้างรายงาน Markdown / JSON

---
# 5. ตัวอย่าง JSON Output
```json
{
  "spec_path": "specs/.../spec.md",
  "tasks_path": "specs/.../tasks.md",
  "summary": {
    "total": 78,
    "complete": 45,
    "unsynced_complete": 3,
    "false_positive": 4,
    "partial": 16,
    "incomplete": 10,
    "progress_percent": 61.5,
    "risk": "HIGH"
  }
}
```

---
# 6. ตัวอย่างคำสั่งใช้งาน
## แบบพื้นฐาน
```bash
/smartspec_verify_tasks_progress_strict \
  --spec specs/.../spec.md
```

## ออกผล JSON
```bash
/smartspec_verify_tasks_progress_strict \
  --spec specs/.../spec.md \
  --report-format=json
```

## ใช้ evidence-config
```bash
/smartspec_verify_tasks_progress_strict \
  --spec specs/.../spec.md \
  --evidence-config specs/.../evidence.yaml
```

## รายงานแบบละเอียดทุก task
```bash
/smartspec_verify_tasks_progress_strict \
  --spec specs/.../spec.md \
  --report=detailed --report-format=both
```

---
# 7. กฎ Governance
Workflow นี้ต้อง:
- **ห้ามแก้** spec.md
- **ห้ามแก้** tasks.md
- **ห้ามแก้** โค้ดหรือไฟล์โปรเจกต์
- ออกได้เฉพาะรายงาน markdown / JSON
- ไม่จำเป็นต้อง compatible กับ workflow รุ่นเก่า (ออกแบบมาแทนที่โดยตรง)

---
# 8. ควรใช้เมื่อไหร่?
เหมาะอย่างยิ่งสำหรับ:
- งานที่ต้องการความถูกต้องสูง เช่น EPIC / ระบบ auth / ระบบสำคัญ
- ใช้ใน CI/CD เพื่อประเมินความพร้อมก่อน deploy
- ใช้ตรวจความคืบหน้า sprint แบบอิงหลักฐานจริง
- ใช้ตรวจจับงานที่ “ติ๊กว่าเสร็จ แต่ไม่เสร็จจริง”

---
# 9. หากต้องการคู่มือแบบ PDF หรือรวมสองภาษา
แจ้งได้เลย ผมสามารถจัดรูปแบบให้พร้อมดาวน์โหลดได้ทันที

---
# จบคู่มือ 🚀