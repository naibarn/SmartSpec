# คู่มือการใช้งาน SmartSpec Workflow
## `/smartspec_verify_tasks_progress` — ระบบตรวจสอบความคืบหน้าตามหลักฐานจริง (Evidence‑First)
**เวอร์ชันภาษาไทย — คู่มือแบบแยกอิสระ**

---
# 1. วัตถุประสงค์ของ Workflow
`/smartspec_verify_tasks_progress` คือ workflow อย่างเป็นทางการของ SmartSpec สำหรับ
**ตรวจสอบความคืบหน้าของงาน (tasks)** ว่า *ทำจริงตามโค้ดหรือไม่*

Workflow นี้ใช้หลักการ **Evidence‑First**:
- Checkbox ใน `tasks.md` เป็นเพียง *ตัวบอกสัญญาณ* เท่านั้น
- ความจริงว่างานเสร็จหรือไม่ จะวัดจาก **โค้ดจริง, เทสจริง, เอกสารจริง และไฟล์ deployment จริง**
- งานจะถูกนับว่าเสร็จ **ก็ต่อเมื่อ** พบหลักฐานจริงรองรับ
- Workflow นี้เป็น **แบบอ่านอย่างเดียว (Read‑Only)** — ไม่แก้ไข `tasks.md`, `spec.md`, โค้ด หรือ registry ใด ๆ

คู่มือนี้อธิบายวิธีใช้งาน workflow นี้อย่างละเอียด

---
# 2. Workflow นี้ทำอะไรบ้าง
### ✔ อ่านและวิเคราะห์ไฟล์ดังต่อไปนี้
- `spec.md`
- `tasks.md`
- ไฟล์โค้ด: services, routes, helpers
- ไฟล์เทส: unit / integration / performance
- เอกสารประกอบ (docs)
- ไฟล์ deployment เช่น Kubernetes, CI/CD, Monitoring
- หลักฐานเพิ่มเติมจากสคริปต์ที่ผู้ใช้กำหนด (`--evidence-script`)

### ✔ ผลลัพธ์ที่สร้างขึ้น
- รายงาน **Markdown** (อ่านง่ายสำหรับทีม)
- รายงาน **JSON** (ใช้สำหรับ automation เช่น sync checkboxes)

### ✔ ให้ผลลัพธ์เป็น “verdict” ต่อ task แต่ละรายการ
| Verdict | ความหมาย |
|--------|-----------|
| **complete** | มีหลักฐานครบ โค้ด + เทสตรงตามที่ต้องการ |
| **unsynced_complete** | งานเสร็จจริง แต่ checkbox ยังไม่ถูกติ๊ก |
| **false_positive** | ถูกติ๊กว่าเสร็จ แต่ไม่พบหลักฐานในโค้ด |
| **partial** | ทำไปบางส่วน แต่ไม่ครบ |
| **incomplete** | ยังไม่มีหลักฐานเลย |

Workflow นี้ทำหน้าที่ให้ **ตัวเลขความคืบหน้าที่ไม่สามารถโกงได้**
เพราะอิงจากหลักฐานล้วน ๆ

---
# 3. รายละเอียด Flags / Options
## 3.1 กลุ่ม Evidence‑First (ใหม่ใน v5.8)
```bash
--evidence-only
```
ไม่สน checkbox เลย ใช้เฉพาะหลักฐานในการคำนวณความคืบหน้า

```bash
--run-tests
```
รันเทสเพื่อประเมินความสมบูรณ์ของงาน
- ถ้าเทส fail → verdict จะถูกลดเหลือ `partial`

```bash
--evidence-script=<path>
```
สั่ง workflow ให้รันสคริปต์ตรวจสอบเพิ่มเติมที่ผู้ใช้เขียนเอง เช่น
```bash
scripts/check_tasks_evidence.py
```
สคริปต์ต้องคืนค่าเป็น JSON แล้ว workflow จะรวมเข้ากับหลักฐานอื่น ๆ

---
## 3.2 การเลือกไฟล์ Spec / Tasks
```bash
--spec=<path>
--tasks=<path>
--spec-id=<id>
```
กรณีไม่ระบุ `--tasks` ระบบจะค้นหา `tasks.md` ที่อยู่คู่กับ `spec.md` อัตโนมัติ

---
## 3.3 Legacy Flags (ของเดิมยังอยู่ครบ)
### SPEC_INDEX / Registry
```bash
--index=<path>
--specindex=<path>
--registry-dir=<path>
--registry-roots=<csv>
```

### Multi‑Repo
```bash
--workspace-roots=<csv>
--repos-config=<path>
```

### Reporting
```bash
--report-dir=<path>
--report=<summary|detailed>
--report-format=<md|json>
--dry-run
```

### Safety
```bash
--safety-mode=<strict|dev>
--strict
```

> ฟังก์ชันทั้งหมดจาก v5.7 ยังคงทำงานเหมือนเดิม 100%

---
# 4. รูปแบบ JSON Output
ใช้สำหรับ automation เช่น `/smartspec_sync_tasks_checkboxes`

ตัวอย่าง:
```json
{
  "spec_id": "spec-002-user-management",
  "tasks_path": "specs/feature/spec-002-user-management/tasks.md",
  "summary": {
    "total_tasks": 78,
    "complete": 40,
    "unsynced_complete": 5,
    "false_positive": 6,
    "partial": 15,
    "incomplete": 12,
    "progress_percent": 57.7,
    "risk_level": "MEDIUM"
  }
}
```

---
# 5. วิธีประเมินผลลัพธ์ของ Workflow
### ขั้นตอนที่ 1 — อ่าน `tasks.md`
- ดึง ID ของ task
- อ่าน checkbox
- ตรวจลำดับชั้น (parent/subtask)

### ขั้นตอนที่ 2 — เก็บหลักฐาน
- ค้นหาโค้ดที่เกี่ยวข้อง
- ค้นหาเทสที่เกี่ยวข้อง
- อ่านผลลัพธ์จาก evidence script
- อ่านผลลัพธ์การรันเทส (ถ้ามี)

### ขั้นตอนที่ 3 — ให้ verdict ตามกฎ
- evidence = complete → `complete` / `unsynced_complete`
- evidence = none + checkbox checked → `false_positive`
- evidence = none → `incomplete`
- evidence = partial → `partial`

### ขั้นตอนที่ 4 — สร้างรายงาน
ทั้ง Markdown และ/หรือ JSON ตาม flags ที่ระบุ

---
# 6. ตัวอย่างคำสั่งใช้งานจริง
## 6.1 ตรวจสอบแบบพื้นฐาน
```bash
/smartspec_verify_tasks_progress \
  --spec specs/feature/spec-002-user-management/spec.md
```

## 6.2 สร้างรายงานแบบ JSON (สำหรับ automation)
```bash
/smartspec_verify_tasks_progress \
  --spec specs/feature/spec-002-user-management/spec.md \
  --report-format=json --report=summary
```

## 6.3 ใช้สคริปต์ตรวจสอบเพิ่มเติม
```bash
/smartspec_verify_tasks_progress \
  --spec specs/feature/spec-002-user-management/spec.md \
  --evidence-script=scripts/check_tasks_evidence.py
```

## 6.4 ให้ workflow รันเทสด้วย
```bash
/smartspec_verify_tasks_progress \
  --spec specs/.../spec.md \
  --run-tests --report-format=json
```

---
# 7. กฎ Governance ที่ต้องปฏิบัติตาม
Workflow นี้ต้องปฏิบัติตามกฎต่อไปนี้อย่างเคร่งครัด:
- **ห้ามแก้ไข**
  - spec.md
  - tasks.md
  - source code
  - SPEC_INDEX
  - registry
  - UI JSON
- อนุญาตให้เขียนได้เฉพาะ
  - รายงาน Markdown
  - รายงาน JSON

Workflow นี้เป็น *read‑only* 100%

---
# 8. แนวทางใช้งานที่แนะนำสำหรับทีมพัฒนา
1. เขียนโค้ดและเทสตาม tasks
2. รัน:
   ```bash
   /smartspec_verify_tasks_progress --report-format=json
   ```
3. อ่านรายงาน JSON เพื่อดู task ไหนทำจริงแล้ว
4. (หากต้องการ) ใช้ workflow sync checkboxes แยกต่างหาก
5. นำผลที่ได้ไปใช้วางแผน sprint ต่อไปอย่างแม่นยำ

---
# 9. ใช้เมื่อไหร่?
ใช้ workflow นี้เมื่อคุณต้องการ:
- ตรวจสอบความคืบหน้าที่ **ตรงตามความจริง 100%**
- ไม่ต้องการให้ใครบอกว่า "ทำเสร็จแล้ว" แต่ไม่เห็นโค้ด
- ต้องการรายงานใช้ใน sprint review
- ต้องการหลักฐานสำหรับ governance / audit
- ต้องการตรวจสอบระบบขนาดใหญ่ (EPIC‑scale)

---
# 10. หากต้องการคู่มือภาษาอังกฤษ
คู่มือภาษาอังกฤษถูกสร้างแล้วใน Canvas แยกอีกไฟล์หนึ่ง

หากต้องการรวมสองภาษาหรือสร้าง PDF ก็แจ้งได้เลยครับ 🚀

