| manual_name | manual_version | compatible_workflow | compatible_workflow_versions |
|-------------|----------------|---------------------|------------------------------|
| /smartspec_verify_tasks_progress คู่มือ (TH) | 6.0 | /smartspec_verify_tasks_progress | 6.0.x |

# /smartspec_verify_tasks_progress คู่มือ (v6.0, ภาษาไทย)

## 1. ภาพรวม

คู่มือนี้อธิบายวิธีการใช้งาน workflow:

`/smartspec_verify_tasks_progress` workflow ทำการตรวจสอบแบบเข้มงวดโดยใช้หลักฐานที่สามารถแยกวิเคราะห์ได้ (parseable evidence hooks) เพื่อยืนยันความสมบูรณ์ของงานที่ทำเสร็จแล้ว

**วัตถุประสงค์:** การตรวจสอบแบบเข้มงวดโดยใช้หลักฐานที่สามารถแยกวิเคราะห์ได้ (evidence: type key=value...) เพื่อให้มั่นใจว่างานที่ทำเสร็จทั้งหมดมีหลักฐานที่ตรวจสอบได้ใน codebase

**เวอร์ชัน:** 6.0  
**หมวดหมู่:** verify

---

## 2. การใช้งาน

workflow นี้รองรับการเรียกใช้งานผ่าน Command Line Interface (CLI) และ Kilo Code (สภาพแวดล้อมสคริปต์ภายใน)

### 🔗 การใช้งาน CLI

การเรียกใช้งาน CLI ต้องระบุไฟล์ tasks.md เป้าหมาย

```bash
/smartspec_verify_tasks_progress \
  <tasks_md> \
  [--report-format <markdown|json>] \
  [--strict]
```

### การใช้งาน Kilo Code

การเรียกใช้งาน Kilo Code มีโครงสร้างเหมือนกับ CLI โดยปกติใช้ใน pipeline อัตโนมัติหรือสคริปต์ภายใน

**หมายเหตุสำคัญ:** เมื่อใช้ Kilo Code ต้องระบุ flag `--platform kilo` ด้วยเสมอ

```bash
/smartspec_verify_tasks_progress.md \
  <tasks_md> \
  [--report-format <markdown|json>] \
  [--strict] \
  --platform kilo
```

---

## 3. กรณีการใช้งาน

### กรณีการใช้งาน 1: ตรวจสอบความคืบหน้างานพร้อมหลักฐาน (CLI)

**สถานการณ์:** นักพัฒนาต้องการตรวจสอบว่างานที่ถูกติ๊กทำเสร็จมีหลักฐานที่ถูกต้องใน codebase

**คำสั่ง:**

```bash
/smartspec_verify_tasks_progress specs/auth/login_service/tasks.md
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดไฟล์ `specs/auth/login_service/tasks.md`  
2. สแกนหา evidence hooks ใน codebase สำหรับงานที่ถูกติ๊กทำเสร็จแต่ละงาน  
3. สร้างรายงานการตรวจสอบแสดงสถานะหลักฐาน  
4. คืน exit code `0` หากตรวจสอบผ่านทั้งหมด, `1` หากพบการละเมิด

### กรณีการใช้งาน 2: การตรวจสอบแบบเข้มงวดสำหรับ CI Pipeline (Kilo Code)

**สถานการณ์:** pipeline CI ต้องการการตรวจสอบแบบเข้มงวดที่งานที่ถูกติ๊กทำเสร็จทั้งหมดต้องมีหลักฐานที่สามารถแยกวิเคราะห์ได้

**คำสั่ง (ตัวอย่าง Kilo Code):**

```bash
/smartspec_verify_tasks_progress.md \
  specs/payments/checkout/tasks.md \
  --strict \
  --report-format json \
  --platform kilo
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดไฟล์ tasks ในโหมดเข้มงวด  
2. ตรวจสอบหลักฐานสำหรับงานที่ถูกติ๊กทำเสร็จทั้งหมด  
3. งานที่ถูกติ๊กแต่ไม่มีหลักฐานที่ถูกต้องจะทำให้ล้มเหลว  
4. ผลลัพธ์มีไฟล์ `verification.json` ที่มีรายละเอียดผลการตรวจสอบ  
5. คืน exit code `0` หากผ่านทั้งหมด, `1` หากพบการละเมิด

### กรณีการใช้งาน 3: รายงาน Markdown สำหรับการทบทวนของทีม (CLI)

**สถานการณ์:** หัวหน้าทีมต้องการรายงานที่อ่านง่ายสำหรับการทบทวนงานของทีม

**คำสั่ง:**

```bash
/smartspec_verify_tasks_progress specs/dashboard/analytics/tasks.md \
  --report-format markdown
```

**ผลลัพธ์ที่คาดหวัง:**

1. workflow โหลดไฟล์ tasks  
2. ตรวจสอบหลักฐานสำหรับงานที่ถูกติ๊กทำเสร็จทั้งหมด  
3. สร้างรายงานในรูปแบบ markdown ที่มีรายละเอียดผลการตรวจสอบ  
4. คืน exit code `0` (สำเร็จ)

---

## 4. พารามิเตอร์

พารามิเตอร์และ flags ต่อไปนี้ควบคุมการทำงานและพฤติกรรมของ `/smartspec_verify_tasks_progress` workflow

### พารามิเตอร์ที่จำเป็น

| Parameter | Type | Description | Validation |
| :--- | :--- | :--- | :--- |
| `<tasks_md>` | `<path>` | เส้นทางไปยังไฟล์ tasks.md ที่ต้องการตรวจสอบ | ต้องอยู่ภายใต้ `specs/**` |

### Flags ทั่วไป

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--config` | เส้นทางไปยังไฟล์ตั้งค่า SmartSpec | `.spec/smartspec.config.yaml` | `cli` \| `kilo` \| `ci` \| `other` |
| `--lang` | ภาษาในการสร้างรายงาน (เช่น `th`, `en`) | (ค่าเริ่มต้นระบบ) | `cli` \| `kilo` \| `ci` \| `other` |
| `--platform` | บริบทแพลตฟอร์มที่ใช้รัน **จำเป็นสำหรับ Kilo Code** | (อนุมานค่า) | `cli` \| `kilo` \| `ci` \| `other` |
| `--out` | เส้นทางฐานสำหรับผลลัพธ์ที่ปลอดภัย | `.spec/reports/verify-tasks-progress/` | `cli` \| `kilo` \| `ci` \| `other` |
| `--json` | ผลลัพธ์ในรูปแบบ JSON | `false` | `cli` \| `kilo` \| `ci` \| `other` |
| `--quiet` | ปิดการแสดงผลลัพธ์ใน stdout | `false` | `cli` \| `kilo` \| `ci` \| `other` |

### Flags เฉพาะ workflow

| Flag | Description | Default | Platform Support |
| :--- | :--- | :--- | :--- |
| `--report-format` | รูปแบบรายงาน: `markdown` หรือ `json` | `markdown` | `cli` \| `kilo` \| `ci` \| `other` |
| `--strict` | เปิดโหมดตรวจสอบเข้มงวด (ล้มเหลวหากขาดหลักฐาน) | `false` | `cli` \| `kilo` \| `ci` \| `other` |

---

## 5. ผลลัพธ์

workflow จะสร้างผลลัพธ์ตามการตั้งค่าที่กำหนด

### ไฟล์ผลลัพธ์

| File Path | Description |
| :--- | :--- |
| `.spec/reports/verify-tasks-progress/<run-id>/verification.md` | รายงานการตรวจสอบในรูปแบบ markdown |
| `.spec/reports/verify-tasks-progress/<run-id>/verification.json` | รายงานการตรวจสอบในรูปแบบ JSON (ถ้าใช้ `--report-format json`) |

---

## 6. หมายเหตุ

- **Flag แพลตฟอร์ม:** เมื่อใช้ Kilo Code ต้องระบุ `--platform kilo` เสมอเพื่อให้บริบทและการบันทึกถูกต้อง  
- **Evidence Hooks:** งานต้องมี evidence hooks ที่สามารถแยกวิเคราะห์ได้ (เช่น `evidence: file path=src/auth.ts`) เพื่อใช้ตรวจสอบ  
- **โหมดเข้มงวด:** ใช้ `--strict` เพื่อให้ workflow ล้มเหลวหากงานที่ถูกติ๊กไม่มีหลักฐานที่ถูกต้อง  
- **รูปแบบรายงาน:** เลือกรูปแบบระหว่าง markdown สำหรับอ่านคน หรือ JSON สำหรับประมวลผลโปรแกรม  
- **การตั้งค่า:** workflow จะเคารพการตั้งค่าใน `.spec/smartspec.config.yaml`

---

**จบคู่มือ**