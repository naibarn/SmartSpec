/**
 * Auth Middleware
 *
 * JWT verification and authorization middleware
 * Generated by SmartSpec Auth Generator
 */
import { Request, Response, NextFunction } from 'express';
import { JWTService } from '../services/jwt.service';
import { User, UserRole } from '../types/auth.types';
export interface AuthRequest extends Request {
    user?: User;
}
export declare class AuthMiddleware {
    private jwtService;
    constructor(jwtService: JWTService);
    /**
     * Verify JWT token and attach user to request
     */
    authenticate: (req: AuthRequest, res: Response, next: NextFunction) => Promise<void>;
    /**
     * Check if user has required role
     */
    requireRole: (allowedRoles: UserRole[]) => (req: AuthRequest, res: Response, next: NextFunction) => Promise<void>;
    /**
     * Optional authentication - attach user if token is valid, but don't fail if not
     */
    optionalAuth: (req: AuthRequest, res: Response, next: NextFunction) => Promise<void>;
    /**
     * Check if account is locked
     */
    checkAccountLockout: (req: AuthRequest, res: Response, next: NextFunction) => Promise<void>;
    /**
     * Require email verification
     */
    requireEmailVerified: (req: AuthRequest, res: Response, next: NextFunction) => Promise<void>;
}
/**
 * Helper to create middleware instance
 */
export declare function createAuthMiddleware(jwtService: JWTService): AuthMiddleware;
//# sourceMappingURL=auth.middleware.d.ts.map