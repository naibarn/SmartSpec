/**
 * Auth Service (Generated Template)
 *
 * This is a template that shows the structure.
 * The actual implementation is in src/services/auth.service.ts
 * Generated by SmartSpec Auth Generator
 */
import { JWTService } from './jwt.service';
import { PasswordService } from './password.service';
import { User, RegisterInput, LoginInput, RegisterResult, LoginResult, TokenPair } from '../types/auth.types';
export declare class AuthService {
    private jwtService;
    private passwordService;
    constructor(jwtService: JWTService, passwordService: PasswordService);
    /**
     * Register new user
     */
    register(input: RegisterInput): Promise<RegisterResult>;
    /**
     * Login user
     */
    login(input: LoginInput, user: User): Promise<LoginResult>;
    /**
     * Refresh tokens
     */
    refreshTokens(refreshToken: string, user: User): Promise<TokenPair>;
    /**
     * Verify email
     */
    verifyEmail(token: string, user: User): Promise<void>;
    /**
     * Request password reset
     */
    requestPasswordReset(email: string): Promise<string>;
    /**
     * Reset password
     */
    resetPassword(token: string, newPassword: string, user: User): Promise<void>;
    /**
     * Change password (authenticated user)
     */
    changePassword(user: User, currentPassword: string, newPassword: string): Promise<void>;
    /**
     * Generate secure random token
     */
    private generateSecureToken;
}
//# sourceMappingURL=auth.service.d.ts.map