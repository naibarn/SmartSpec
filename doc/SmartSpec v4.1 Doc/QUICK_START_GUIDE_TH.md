# SmartSpec v4.0 - คู่มือเริ่มต้นอย่างรวดเร็ว

**เวอร์ชัน:** 4.0.0  
**ภาษา:** ไทย  
**สำหรับ:** ผู้ใช้งานครั้งแรกหรือต้องการคู่มือรวดเร็ว

---

## เริ่มต้นภายใน 5 นาที

### สิ่งที่คุณต้องมี

✅ `.smartspec/` directory พร้อมไฟล์ Knowledge Base  
✅ `SPEC_INDEX.json` (หรือจะใช้ default ก็ได้)  
✅ ความคิดหรือ requirement ของโปรเจค

---

## ขั้นตอนพื้นฐาน (4 ขั้นตอน)

### 1️⃣ สร้าง SPEC

```bash
# บอกระบบว่าต้องการสร้างอะไร
"สร้างระบบ authentication ด้วย JWT และ refresh token"
```

**ผลลัพธ์:**
- `specs/feature/spec-XXX/spec.md`

**ใช้เวลา:** ~2 นาที

---

### 2️⃣ สร้าง Tasks

```bash
# ใช้ spec.md ที่ได้จากขั้นตอนที่ 1
specs/feature/spec-XXX/spec.md
```

**ผลลัพธ์:**
- `tasks.md` - รายการงานทั้งหมด
- `README.md` - คู่มือ (ถ้าจำเป็น)
- `data-model.md` - โครงสร้างข้อมูล (ถ้าจำเป็น)
- `openapi.yaml` - API spec (ถ้าจำเป็น)

**ใช้เวลา:** ~3 นาที

---

### 3️⃣ สร้าง Implementation Prompt

```bash
# ใช้ tasks.md ที่ได้จากขั้นตอนที่ 2
specs/feature/spec-XXX/tasks.md
```

**ผลลัพธ์:**
- `kilo-prompt.md` - คำสั่งสำหรับ implement

**ใช้เวลา:** ~1 นาที

---

### 4️⃣ เริ่ม Implement

**ตัวเลือก A: ใช้ Kilo Code** (แนะนำสำหรับโปรเจคใหญ่)
```bash
kilo specs/feature/spec-XXX/kilo-prompt.md
```

**ตัวเลือก B: ใช้ Claude Code** (แนะนำสำหรับโปรเจคเล็ก)
- เปิด Claude Code ใน terminal
- Copy เนื้อหาจาก kilo-prompt.md
- Execute ทีละ task

---

## กรณีใช้งานทั่วไป

### กรณี 1: เริ่มโปรเจคใหม่ทั้งหมด

```
1. สร้าง SPEC → spec.md
2. สร้าง Plan (optional) → plan.md  
3. สร้าง Tasks → tasks.md
4. สร้าง Prompt → kilo-prompt.md
5. Implement → ใช้ Kilo หรือ Claude Code
```

**ใช้เวลารวม:** ~10-15 นาที (ไม่รวม implementation)

---

### กรณี 2: อัปเดต SPEC ที่มีอยู่

```
1. แก้ไข SPEC
   specs/feature/spec-004/spec.md

2. ตรวจสอบความสอดคล้อง
   sync_spec_tasks spec.md
   
3. อัปเดต tasks.md (ถ้าจำเป็น)

4. สร้าง prompt ใหม่
   generate_kilo_prompt_v4 tasks.md
```

---

### กรณี 3: เช็คความคืบหน้า

```
verify_tasks_progress tasks.md

→ จะได้ progress-report-*.md
  - แสดง % ความสำเร็จ
  - Tasks ที่เสร็จแล้ว
  - Tasks ที่ติดขัด
  - ปัญหาที่พบ
```

---

## พารามิเตอร์ที่ควรรู้

### --specindex

**ใช้เมื่อ:** มี SPEC_INDEX แยกหรือไม่ใช้ default

```bash
specs/feature/spec-004/spec.md --specindex="custom/index.json"
```

---

### --nogenerate

**ใช้เมื่อ:** อยากดูแผนก่อนสร้างจริง (Dry run)

```bash
generate_tasks_v4 spec.md --nogenerate

# ดูผลลัพธ์
# ถ้า OK แล้ว:
generate_tasks_v4 spec.md
```

---

## เคล็ดลับสำคัญ ⭐

### 1. ใช้ --nogenerate สำหรับครั้งแรก

```bash
# ทดลองดูก่อน
command --nogenerate

# ถ้าชอบแล้วค่อยรันจริง
command
```

### 2. เก็บ SPEC_INDEX ให้เป็นปัจจุบัน

- อัปเดตทุกครั้งที่เพิ่ม spec ใหม่
- ใส่ path และ repo ให้ครบ
- Format ให้ถูกต้อง

### 3. ตรวจสอบความสอดคล้องเป็นประจำ

```bash
# หลังแก้ spec
sync_spec_tasks spec.md

# หลังทำงานไปสักระยะ
verify_tasks_progress tasks.md
```

### 4. ใช้ Supporting Files

- ปล่อยให้ระบบ auto-generate
- Review แล้วปรับปรุงตามต้องการ
- เก็บไฟล์ไว้ใน directory เดียวกับ spec.md

---

## แก้ปัญหาเบื้องต้น

### ปัญหา: หา SPEC_INDEX ไม่เจอ

**แก้:**
```bash
# ตรวจสอบว่ามีไฟล์
ls .smartspec/SPEC_INDEX.json

# หรือระบุ path เอง
command --specindex="path/ที่ถูกต้อง.json"
```

---

### ปัญหา: Tasks ไม่ตรงกับ Spec

**แก้:**
```bash
# รัน sync checker
sync_spec_tasks spec.md

# ดู sync-report
# อัปเดต tasks.md ตามรายงาน
```

---

### ปัญหา: ไม่แน่ใจจะใช้ workflow ไหน

**แก้:**
- ดู: `WORKFLOW_DECISION_TREE.md`
- หรือถามตัวเอง:
  - ต้องการ spec? → generate_spec_v4
  - ต้องการ tasks? → generate_tasks_v4
  - ต้องการ prompt? → generate_kilo_prompt_v4
  - ต้องการเช็คความคืบหน้า? → verify_tasks_progress

---

## เอกสารเพิ่มเติม

📚 **คู่มือฉบับเต็ม:**
- `SMARTSPEC_SYSTEM_GUIDE.md` - ภาพรวมระบบทั้งหมด
- `WORKFLOW_DECISION_TREE.md` - ตัดสินใจเลือก workflow

📖 **คู่มือเฉพาะเรื่อง:**
- `SUPPORTING_FILES_GUIDE.md` - Supporting files คืออะไร
- `SPEC_INDEX_GUIDE.md` - จัดการ SPEC_INDEX
- `TESTING_WORKFLOWS_GUIDE.md` - ทดสอบ workflows

💡 **ตัวอย่าง:**
- `COMPLETE_EXAMPLE.md` - ตัวอย่างแบบเต็ม end-to-end

---

## ติดต่อและขอความช่วยเหลือ

### ต้องการความช่วยเหลือ?

1. ดูคู่มือที่เกี่ยวข้อง
2. ลอง --nogenerate ดูผลลัพธ์
3. ตรวจสอบ .smartspec/ files
4. ดูตัวอย่างใน COMPLETE_EXAMPLE.md

---

## สรุป - เริ่มต้นได้ทันที

```bash
# 1. สร้าง SPEC
"สร้างระบบที่คุณต้องการ..."

# 2. สร้าง Tasks
specs/feature/spec-XXX/spec.md

# 3. สร้าง Prompt
specs/feature/spec-XXX/tasks.md

# 4. Implement
kilo kilo-prompt.md
# หรือ
# ใช้ Claude Code แบบ interactive
```

**เท่านี้ก็เริ่มได้แล้ว! 🚀**

---

**เวอร์ชัน:** 4.0.0  
**อัปเดตล่าสุด:** 3 ธันวาคม 2025  
**ภาษา:** ไทย
